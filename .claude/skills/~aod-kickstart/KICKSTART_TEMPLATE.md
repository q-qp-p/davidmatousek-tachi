# Kickstart Template

This template defines the exact structure for consumer guides generated by `/aod.kickstart`. Replace all `{placeholders}` with actual content. Reference guides in `docs/guides/Archive/` demonstrate the expected quality and detail level.

---

## Header

```markdown
# Consumer Guide — {project_name} ({stack_tech})

**Purpose**: {one-sentence purpose derived from idea_description}
**What you're building**: {1-2 sentence description from idea_description}
**Target user**: {target_user from Stage 1}
**Key constraints**:
- {constraint derived from stack and capabilities}
- {constraint derived from stack and capabilities}

**How to use this guide**: Work through each seed feature in order using the AOD lifecycle. Copy each feature block into `/aod.discover` to create a GitHub Issue, then run `/aod.define` through `/aod.deliver` to implement it.
```

Optional: Add a **Related documents** section if the project has companion guides (research docs, integration guides, etc.).

---

## Prerequisites

Always include these standard items, then add project-specific tools:

```markdown
## Prerequisites

- Claude Code installed (`claude` CLI)
- Git 2.30+ installed
- GitHub CLI (`gh`) installed and authenticated
- A GitHub account with repo creation permissions
- {additional tools and runtimes from pack's STACK.md or custom stack, if any}
```

---

## Phase 1: Clone & Initialize

Every guide MUST include all of these elements. Do not abbreviate.

```markdown
## Phase 1: Clone & Initialize

Navigate to your projects directory (e.g., `~/Projects/` or `~/code/`) — the clone command will create a new subfolder here:

\```bash
# Clone the public template
git clone https://github.com/davidmatousek/agentic-oriented-development-kit.git {project_slug}
cd {project_slug}

# Run interactive setup
make init
\```

**When prompted, enter:**

| Prompt | Value |
|--------|-------|
| Project Name | `{project_slug}` |
| Description | `{one-line purpose from idea_description}` |
| GitHub Org | `{github_org}` |
| GitHub Repo | `{project_slug}` |
| AI Agent | `1` (Claude Code) |
| Tech Stack | {see Tech Stack guidance below} |

\```bash
# Verify setup
make check
\```

**Expected output:**
- All checks pass (green checkmarks)
- Stack packs available
- No pack active yet

### Post-Init Verification

Confirm that `make init` replaced all template placeholders:

\```bash
# Should return NO results — all placeholders replaced
grep -rn '{{' .aod/memory/constitution.md
\```
```

### Tech Stack guidance

- **Pack-based**: `Select \`{selected_pack}\` — "{stack_description}"` with a `> **Note**:` explaining what the pack auto-fills or why it's the right fit.
- **Custom stack**: `Select \`Other\`` with a `> **Note**:` explaining what to enter for database/cloud provider prompts and why no bundled pack matches.

---

## Phase 2: Create GitHub Repo

```markdown
## Phase 2: Create GitHub Repo

\```bash
# Create a GitHub repo (needed for issue tracking)
gh repo create {github_org}/{project_slug} --public --source=. --push

# If you get "Unable to add remote origin" (origin exists from the clone), run:
git remote set-url origin https://github.com/{github_org}/{project_slug}.git
git push -u origin main
\```
```

If the project needs additional setup after repo creation (e.g., copying guide files from the AOD Kit, setting up a GitHub Projects board), include those steps here with clear instructions and paths.

---

## Phase 3: Activate Stack Pack & Scaffold (pack-based)

```markdown
## Phase 3: Activate Stack Pack & Scaffold

Open Claude Code in your project directory:

\```bash
claude
\```

Run these commands inside Claude Code:

\```
# List available packs — verify {selected_pack} shows up
/aod.stack list

# Activate the pack
/aod.stack use {selected_pack}

# Scaffold the project structure
/aod.stack scaffold
\```

### Verification Checklist

After activation:
- [ ] `.aod/stack-active.json` exists with `"pack": "{selected_pack}"`
- [ ] `.claude/rules/stack/` contains conventions and persona-loader rules

After scaffold:
- [ ] {expected directories/files specific to this pack's scaffold}
```

### Custom stack alternative (Phase 3)

When no pack is selected, replace Phase 3 with **Custom Stack Setup**:

```markdown
## Phase 3: Custom Stack Setup

Open Claude Code in your project directory:

\```bash
claude
\```

Create the project directory structure by typing this into Claude Code:

\```
Create the project directory structure for a {custom_stack_description}:
- {dir_1}/ — {purpose}
- {dir_2}/ — {purpose}
- {dir_3}/ — {purpose}
- Add {config files} for {purpose}
- Add a .env.example with {relevant env var placeholders}
\```

> **Note**: No stack pack scaffold for this custom stack — the prompt above creates the base structure. Feature-specific files get added as you build each feature through the AOD lifecycle.
```

---

## Phase 4: Install Dependencies

```markdown
## Phase 4: Install Dependencies

{dependency install commands appropriate to the stack}
```

- For pack-based projects: derive commands from pack conventions (e.g., `npm install`, `uv sync`, `pip install -r requirements.txt`)
- For custom stacks: list specific packages with a table explaining each one's purpose
- For projects with no runtime dependencies (e.g., markdown-only): omit this phase entirely or state "No external dependencies required"
- If external services need setup (e.g., Supabase project, API keys), include sub-steps with dashboard instructions

---

## Phase 5: AOD Lifecycle (Governance)

```markdown
## Phase 5: AOD Lifecycle (Governance)

For each feature in dependency order, seed it into a GitHub Issue and then run the full Triad workflow.

### Quick Start Commands

Copy-paste these commands one at a time into Claude Code to seed all features into GitHub Issues. Each command reads the full epic (stories, acceptance criteria, interface contracts, DoD) directly from this guide:

\```
/aod.discover --seed read #### F-001: {F-001 name} in docs/guides/CONSUMER_GUIDE_{file_name}.md
/aod.discover --seed read #### F-002: {F-002 name} in docs/guides/CONSUMER_GUIDE_{file_name}.md
...
/aod.discover --seed read #### F-NNN: {F-NNN name} in docs/guides/CONSUMER_GUIDE_{file_name}.md
\```

> **Tip**: Each command reads the full feature section from this guide and creates a GitHub Issue with the complete epic content. You can seed all features at once, then work through them in order.

### Triad Workflow (per feature)

After seeding, run the Triad workflow for each feature starting with F-001:

\```
/aod.define
/aod.plan
/aod.build
/aod.deliver
\```

### Governance Verification

- [ ] `.aod/spec.md` contains PM sign-off block
- [ ] `.aod/plan.md` contains PM + Architect sign-off blocks
- [ ] `.aod/tasks.md` contains PM + Architect + Team-Lead sign-off blocks
- [ ] Feature branch follows `NNN-feature-name` format
- [ ] Each governance gate required approval before proceeding
```

---

## Requirements (optional)

If the project has high-level functional requirements that span multiple features, include a Requirements section between the setup phases and the Feature Summary:

```markdown
## Requirements

### Functional Requirements
1. **{Requirement name}**: {description}
2. **{Requirement name}**: {description}
```

This section is optional — only include when the project has cross-cutting requirements that help readers understand the big picture before diving into individual features.

---

## Feature Summary Table

```markdown
## Feature Summary

| ID | Feature | Group | Stories | Depends On |
|----|---------|-------|---------|------------|
| F-001 | {name} | Foundation | {count} | — |
| F-002 | {name} | Foundation | {count} | F-001 |
| F-003 | {name} | Core | {count} | F-001, F-002 |
```

Every feature must appear in this table. The `Depends On` column uses feature IDs or `—` for no dependencies.

---

## Seed Feature Blocks

Each seed feature uses this exact format (the entire block from `####` to `---` is the copy unit for `/aod.discover`):

```markdown
### Group {N}: {Group Name}

#### F-NNN: {Feature Name}

**Goal**: {one-sentence goal statement}

**Stories**:

1. **As a {role}, I want {capability}**, so that {value}.
   - {acceptance criterion}
   - {acceptance criterion}

2. **As a {role}, I want {capability}**, so that {value}.
   - {acceptance criterion}

**Interface Contract (produces)**:
- {what this feature produces that downstream features depend on}

**Definition of Done**: {completion criteria summary}

---
```

**Requirements per block**:
- 2-5 user stories per feature
- Each story has 1-3 acceptance criteria
- Interface contract specifies what this feature produces for dependents
- DoD is a single summary sentence
- The `---` separator after each block is required

---

## Tail Sections

After all seed feature blocks, include these sections in order:

```markdown
## How to Execute Each Feature

For each feature in dependency order:
1. Use the Quick Start Command from Phase 5 to seed it into a GitHub Issue
2. Run `/aod.define` to create the PRD
3. Run `/aod.plan` to create spec, plan, and tasks
4. Run `/aod.build` to implement
5. Run `/aod.deliver` to close

## Feature Completion Tracker

| ID | Feature | Status |
|----|---------|--------|
| F-001 | {name} | [ ] |
| F-002 | {name} | [ ] |

## Success Criteria
- All {feature_count} features implemented and delivered
- Each feature independently demonstrable
- Valid dependency chain maintained throughout
- {2-3 project-specific success criteria}

---

## Troubleshooting

| Issue | Resolution |
|-------|------------|
| `make init` fails | Ensure Git is installed (`git --version`) |
| `/aod.stack use` says pack not found | Verify `stacks/{selected_pack}/STACK.md` exists |
| Scaffold conflicts with existing files | Choose overwrite/skip per-file when prompted |
| Governance sign-off loops | Address reviewer feedback, re-submit until APPROVED |
| `gh repo create` fails | Ensure `gh auth login` completed successfully |
| {2-3 project-specific troubleshooting rows} |

---

## Notes

- This guide uses the **public template** repo (`agentic-oriented-development-kit`)
- {2-3 project-specific notes about architecture, constraints, or related documents}
- Core governance agents (PM, Architect, Team-Lead) are **not** affected by stack packs — they remain stack-agnostic
```

---
description: "Unified project setup & story generation — auto-detects new vs existing projects"
---

## Overview

Transform a project idea into a registered project with dependency-ordered, ICE-scored stories ready for the AOD lifecycle. Auto-detects new vs existing projects and supports interactive intake or demo mode (`--demo`).

## User Input

```text
$ARGUMENTS
```

## Execution

Invoke the `~aod-blueprint` skill via the Skill tool. Pass any user arguments through.

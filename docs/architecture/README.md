# Architecture Documentation - tachi

**Last Updated**: 2026-05-04
**Owner**: Architect
**Status**: Template

---

## Overview

This directory contains all technical architecture documentation for tachi.

**Canonical MAESTRO Worked Example**: For the end-to-end demonstration of the MAESTRO umbrella (Phases 1-5 delivered across Features 084 / 136 / 141 / 142 / 143 / 144), see [`examples/maestro-reference/`](../../examples/maestro-reference/) — the Healthcare CDSS multi-agent reference scenario spanning all seven MAESTRO layers, with cross-layer attack chains, agentic pattern classifications, and compliance posture cross-references (ADR-024 AIVSS, ADR-025 NIST AI RMF) surfaced end-to-end by a single pipeline run. Delivered in Feature 145 (content-authoring only — zero code, schema, or agent changes).

---

## Structure

### 00_Tech_Stack/
Technology choices and justifications
- `README.md` - Tech stack overview with {{TEMPLATE_VARIABLES}}

### 01_system_design/
High-level system design and component diagrams
- `README.md` - System architecture overview (auto-scaffolded from plan.md by `/aod.project-plan` after Architect sign-off)
- `upstream-sync-architecture.md` - Repo topology and sync flow between private, public template, and adopter projects
- Component interaction diagrams
- Data flow diagrams

### 02_ADRs/ (Architecture Decision Records)
Significant technical decisions with context and trade-offs
- `ADR-000-template.md` - ADR template and example
- `ADR-001-atomic-state-persistence.md` - Write-then-rename for orchestrator state (Feature 022)
- `ADR-002-prompt-segmentation.md` - On-demand reference file loading for context efficiency (Feature 030)
- `ADR-005-serialization-trade-off.md` - Serialization trade-off for parallel Triad reviews (Feature 047)
- `ADR-006-non-fatal-observability-operations.md` - Non-fatal error handling for observability and circuit-breaker functions (Feature 054)
- `ADR-007-stack-pack-dual-surface-injection.md` - Dual-surface injection pattern for stack pack context loading (Feature 058)
- `ADR-008-opt-out-flag-for-default-quality-gates.md` - Opt-out flag (`--no-simplify`) for default-on quality gate steps in commands (Feature 065)
- `ADR-009-template-variable-expansion-scope.md` - Expanding `tachi` placeholder to all user-facing template files (Feature 061)
- `ADR-010-minimal-return-architecture.md` - Minimal return architecture for subagent→main token efficiency (Feature 073)
- `ADR-011-multi-flag-opt-out-and-step-insertion-pattern.md` - Multi-flag opt-out pattern and step insertion convention for `/aod.build` (Feature 080)
- `ADR-012-cross-agent-correlation-detection.md` - Deterministic cross-agent correlation detection algorithm (Feature 010)
- `ADR-013-sarif-output-format-adoption.md` - SARIF 2.1.0 output format for GitHub Code Scanning integration (Feature 012)
- `ADR-014-gemini-api-optional-image-generation.md` - Optional Gemini API image generation for infographic agent (Feature 018)
- `ADR-015-platform-adapter-hub-and-spoke-distribution.md` - Hub-and-spoke distribution pattern for platform adapters (Feature 021)
- `ADR-019-shared-definitions-and-model-field-governance.md` - Shared cross-agent definitions and model field governance (Feature 078)
- `ADR-020-maestro-layer-classification.md` - CSA MAESTRO seven-layer taxonomy for agentic AI component classification (Feature 084; Revision History adds canonical layer rename rule for enum-value-only minor schema bumps in Feature 136)
- `ADR-021-source-date-epoch-for-deterministic-pdf-comparison.md` - SOURCE_DATE_EPOCH reproducible-builds convention for byte-deterministic PDF baseline comparison (Feature 128)
- `ADR-022-mmdc-hard-prerequisite.md` - `mmdc` (Mermaid CLI) as hard prerequisite gated on attack-tree detection; establishes fail-loud-on-missing-CLI posture with defense-in-depth preflight gates — first ADR governing CLI-prerequisite posture (Feature 130)
- `ADR-023-threat-agent-skill-references-pattern.md` - Detection variant of the lean + skill references pattern as a sibling to the methodology variant; single-point `**MANDATORY**: Read` at detection start for all 11 threat agents (6 STRIDE + 5 AI); MAESTRO classification remains orchestrator-owned; shared reference edits are additive-only; `finding-format-shared.md` gains "For Threat Agents" producer section. Completes the lean-agent architecture migration for all 17 tachi agents (Feature 082)
- `ADR-024-owasp-aivss-evaluation.md` - **Accepted (2026-04-15)**: tachi diverges from OWASP AIVSS v0.8 at the present time; existing four-dimensional weighted-sum composite (`0.35 × CVSS 3.1 + 0.30 × Exploitability + 0.20 × Reachability + 0.15 × Scalability`) remains the canonical scoring model. Records the three-surface evaluation (dimension set, composite formula, severity bands) against AIVSS v0.8, establishes structural divergence on Surfaces A (dimensions) and B (formula) with alignment only on Surface C (severity bands), and defines re-evaluation triggers (AIVSS v1.0 + at least one external adopter case study). Cross-references ADR-020 (MAESTRO classification), ADR-019 (shared definitions), ADR-018 (baseline-aware scoring lineage), and ADR-025 (companion NIST AI RMF evaluation). Documentation-only spike — zero schema, script, or pipeline changes (Feature 143)
- `ADR-025-nist-ai-rmf-evaluation.md` - **Accepted (2026-04-16)**: tachi adopts a **documentation-only NIST AI RMF 1.0 mapping posture** (Option A) — no schema, pipeline, or runtime changes; the mapping lives in this ADR and a companion skill reference (`.claude/skills/tachi-shared/references/nist-ai-rmf-mapping.md`). Records the three-surface evaluation of AI RMF 1.0 and the NIST AI 600-1 Generative AI Profile: Surface A (AI RMF Functions Govern/Map/Measure/Manage × tachi pipeline phases — 3 Overlap, 1 structural Gap on Govern), Surface B (16 representative AI RMF Subcategories × 8 tachi compensating-control categories — 8 Overlap, 2 Gap, 2 No equivalent; MEASURE 2.7 is the strongest direct mapping), Surface C (12 NIST AI 600-1 GAI risks × tachi STRIDE+AI categories — 4 Overlap, 1 Gap, 7 No equivalent; §2.9 Information Security is the strongest direct mapping). Captures the structural tier-mismatch insight: AI RMF Functions are *organizational-tier outcomes* while tachi produces *artifact-tier evidence* — this tier gap is why Option A (docs-only) was chosen over deeper wired integration. Distinct from ADR-024's pre-1.0 AIVSS divergence: AI RMF 1.0 is mature (3+ year runway through 2028, federal procurement adoption, FFIEC/HIPAA references), so maturity is a permission rather than a blocker. Cross-references ADR-024 (companion AIVSS), ADR-020 (MAESTRO classification), ADR-019 (shared definitions), ADR-018 (baseline lineage), ADR-021 (determinism), ADR-023 (skill-references pattern). Closes the MAESTRO compliance umbrella together with Features 084/141/136/082/143. Documentation-only spike — zero code, schema, agent, or example changes (Feature 144)
- `ADR-026-pattern-classification-mechanism.md` - **Accepted (2026-04-16)**: records the **Hybrid Post-Hoc Synthesis (Option C)** decision for MAESTRO Phase 3 agentic pattern classification. Adds orchestrator **Phase 3.6 (Pattern Synthesis Engine)** running after Feature 141's Phase 3.5 cross-layer chain correlation and before Phase 4 assessment; introduces `agentic_pattern` enum field on `schemas/finding.yaml` (8 values: 6 canonical CSA MAESTRO patterns + `none` + `multiple`, default `none`) with schema version bumped **1.3 → 1.4**; extends the Feature 136 enum-VALUE-rename minor-bump rule to cover **NEW enum-typed field additions** where the new field is additive, has a default, and the schema shape is unchanged. Establishes a durable **governance rule for future post-hoc synthesis phases**: finding-level metadata synthesis MAY write back to the finding IR (Phase 3.6 model); cross-finding aggregates remain aggregate-only / separate-artifact (Phase 3.5 model). Preserves the zero-edit invariant on the 11 detection agents (6 STRIDE + 5 AI) stabilized in Feature 082 per ADR-023 governance. Cross-references ADR-019 (shared definitions), ADR-020 (MAESTRO classification — extended here with Phase 3), ADR-021 (determinism), ADR-023 (skill-references pattern) (Feature 142)
- `ADR-027-taxonomy-crosswalk-schema.md` - **Accepted (2026-04-17)**: records 8 numbered decisions for the F-A1 machine-readable taxonomy catalog + crosswalk foundation (`schemas/taxonomy/` directory, 9 files, 526 primary edges). Per-item record shape (5-field `{id, full_id, name, url, cwe_refs}` with unidirectional OWASP→CWE rule), per-edge record shape, 7-value `taxonomy` enum (closed for F-A1), 3-value `edge_type` enum (only `primary` populated in F-A1; `related` / `superseded` deferred to F-A1.2), 3-value `confidence` enum with anti-drift rule (downgrade to weaker label if curator cannot articulate a one-sentence citation supporting `high`/`medium`), citation non-empty-and-resolvable rule, **Interpretation C single-feature cadence exception** (bounded to foundation-data features; does NOT set precedent for downstream gap-closure features like F-A2), and the **Proposed → Accepted dual-commit governance protocol** mirroring ADR-024/025 precedent. Cross-references ADR-020/021/023/024/025/026 (Feature 180)
- `ADR-028-source-attribution-schema-extension.md` - **Accepted (2026-04-17)**: records 7 numbered decisions for the F-A2 finding-level `source_attribution` contract. Minor-bump 1.4 → 1.5 under the **Complex-Shape Addition Clarifier** extending the ADR-026 minor-bump rule from NEW enum-typed SCALAR field additions to NEW list-of-RECORD field additions — first such extension in tachi schema evolution. Q1 serialization surface (conditional `## 9. Source Attribution` YAML block in `threats.md` per Feature 141 `has-attack-chains` precedent), closed 5-value `taxonomy` enum (external frameworks only — deliberately distinct-scope subset of ADR-027's 7-value catalog enum), closed 3-value `relationship` enum (`primary` / `related` / `derived`, default `primary`), Q2 referential-integrity contract (two-tier validation with separate post-parse helper `validate_source_attribution` invoked from orchestrator Phase 4), 22-file zero-edit invariant preserved (11 STRIDE+AI agent files + 11 companion `detection-patterns.md` skill-references), Proposed → Accepted dual-commit governance protocol. Cross-references ADR-020/021/023/026/027 (Feature 189)
- `ADR-029-coverage-attestation-report-section.md` - **Accepted (2026-04-18)**: records 8 numbered decisions for the F-B coverage-attestation PDF section. Decision 2 (three-state classification rubric: Covered / Partial / Gap) with Decision 3 (catalog-length denominator) computed from `source_attribution` arrays over the 5 external-framework catalogs. One new Typst page template (`templates/tachi/security-report/coverage-attestation.typ`) conditionally included via `has-source-attribution` boolean flag (Feature 141 / 112 precedent). Aggregator surface in `scripts/extract-report-data.py` with lazy `import yaml` inside `_load_framework_yaml_records` to preserve stdlib-only module-load invariant (post-merge refinement). 22-file zero-edit invariant preserved. Cross-references ADR-020/021/023/027/028 (Feature 194)
- `ADR-030-output-integrity-agent.md` - **Accepted (2026-04-18)**: records 8 numbered decisions for the F-1 `output-integrity` threat agent — **first net-new threat detection agent under BLP-01 Tier 1** and **first downstream consumer of F-A1 + F-A2 + F-B Foundation tier**. (D1) adopt new AI-tier agent for OWASP LLM05:2025 output-handling coverage closure; (D2) Heuristic A Outcome B split scope — F-1 owns encoding/sanitization primitives, F-4 `trust-exploitation` forward-referenced for human-victim signal class per BLP-01 §8; (D3) lean-agent detection variant per ADR-023; (D4) LLM05 + ML09:2023 documentation-only bundling (ML09 cited in prose but NOT in `source_attribution`); (D5) 22-file zero-edit invariant preserved with grep-auditable enumeration; (D6) Proposed → Accepted dual-commit with post-merge SHA fill; (D7) `category: llm` enum reuse (no new category value required since `OI-{N}` ID prefix plus `source_attribution.id: LLM05` carry sufficient discrimination); (D8) **ADR-026 Complex-Shape Clarifier extended to regex-alternation prefix additions** — first such extension; `schemas/finding.yaml` bumped 1.5 → 1.6 with `id.pattern` regex extended from `^(S|T|R|I|D|E|AG|LLM|AGP)-\d+$` to `^(S|T|R|I|D|E|AG|LLM|AGP|OI)-\d+$`. Cross-references ADR-020/021/022/023/026/027/028/029 (Feature 201)
- `ADR-031-misinformation-agent.md` - **Accepted (2026-04-24)**: records 9 numbered decisions for the F-2 `misinformation` threat agent — **second net-new threat detection agent under the ADR-030 lineage** (closing OWASP LLM09:2025 Misinformation on the Coverage Matrix: Planned → Covered) and **second net-new producer of `source_attribution`** after F-1. (D1) adopt new AI-tier agent for LLM09:2025 factual-integrity coverage closure as the 7th AI-tier agent; (D2) **Heuristic A three-way scope inheritance from ADR-030 Decision 1** — `prompt-injection` (input-side attacker injection) / `output-integrity` (downstream-execution-sanitization) / `misinformation` (factual-integrity, human-victim + decision-cascade-victim) partition auditable at vocabulary / CWE mapping / mitigation-taxonomy levels; three-way partition is inheritance from ADR-030, not re-adjudication; (D3) lean-agent shape conformance per ADR-023 detection variant (≤150 lines, single `**MANDATORY**: Read`, zero MAESTRO); (D4) 5-category pattern scope (Ungrounded Factual Emission / Citation Fabrication / Overreliance-Missing-HITL / Retrieval-Grounding Gap / Confidence-Calibration Absence) with Q1 deferrals (Model-Specific Hallucination Patterns, Feedback-Loop Overreliance); (D5) **24-file zero-edit invariant preserved** with grep-auditable enumeration (22 original + F-1's 2) — F-2 adds 1 agent + 1 companion skill as new additions, bringing the invariant to 26 post-merge; (D6) Proposed → Accepted dual-commit with post-merge SHA fill (fifth consecutive ADR under this protocol in the ADR-027/028/029/030/031 series); (D7) post-merge SHA fill recording squash commit; (D8) **schema bump 1.6 → 1.7 as 2nd recorded application of ADR-030 Decision 8 regex-alternation minor-bump rule** — `MI` prefix appended to `id.pattern` regex (`^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI)-\d+$` — 11 alternation values); pure precedent application, no further extension; (D9) **CWE-1039 deliberate exclusion** per architect MEDIUM-3 scope-discipline ruling — model-robustness primitive distinct from factual-content primitive; adversarial-robustness coverage would belong in a future sibling agent with `AR-{N}` prefix. Cross-references ADR-021/023/026/027/028/029/030 (Feature 206)
- `ADR-033-human-trust-exploitation-agent.md` - **Accepted (2026-04-26)**: records 10 numbered decisions for the F-4 net-new `human-trust-exploitation` threat agent — **fourth Tier 1 BLP-01 feature** (after F-1 / Feature 201 / ADR-030, F-2 / Feature 206 / ADR-031, F-3 / Feature 219 / ADR-032) and **third execution of the Heuristic A standalone-branch path** (vs. F-3's enrichment branch); closes OWASP Agentic ASI09:2026 communication axis on the BLP-01 Coverage Matrix (Planned → Covered) while the autonomy axis remains attributed to `agent-autonomy`. (D1) adopt new standalone agent `.claude/agents/tachi/human-trust-exploitation.md` + companion skill `.claude/skills/tachi-human-trust-exploitation/` emitting findings under `TE-{N}` prefix on Process DFD elements with consumer-facing AI emission patterns; (D2) **Heuristic A four-way signal-class boundary + ASI09 sub-scope carve-up** — `human-trust-exploitation` (communication axis: AI-disclosure / authority-attestation / persuasion-tone / persona-boundary / synthetic-relationship safeguards) is vocabulary-disjoint from `output-integrity` (machine-victim sanitization, LLM05), `misinformation` (factual-integrity, LLM09), and `agent-autonomy` (autonomy-axis: excessive-autonomy / missing-HITL / goal-drift / cascading-failures); ASI-09 is a shared framework anchor with `agent-autonomy` retaining autonomy axis (already in `owasp_references`) and `human-trust-exploitation` adding communication axis — **no edit to `agent-autonomy.md` required**, carve-up documented at ADR layer only preserving SC-009 26-file zero-edit invariant; cross-references ADR-030 D2 Outcome B (the binding architectural reservation that **created** F-4's scope); (D3) **standalone-vs-enrichment branch contrast** with F-3 — F-4 vocabulary-disjoint per ADR-032 enrichment-eligibility test; standalone is the only viable Heuristic A path; third execution of standalone branch (after F-1 / F-2); (D4) lean-agent shape conformance per ADR-023 detection variant (≤150 lines, single `**MANDATORY**: Read`, zero MAESTRO classification); (D5) 5-category pattern scope (Missing AI Disclosure / Authority-Attestation Without Verification / Persuasion-Tone Without Safeguards / Persona-Boundary Drift / Synthetic-Relationship Safeguards Absence) with ≥4 indicators + ≥1 worked example + ≥1 anti-indicator per category; (D6) **schema bump 1.7 → 1.8 as 3rd recorded application of ADR-030 Decision 8 regex-alternation minor-bump rule** — `TE` prefix appended to `id.pattern` regex (`^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI|TE)-\d+$` — 12 alternation values); pure precedent application after F-1's 1st (OI) and F-2's 2nd (MI); (D7) `category: agentic` enum reuse (no new category value required since `TE-{N}` ID prefix plus `source_attribution.id: ASI-09` carry sufficient discrimination — three-prefix-family within agentic: `AG` autonomy axis / `AGP` multi-agent topology / `TE` communication axis with disjoint mitigation vocabularies); (D8) **DFD targets: `[Process]` only** per BLOCKING-1 — no External Entity declaration; mirrors F-1 / F-2 single-target precedent; (D9) **Naming Disambiguation** — `human-trust-exploitation` (hyphen-cased agent name / agent-to-human ASI09 scope) vs. existing `agentic_pattern: "trust_exploitation"` schema-enum value (underscore-cased / agent-to-agent multi-agent-topology scope per Feature 142); the two never collide because they live in disjoint namespaces (filesystem path vs. enum value) and address orthogonal threat surfaces; (D10) **26-file zero-edit invariant preserved** with grep-auditable enumeration including the `agent-autonomy.md` NOT-edit per D2 sub-scope carve-up at ADR layer; F-4 = 22 original + F-1's 2 + F-2's 2 + F-3's 0 (enrichment-branch — modifies existing, no new files) + F-4's 2 = 26-file inventory + 2 net-new = 28 files post-merge. Three-prefix-family discipline within agentic (`AG` / `AGP` / `TE`) combined with the LLM three-prefix family from F-2 (`LLM` / `OI` / `MI`) yields a **6-prefix-family AI threat surface** with Heuristic A signal-class boundary preservation across both AI-tier sub-categories. F-4 = third net-new producer of `source_attribution` (after F-1 + F-2; F-3 was first to use the enrichment-branch pattern instead of standalone), proving F-A2 referential-integrity contract against three independent populators. New `examples/consumer-agent-app/` clean-slate baseline (per architect Wave 3 Step 1 Q5 lean decision) — WellnessCompanionChatbot mental-health/wellness companion archetype with all 4 FR-006 emission indicators engaged; emits 5 TE findings (TE-1..TE-5, one per Pattern Category 1-5) on regen; pipeline regen byte-identical (40-page PDF, SHA-256 `7ac0b639...269bce5`) per ADR-021 `SOURCE_DATE_EPOCH=1700000000` invariant. Cross-references ADR-021/023/026/027/028/030/031/032 (Feature 224)
- `ADR-032-asi07-tool-abuse-enrichment.md` - **Accepted (2026-04-26)**: records 7 numbered decisions for the F-3 ASI07 enrichment of the existing `tool-abuse` threat agent — **first execution of the Heuristic A enrichment branch** (vs. the new-agent branch validated by F-1 + F-2) and **third producer flow / first enrichment of an existing populator** of `source_attribution`. Closes OWASP Agentic ASI07:2026 (Insecure Inter-Agent Communication) on the BLP-01 Coverage Matrix: Planned → Covered. (D1) **Heuristic A enrichment vs. new agent** — extend `tool-abuse` rather than spawn `asi07-inter-agent-communication`, signal-class identity rationale (message flow between agent-or-tool endpoints — same as Categories 1-8); inheritance of the rule from ADR-030 Decision 1, applied at the AG tier in **enrichment** mode; (D2) **Additive-only edit discipline per ADR-023 Decision 3** in its purest form — 3 anchor points on `tool-abuse.md` (metadata `owasp_references += [ASI-07]`, `## Purpose` 1-line extension, Detection Workflow Step 5 references += `[ASI-07, AML.T0060, CWE-287, CWE-345]`); 4 additive sections appended to companion `detection-patterns.md` with byte-identity preserved on Categories 1-8 + `## Overview` + `## Targeted DFD Element Types` + `## Trigger Keywords` (SC-006 BLOCKER); (D3) **No-schema-bump asymmetry to ADR-031 Decision 8** — F-3 reuses existing `AG-{N}` prefix; `schemas/finding.yaml` `schema_version` remains `"1.7"`, `id.pattern` regex unchanged; F-3 is the **first BLP-01 detection feature with no schema bump**; (D4) **Zero consumers-list edit** — `tool-abuse` already at `finding-format-shared.md:18` from prior wiring; (D5) **Zero functional orchestrator/dispatch edit** — `tool-abuse` already fully registered across multiple callsites; optional cosmetic Q2 single-token annotation to `dispatch-rules.md` (`tool-abuse (MCP-03)` → `tool-abuse (MCP-03, ASI-07)`) is documentation-only; (D6) **Public-only governance** — `_internal/strategy/BLP-01.md` and SDR-001 commercial framing remain private per SDR-001 Option C governance contract; (D7) **Pattern Category Disambiguation** — explicit non-overlap carve between Category 6 (LLM03 supply-chain — upstream ingestion at registration time) and Category 10 (runtime trust propagation between already-registered MCP servers at invocation time), plus boundary-marking subsection between Categories 1-8 (single-agent tool-abuse) and Categories 9-10 (cross-agent trust propagation). 24-file zero-edit invariant preserved (22 original + F-1's 2 + F-2's 2 = 26 detection files; F-3 edits 2 host files; remaining 24 stay byte-identical). Heuristic A discipline now operationalized across **two branches**: new-agent (F-1 OI + F-2 MI within LLM tier) and enrichment (F-3 within AG tier); F-A2 source_attribution contract validated across **two agent classes** (LLM new + AG enrichment). Cross-references ADR-021/023/027/028/030/031 (Feature 219)
- `ADR-034-llm10-unbounded-consumption-audit.md` - **Accepted (2026-04-27)**: records 9 numbered decisions for the F-5 LLM10:2025 Unbounded Consumption enrichment of the existing `denial-of-service` STRIDE-tier agent AND `model-theft` AI-tier agent — **second execution of the Heuristic A enrichment branch** (after F-3 single-agent scope) and **first at two-agent enrichment scope**. Closes OWASP LLM10:2025 Unbounded Consumption on the BLP-01 Coverage Matrix: Partial → Covered, completing the **OWASP LLM Top 10:2025 framework at 10/10 fully closed** (combined with F-4's ASI09 closure: 20/20 OWASP AI top-10 entries). (D1) **Heuristic A enrichment-vs-new-agent at TWO-AGENT scope** — enrich BOTH `denial-of-service` (with Pattern Categories 12 LLM Inference-Request Flooding + 13 Context-Window Exhaustion Latency-Driven Variant — Q1 SPLIT Vector A) AND `model-theft` (with Pattern Categories 10 Cost Amplification + 11 Denial-of-Wallet — Q1 SPLIT Vector B + broader DoW), signal-class identity rationale at two-agent scope; inheritance of the rule from ADR-030 D1 + ADR-032 (direct precedent at single-agent scope); (D2) **Additive-only edit discipline per ADR-023 Decision 3 across 4 host files** (`denial-of-service.md` + companion `tachi-denial-of-service/references/detection-patterns.md` + `model-theft.md` + companion `tachi-model-theft/references/detection-patterns.md`); (D3) **Canonical 5-row LLM10 sub-pattern → owning-agent mapping table populated COMPLETE** with severity-hint annotation column (audit deliverable per team-lead MEDIUM-1) — covers LLM inference-request flooding, context-window exhaustion (Vector A latency + Vector B cost — Q1 SPLIT cross-agent vector decomposition, **first BLP-01 sub-pattern with cross-agent vector decomposition**), cost amplification, and denial-of-wallet; (D4) **No schema bump** — F-5 reuses existing `D-{N}` and `LLM-{N}` prefixes; `schemas/finding.yaml` `schema_version` remains `"1.8"`, `id.pattern` regex unchanged; F-5 is the **second BLP-01 detection feature with no schema bump** (after F-3) and the **first at two-agent enrichment scope**, deliberate asymmetry with F-1 (1.5→1.6), F-2 (1.6→1.7), F-4 (1.7→1.8) per ADR-031 Decision 8 cross-referenced as the rule F-5 does NOT invoke; (D5) **No consumers-list edit** — `denial-of-service` already at `finding-format-shared.md:12` and `model-theft` already at line 16 from prior wiring; (D6) **No functional orchestrator/dispatch edit** — both hosts already fully registered across multiple callsites; cosmetic Q2 single-token annotation decided NO at architect plan-day decision (DoS dispatch-rules entry remains unchanged); (D7) **Pattern Category Disambiguation across two companion catalogs** — DoS Cat 9 (CWE Top 25 generic resource exhaustion) vs. Cat 12/13 (LLM-tier inference-exhaustion) + model-theft Cat 6 (Unbounded Inference Consumption pre-existing) vs. Cat 10/11 (specific cost-amplification + denial-of-wallet attack vectors below the abstraction level Cat 6 covers); (D8) **No `source_attribution` populator wiring extension** — F-5 cites LLM10 in `references` array only; populator wiring deferred to F-A3 per ADR-028 Decision 6 F-A3 deferral; F-5 is the first BLP-01 detection feature to defer populator wiring (asymmetry with F-1/F-2/F-3/F-4 third-net-new-producer pattern); (D9) **Public-only governance** per SDR-001 Option C governance contract. **Q3 severity-floor 2-condition CRITICAL rule on Cat 11**: HIGH default; CRITICAL only when (a) multi-tenant freemium structurally evident AND (b) BOTH per-tenant token budget AND cost alerting absent. **MITRE ATT&CK T1496 prose-only invariant**: Resource Hijacking cited as prose context on Cat 10/11 (NOT in `references` array — not catalog-resolvable per D6). **22-file zero-edit invariant** preserved (12 other agents + 7 infrastructure consumers + `human-trust-exploitation.md` + `tool-abuse.md` NOT-edit); F-5 = 28 detection-tier files post-F-4 inventory + 4 file edits + 0 net-new = 28 files post-merge. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000` (web-app + microservices + ascii-web-api + mermaid-agentic-app + free-text-microservice + maestro-reference); LLM-serving topology gate (FR-015) properly filters Cat 12/13/10/11 on non-LLM-serving architectures. 4 NEW findings emerged on `examples/agentic-app/` regen — D-10 (Cat 12) + D-11 (Cat 13, Q1 SPLIT Vector A) + LLM-15 (Cat 10) + LLM-16 (Cat 11, HIGH per Q3 default — single-tenant). Cross-references ADR-021/023/027/028/030/031/032/033 (Feature 229)
- `ADR-035-ml-top-10-coverage-bundle.md` - **Accepted (2026-04-28)**: records 10 numbered decisions for the F-6 OWASP ML Top 10:2023 Coverage Bundle enrichment of the existing `tampering` (STRIDE-tier) + `data-poisoning` (AI-tier) + `model-theft` (AI-tier) agents — **third execution of the Heuristic A enrichment branch** (after F-3 single-agent scope and F-5 two-agent scope) and **first at three-agent enrichment scope**. Closes OWASP ML Top 10:2023 framework at 10/10 fully closed via 6 F-6 entry closures (ML01 + ML03 + ML04 + ML06 + ML07 + ML08) + 3 pre-existing closures (ML02 + ML05 + ML10) + F-1's ML09 documentation-only bundling, advancing **OWASP three-framework total to 30/30 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 — OWASP AI security framework family fully closed by tachi). (D-1) **Heuristic A enrichment-vs-new-agent at THREE-AGENT scope** — enrich `tampering` (ML01 Adversarial Input Manipulation), `data-poisoning` (ML07 Transfer Learning + ML08 Model Skewing + ML06 corpus-side two-facet split), and `model-theft` (ML03 + ML04 disjoint-tells + ML06 artifact-side two-facet split); inheritance of the rule from ADR-030 D1 + ADR-032 (single-agent precedent) + ADR-034 (two-agent precedent); (D-2) **Additive-only edit discipline per ADR-023 Decision 3 across 6 host files** (`tampering.md` + companion + `data-poisoning.md` + companion + `model-theft.md` + companion); (D-3) **Canonical 8-row mapping table populated COMPLETE** (7 closure rows + 4 reference rows + severity-hint annotation column); (D-4) **ML06 two-facet cross-agent decomposition** — corpus-side (data-poisoning Cat 10 over Public Dataset Repository / Internal Merchant Transaction History / Feast Feature Store) AND artifact-side (model-theft Cat 14 over MLflow Model Registry / Weight Checkpoint Storage); **second BLP-01 sub-pattern with cross-agent decomposition** after F-5's Q1 SPLIT vector axis, first across distinct host agents at three-agent scope (architectural-surface axis); (D-5) **ML03 vs ML04 disjoint architectural-tells on shared AML.T0024** — both target FraudDetectionML Prediction API and cite AML.T0024 (Exfiltration via ML Inference API), but ML03 → output-minimization mitigations vs ML04 → inference-privacy mitigations; **first BLP-01 feature where two OWASP entries share an ATLAS catalog reference but get independent Pattern Categories** with disjoint mitigation taxonomies; (D-6) **No schema bump** — F-6 reuses existing `T` + `D` + `LLM` prefixes; `schemas/finding.yaml` `schema_version` remains `"1.8"`, `id.pattern` regex unchanged; F-6 is the **third BLP-01 detection feature with no schema bump** (after F-3 single-agent + F-5 two-agent) and **first at three-agent enrichment scope**; (D-7) **No consumers-list edit** — three host agents already in consumers list; (D-8) **No functional orchestrator/dispatch edit** — three hosts already fully registered; (D-9) **Pattern Category Disambiguation across 3 companion catalogs** — tampering Cat 10 vs Cat 1-9 + data-poisoning Cat 8/9/10 vs Cat 1-7 + model-theft Cat 12/13/14 vs Cat 1-11; (D-10) **No `source_attribution` populator wiring extension** — F-A3 deferral; second BLP-01 detection feature to defer populator wiring after F-5. **ATLAS catalog gap propagation 3x F-5 scale**: 3 of 6 cited ATLAS techniques (AML.T0015 + AML.T0019 + AML.T0031) are NOT catalog-resolvable per `mitre-atlas.yaml` and ship as prose-only worked-example narratives (NOT in references arrays); 3 of 6 (AML.T0018 + AML.T0020 + AML.T0024) ship in references arrays. **22-file zero-edit invariant** preserved (10 other agents + 7 infrastructure consumers + 5 non-F-6 detection companions NOT-edited); F-6 = 30 detection-tier files post-F-5 inventory + 6 file edits + 0 net-new + 1 new ADR + 1 new architecture mutation target. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000`; predictive-ML topology gate (FR-016) properly filters ML-tier categories on non-predictive-ML architectures. 9 NEW F-6 findings emerged on new `examples/predictive-ml-app/` baseline (7th example) — T-10 (tampering Cat 10 ML01) + D-8 (Cat 8 ML07) + D-9 (Cat 9 ML08) + D-10/D-11 (Cat 10 ML06 corpus-side) + LLM-1/LLM-2 (model-theft Cat 12/13 ML03/ML04 disjoint-tells) + LLM-3/LLM-4 (Cat 14 ML06 artifact-side); 43 total findings on regen. **Forward-scope marker (line 77)**: F-7 Tier 2 OWASP Mobile Top 10 bundle at planned 5-agent fan-out may invoke the enrichment branch at single-agent or multi-agent scope depending on signal-class analysis at PRD time; F-6 establishes three-agent enrichment as a viable operational pattern beyond F-3 single-agent and F-5 two-agent precedents, with extensibility to ≥4-agent fan-out implied by structural symmetry — **fulfilled at F-7 four-or-five-agent scope per ADR-036 D-6 verbatim citation**. Cross-references ADR-021/023/027/028/030/031/032/034 (Feature 232)
- `ADR-037-web-api-coverage-attestation-and-populator-wiring.md` - **Accepted (2026-05-01)**: records 13 numbered decisions for the F-241 Web/API Coverage Attestation + Populator Wiring [Tier 3] feature — **eleventh and final BLP-01 feature**, **fifth execution of the Heuristic A enrichment branch** (after F-3 single-agent + F-5 two-agent + F-6 three-agent + F-7 four-or-five-agent), and **first at 11-host scope (full F-A3 closure)**. **BLP-01 11-feature initiative CLOSES at F-241 squash-merge.** Closes OWASP Web/API combined Top 10:2021 + API Security 2023 framework at 10/10 fully closed (A05 + A06 + API6 + API8 + API9 + API10 Stream 2 closures), advancing **OWASP five-framework total to 50/50 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 + Mobile 10/10 + Web/API 10/10 — closing OWASP coverage milestone). (D-1) **Single combined ADR-037** rejected M-A split into separate F-8 + F-A3 + record-shape ADRs per Q-PM-1 plan-day RESOLVED — four work streams interlock; (D-2) **11-host F-A3 closure** (Architect HIGH-A 11-host expansion absorbing Team-Lead HIGH-1 — `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` + `denial-of-service` + `tool-abuse` + `data-poisoning` + `model-theft` + `prompt-injection` + `agent-autonomy` all populate `source_attribution`; **F-A3 deferral debt CLOSED** across ADR-032 + ADR-034 D-8 + ADR-035 D-10 + ADR-036 D-10 four-feature deferral lineage); (D-3) **6 Partial → Covered closures across Web/API tier** (A05 + A06 + API6 + API8 + API9 + API10 Stream 2 closures); (D-4) pipeline-generated `9. Coverage Attestation` section emitted on all 8 baselines; (D-5) **ATT&CK Enterprise tactical-grouping OOS strategy** (38 → 701 records — 323 in-scope + 378 OOS; 7 verbatim T040 tactical-grouping rationale strings TA0005/7/8/9/10/11/40 + 2 derived TA0112 + TA0043 curator extensions + 1 per-item T1078.004 OOS override); (D-6) BLP-01 §8 Quality Bar spec-anchored `# citation:` comment-anchored evidence convention (60/60 owasp.yaml records valid post-T070 audit reconciliation); (D-7) **Taxonomy YAML record-shape extension** across 3 frameworks (`owasp.yaml` + `mitre-atlas.yaml` + `mitre-attack.yaml` gain `out_of_scope: bool` + `out_of_scope_rationale: str` defaulted-fields per Architect MEDIUM-A — backward-compat via YAML defaults; **ADR-027 receives forward-pointer addendum** at lines 336-355 cross-linking ADR-037 D-7 per Architect M-1 carry-forward, T060 + sanity-check T083 bidirectional cross-link verified); CWE substitution rule extension — 8-CWE canonical mapping table for catalog-absent CWE-307/204/311/319/326/913/451/732 with 12 distinct substitutions across T053 + T054 + T055 with 0 F-A2 referential-integrity errors; ATLAS expansion 12 → 30 records (all 30 in-scope) at T053; (D-8) **Stream 4 aggregator extension at `scripts/extract-report-data.py:_load_framework_yaml_records()` line 1073** (NOT line 1144 per Architect M-2 carry-forward) + dual-emission `yaml_record_count` raw + `in_scope_yaml_record_count` filtered counters; **stdlib-only module-load invariant preserved** by AST walk in `tests/scripts/test_pyyaml_deferred_import.py` (KB-037 invariant regression-guarded — pyyaml import remains lazy inside `_load_framework_yaml_records` body, NOT at module top-level); (D-9) canonical baseline path consistency `examples/{predictive-ml-app, mobile-banking-app}/sample-report/security-report.pdf.baseline` per Architect L-1 carry-forward; (D-10) **dual-commit governance protocol third execution** (Proposed Wave 5.3 T059 commit `7153e1b` → Accepted Wave 6.1 T064 commit `7ed8f4a` → SHA backfilled Wave 6.3 T068 commit `8432cb5`; one new asymmetry: provisional Accepted-date 2026-05-08 overwritten to actual merge-date 2026-05-01 at T068 — 7-day acceleration); (D-11) **surgical Section 9-only backfill approach** (asymmetric to F-1/F-2/F-4 full-orchestrator regen; reduces wall-clock cost ~60% on 8-baseline scope; M8 dual-host runtime validation FIRST at T055 closes ADR-036 D-4 contract); (D-12) **OWASP-only Tier-2 closure rationale** (ATT&CK / ATLAS / NIST AI RMF / CWE remain at 0.00% cross-framework primary attribution across all 8 baselines — **a CHOICE not oversight**; cross-framework primary attribution deferred to hypothetical F-9+ scope BLP-02 envelope candidates); (D-13) **Zero schema bump at 11-host scope** (fifth BLP-01 detection feature reusing existing 12-prefix `S/T/I/E/R/D/LLM/MI/TE/OI/AG/AGP` finding-ID family; first at 11-host scope; ADR-037 D-7 references the taxonomy YAML record-shape extension as **a separate-axis additive change** at taxonomy-tier NOT finding-tier — schema-bump asymmetry preserved). **5/5-dimension reduction confirmed at 11-host scope** (zero new agent files / zero new skill directories / zero schema bumps / zero consumers-list edits / zero functional orchestrator/dispatch edits) — pattern is now load-bearing-and-saturated, no further fan-out possible within current detection-tier surface (11/11 hosts populate `source_attribution`). **8 baselines validated** (web-app + microservices + ascii-web-api + mermaid-agentic-app + free-text-microservice + maestro-reference + new mobile-banking-app/sample-report + new predictive-ml-app/sample-report); 6/6 prior baselines preserved byte-identity on non-Coverage-Attestation pages under `SOURCE_DATE_EPOCH=1700000000` per ADR-021. 84/84 tasks complete; clean single-PR delivery (PR #242 squash-merged 2026-05-01 at `e8a5370`); release-please PR #243 (`chore(main): release 4.27.0` at commit `a86242d`) fired ~30s post-merge per F-212 + F-7 + F-6 cumulative validation. Cross-references ADR-021/023/027/028/030/031/032/034/035/036 (Feature 241)
- `ADR-036-mobile-top-10-coverage-bundle.md` - **Accepted (2026-04-29)**: records 10 numbered decisions for the F-7 OWASP Mobile Top 10:2024 Coverage Bundle enrichment of the existing `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` STRIDE host agents — **fourth execution of the Heuristic A enrichment branch** (after F-3 single-agent + F-5 two-agent + F-6 three-agent) and **first at four-or-five-agent enrichment scope**. Closes OWASP Mobile Top 10:2024 framework at 10/10 fully closed (M1 + M3 → spoofing; M2 + M4 + M7 → tampering; M5 + M6 + M9 + M10 → info-disclosure; M8 dual-host → privilege-escalation + repudiation), advancing **OWASP four-framework total to 40/40 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 + Mobile 10/10 — second framework family closure after AI-security at F-6). (D-1) **Heuristic A enrichment-vs-new-agent at FOUR-OR-FIVE-AGENT scope** — enrich 5 STRIDE host agents in the dual-host path (`spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation`) rather than authoring a new `mobile-platform` 13th-agent sibling; rejected alternative of new agent because signal-class cleave aligns precisely with existing five STRIDE host-agent boundaries (identity impersonation / data integrity / confidentiality leakage / privilege gain / accountability loss); inheritance of the rule from ADR-030 D1 + ADR-032 + ADR-034 + ADR-035; (D-2) **Additive-only edit discipline per ADR-023 Decision 3 across 10 host files** (5 STRIDE agents + 5 companion `detection-patterns.md`); (D-3) **Canonical 11-row Mobile Top 10 sub-pattern → owning-agent mapping table populated COMPLETE** with severity-hint annotation column (M8 occupies 2 rows for dual-host privilege-gain + accountability-loss carve); (D-4) **M8 dual-host disjoint architectural-tells** — privilege-gain variant (`privilege-escalation` host: debug endpoints + ContentProvider exports + missing app-attestation enabling privilege gain) AND accountability-loss variant (`repudiation` host: missing audit logging + disabled crash reporting + debug log leakage enabling audit-trail loss); same OWASP entry split across two architectural-tell axes; **third BLP-01 sub-pattern with cross-agent decomposition** after F-5's Q1 SPLIT (vector axis) and F-6's ML06 two-facet split (corpus-side vs artifact-side); first at architectural-tell layer rather than entry-decomposition layer; mirrors ADR-035 D-4 ML06 two-facet precedent; (D-5) **M4 cross-agent boundary with F-1 `output-integrity`** — Cat 12 Mobile IPC Input Validation (tampering host) owns mobile-IPC-input-side validation (Intent extras / URL-scheme parameters / deep-link payloads / exported ContentProvider gates / pasteboard injection); F-1's `output-integrity` agent owns LLM-output-side sanitization (LLM-generated content flowing into browser/SQL/shell sinks); disjoint architectural-tells prevent duplicate emission on hybrid LLM+mobile architectures; **second BLP-01 feature where two adjacent signal classes share a CWE family but get independent Pattern Categories** with disjoint architectural surfaces; mirrors ADR-035 D-5 ML03/ML04 disjoint-tells precedent at F-7 cross-axis layer; (D-6) **No schema bump** — F-7 reuses existing `S` + `T` + `I` + `E` + `R` STRIDE prefixes; `schemas/finding.yaml` `schema_version` remains `"1.8"`, `id.pattern` regex unchanged; F-7 is the **fourth BLP-01 detection feature with no schema bump** (after F-3 single-agent + F-5 two-agent + F-6 three-agent) and **first at four-or-five-agent enrichment scope**, deliberate asymmetry with F-1 (1.5→1.6), F-2 (1.6→1.7), F-4 (1.7→1.8) per ADR-031 Decision 8 cross-referenced as the rule F-7 does NOT invoke; **ADR-035 closing forward-scope marker line 77 forecast cited verbatim**; (D-7) **ATT&CK Mobile catalog gap codified per F-A2 referential-integrity contract** — T1474 (Supply Chain Compromise — Mobile, M2) + T1626 (Abuse Elevation Control Mechanism, M8 privilege-gain) + T1398 (Boot or Logon Initialization Scripts, M8 accountability-loss) ALL 3 prose-only (verified 0/0/0 catalog-absent at Wave 1.0 T012); 3-of-3 prose-only worst-case scale extending F-5 1-of-1 + F-6 3-of-6 ATLAS-gap precedent at four-or-five-agent scope; (D-8) **No functional orchestrator/dispatch edit** — all 5 STRIDE host agents already registered across multiple callsites; mobile-specific trigger keywords (Android/iOS-tier vocabulary) live inside the five enriched companion `detection-patterns.md` files; (D-9) **Pattern Category Disambiguation across 5 companion catalogs** — each STRIDE companion gains a Disambiguation subsection delineating new mobile-tier Pattern Categories from pre-existing categories; (D-10) **No `source_attribution` populator wiring extension** — F-A3 deferral; third BLP-01 detection feature to defer populator wiring after F-5 + F-6. **28-file zero-edit invariant** preserved (10 modified — 5 host agents + 5 companion catalogs; 18 NOT-edited — 9 other agents + 7 infrastructure consumers + 2 non-F-7 detection companions); F-7 = 28 detection-tier files post-F-6 inventory + 10 file edits + 0 net-new. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000` (web-app + microservices + ascii-web-api + mermaid-agentic-app + free-text-microservice + maestro-reference); mobile-platform topology gate (FR-016) properly filters mobile-tier categories on non-mobile architectures. 16 NEW F-7 findings emerged on new `examples/mobile-banking-app/` baseline (7th example — WellnessBank Android banking app archetype with 6 mobile-platform topology indicators); 62 total findings on regen. Cross-references ADR-021/023/027/028/030/031/032/034/035 (Feature 237)
- `ADR-040-config-file-parsing-hardening.md` - **Accepted (2026-05-05)**: records the F-256 / F-2 BLP-02 Wave 2 source-pattern hardening — replaces 4 enumerated bash `source`/`eval` config-file load sites with `aod_template_load_kv_file <path> <var_prefix> [<allowed_keys_array_name>] [<key_case>]`, the canonical KV-file parsing primitive in `.aod/scripts/bash/template-config-load.sh`. The library implements the 7-step canonical pipeline: argument validation → file existence + Step 2b explicit NUL pre-check (`LC_ALL=C wc -c` vs `tr -d '\000' | wc -c` size-comparison; `bash $(cat …)` truncates at NUL so the regex-only check is unsound) → single `cat $path` into in-memory buffer (TOCTOU race window collapses to "before cat opens" per H-2) → per-line iteration → mode-dependent strict regex (`^[A-Z_][A-Z_0-9]*=("[^"$\\\`]*"|'[^']*'|[A-Za-z0-9._/:@+=-]*)$` — upper mode; lower-mode shape with `[a-z_]` start; the unquoted class uses `*` zero-or-more per B-1 to accept `KEY=` empty-unquoted from version-file contract; double-quoted alternative explicitly rejects `"`/`$`/`\`/backtick — no command substitution, no parameter expansion, no escape sequences) → optional whitelist enforcement (in-pass + post-pass completeness) → defensive identifier check + `printf -v` caller-scope assignment. Refactored sites: (A) `scripts/init.sh:106` defaults.env load, (B-primary) `template-git.sh:561` version-file read, (B-roundtrip) `template-git.sh:485-515` writer self-test, (C) `template-substitute.sh` four `eval` invocations replaced with bash dynamic-deref `${!var}` + `printf -v`, (D) `template-substitute.sh:162-209` `aod_template_load_personalization_env` body. Bundles two related BLP-02 fixes in the same PR: (1) `template-git.sh` `aod_template_fetch_upstream` `git clone` gains a foreground `&`/`wait`/`kill` watchdog timeout closing TACHI-VULN-851fd6a21ba9 (LOW) — hanging upstream no longer blocks `/aod.update` indefinitely; (2) F-1's `aod_init_read_validated` extended to additionally reject `$`, `\`, backtick at the prompt boundary per B-2 Path R-2 — closes the upstream defense loop so writer-side escape pass at `template-substitute.sh:566-571` could be removed. New canonical contract `contracts/stack-pack-defaults-schema.md` enforces lockstep schema for stack-pack `defaults.env` files (whitelist-enforced; new keys land in lockstep). NFR-004 perf delta accepted-with-rationale at the >50% tier per Q-5 (init.sh runs once per adopter project; primitive trades sub-second perf for adversarial-fixture defense vs. four bash-interpretation surfaces). Bash 3.2.57 compatibility preserved (NFR-001) — no `declare -A`, no `mapfile`/`readarray`, no `&>` redirection, no lowercase parameter expansion. Test infrastructure: pytest matrix on `[macos-latest, ubuntu-latest]` extended with the F-2 cases plus a session-scoped `hanging_upstream` fixture per F-250 ADR-039 reuse pattern. Promoted from Proposed (Wave 3 Stream 3 T042) → Accepted (Wave 6 T054 architect promotion after Day-8 checkpoint per Q-6 dual-commit pattern); closes BLP-02 Wave 2 Feature 256 source-pattern-hardening (PR #257 squash-merged 2026-05-05 at `f959622`). Cross-references ADR-038 (F-1 substitution canon — validation-triplet pattern reused; F-2 reuses the PATTERN not the function), ADR-039 (F-250 test-architecture canon — session-scoped fixture pattern adopted for `hanging_upstream`) (Feature 256)
- `ADR-038-placeholder-substitution-strategy.md` - **Accepted (2026-05-04)**: records 7 numbered decisions for the F-248 substitution-surface hardening — **first BLP-02 Wave 1 feature** and first non-detection-tier ADR after the BLP-01 closure at ADR-037. (D-1) **Substitution mechanism replacement** — `find ... -exec sed -i ... +` swapped for per-file `aod_template_substitute_placeholders <src> <dest>` helper using bash parameter expansion `${content//\{\{KEY\}\}/value}` (literal pattern + literal replacement, no regex interpretation either side); single-branch cross-platform behavior eliminates the `OSTYPE` macOS-vs-Linux split; preserves file mode bits via `_aod_preserve_mode` (BSD `stat -f` / GNU `stat -c`); atomic writes via `<dest>.tmp` + `mv` rename (interrupt-safe). (D-2) **NFR-004 perf threshold accept-with-rationale at +658% delta** (T021 measurement: 50.7s vs T008 baseline 6.7s for `init.sh` substitution loop) — init.sh runs ONCE per adopter project (one-time cost; not recurring); metacharacter corruption defects sed introduces are functional, not cosmetic — values like `AT&T` actively break adopter intent; `/aod.update` re-substitution flow operates on `personalized` category (5 files) and remains within original NFR-004 thresholds. (D-3) **Constitution template install via pre-stripped clean variant** — `init.sh:235-241` `sed -i '/^<!--$/,/^-->$/d; /^## Template Instructions$/,$d'` cleanup of the live `.aod/memory/constitution.md` swapped for `cp .aod/templates/constitution-clean.md .aod/memory/constitution.md` followed by `aod_template_substitute_placeholders` re-substitution; `.aod/templates/constitution-instructional.md` ships full template variant + `.aod/templates/constitution-clean.md` ships post-strip variant; T007 byte-equivalence proven manually. (D-4) **`.aod/personalization.env` gitignored-by-default posture** — multi-tenant safety (a fork of an adopter repo MUST NOT inherit the original adopter's `PROJECT_NAME` etc.); migration command `git rm --cached .aod/personalization.env` for adopters who previously committed the file. (D-5) **Validation triplet pattern reuse contract for F-2 BLP-02 Wave 2** — F-1's `aod_init_read_validated` follows `regex-validate` → `reject-on-mismatch` → `printf -v` discipline; F-2 (`defaults.env` strict-KV parser) reuses the **PATTERN**, NOT the function (interactive `read -p`-only). (D-6) **Residual scan scope: `personalized` category from `.aod/template-manifest.txt` only** (currently 5 files: `.claude/rules/{context-loading,deployment,design-context-loader,design-quality,governance}.md`) — codebase contains ~110 legitimate non-canonical `{{KEY}}` tokens used by parallel templating systems (stack-pack scaffolds, brand templates, devops docs, doc examples) that a whole-tree residual scan would falsely halt on; the helper `aod_template_assert_no_residual` regex `\{\{[A-Z_]+\}\}` is unchanged; only the CALLER scope in `init.sh` is constrained. (D-7) **`.claude/mcp-config.json` removal** via `git rm` — zero consumer references per T005 broader sanity check; the file was unused dead code carrying a `{{PROJECT_PATH}}` placeholder; canonical placeholder set remains locked at 12. **Test infrastructure**: 4 new test files (`tests/scripts/test_init_sh_{substitution,adversarial,constitution,self_delete}.py` — 20 tests including ≥13 adversarial cases per NFR-003 covering metachar literal-preservation Cases 1-8, prompt-boundary input rejection Cases 9-12, file-level byte-identity Case 13, residual scan, constitution byte-equality, self-delete, and fixture-replay) plus `tests/fixtures/init-baseline-tree/` golden tree + `tests/fixtures/regenerate-baseline.sh` regen script. **CI infrastructure**: new `.github/workflows/tachi-pytest.yml` runs the suite on a `[macos-latest, ubuntu-latest]` matrix (macOS bash 3.2.57 strict gate / Ubuntu bash 5.x modern reference) — both legs MUST pass green; path-filtered to fire only on F-248 substitution-surface changes. **Promoted from Proposed (Wave 3 T034) → Accepted at T036 post architect review**; closes BLP-02 Wave 1 Feature 248 substitution-surface-hardening (PR #249 squash-merged 2026-05-04 at `6db9a25`); known follow-up at Issue #250 (Test-1 baseline scope refactor + perf — 30-40min CI runtime needs reduction). Cross-references ADR-037 (BLP-01 final closure) (Feature 248)
- `ADR-NNN-decision-title.md` - Individual ADRs

### 03_patterns/
Reusable design patterns and best practices
- `README.md` - Pattern catalog
- Pattern documentation by category

### 04_deployment_environments/
Environment-specific configurations and documentation
- `README.md` - Environment overview
- `development.md` - Local development setup
- `staging.md` - Staging environment
- `production.md` - Production environment

---

## Key Principles

1. **Document Decisions**: Use ADRs for significant technical choices
2. **Keep Current**: Update docs alongside code changes
3. **Auto-Scaffold**: `01_system_design/README.md` is auto-generated from plan.md during `/aod.project-plan` approval (Feature 089)
4. **Template Variables**: Use `{{TEMPLATE_VARIABLES}}` for project-specific values
5. **Version Control**: All architecture docs in git

---

## Quick Links

- [Tech Stack](00_Tech_Stack/README.md)
- [System Design](01_system_design/README.md)
- [ADRs](02_ADRs/ADR-000-template.md)
- [Patterns](03_patterns/README.md)
- [Environments](04_deployment_environments/README.md)

---

**Maintained By**: Architect

# Technology Stack - tachi

**Last Updated**: 2026-05-04
**Owner**: Architect

---

## Overview

This document defines the technology stack for tachi.

---

## Frontend

**Framework**: {{FRONTEND_FRAMEWORK}}
- Version: {{VERSION}}
- Why: {{RATIONALE}}

**UI Library**: {{UI_LIBRARY}}
- Examples: React, Vue, Svelte, Angular

**Styling**: {{STYLING_APPROACH}}
- Examples: Tailwind CSS, CSS Modules, Styled Components

**State Management**: {{STATE_MANAGEMENT}}
- Examples: Redux, Zustand, Jotai, Context API

**Build Tool**: {{BUILD_TOOL}}
- Examples: Vite, Webpack, Parcel

---

## Backend

**Runtime**: {{BACKEND_RUNTIME}}
- Examples: Node.js, Python, Go, Rust

**Framework**: {{BACKEND_FRAMEWORK}}
- Examples: Fastify, Express, FastAPI, Gin

**Language**: {{BACKEND_LANGUAGE}}
- Version: {{VERSION}}

**API Style**: {{API_STYLE}}
- Examples: REST, GraphQL, gRPC

---

## Database

**Primary Database**: {{DATABASE_TYPE}}
- Examples: PostgreSQL, MySQL, MongoDB

**Version**: {{VERSION}}
**Provider**: {{DATABASE_PROVIDER}}
- Examples: Self-hosted, AWS RDS, Neon, PlanetScale

**ORM/Query Builder**: {{ORM}}
- Examples: Prisma, Drizzle, TypeORM, SQLAlchemy

---

## Infrastructure

**Hosting Platform**: {{HOSTING_PLATFORM}}
- Examples: Vercel, AWS, Google Cloud, Railway

**Container Runtime**: {{CONTAINER_RUNTIME}}
- Examples: Docker, Kubernetes, None (serverless)

**CI/CD**: GitHub Actions
- `release-please` (googleapis/release-please-action@v4): Automated version tagging and CHANGELOG generation on merge to main (Feature 086)
- Configuration: `release-please-config.json` (changelog sections), `.release-please-manifest.json` (current version), `.github/workflows/release-please.yml` (workflow trigger)
- Release type: `simple` (no package manager integration; version tracked in manifest)
- Why: Convention-based release automation from Conventional Commits; eliminates manual `git tag` and CHANGELOG maintenance

---

## Monitoring & Observability

**Logging**: {{LOGGING_SOLUTION}}
**Metrics**: {{METRICS_SOLUTION}}
**Error Tracking**: {{ERROR_TRACKING}}

---

## Development Tools

**Package Manager**: {{PACKAGE_MANAGER}}
**Code Quality**: {{LINTING_TOOLS}}
**Testing**: {{TESTING_FRAMEWORKS}}

---

## AOD Kit Internal Tooling

These are tools used by the AOD Kit itself (not the adopter's application stack).

### Threat Modeling Schemas (Feature 001)

**Directory**: `schemas/` (machine-readable data contracts for threat analysis pipeline)
- Architecture: Hub-and-spoke content model -- `agents/` (hub) produces findings conforming to schemas, `templates/` (format) consumes them, `adapters/` serves dual role: knowledge-system configuration (scoring/context) and platform distribution (Feature 021) translating 15 hub agents into native formats for 5 target platforms
- Interface contract: `docs/INTERFACE-CONTRACT.md` -- single integration reference for input formats, dispatch rules, and output structure

| Schema | Purpose | Key Fields |
|--------|---------|------------|
| `schemas/finding.yaml` | Intermediate Representation (IR) -- data contract between agents and templates; optional `scored_finding` extension for quantitative risk scoring (Feature 035); baseline-aware fields `delta_status` and `baseline_run_id` (Feature 074); MAESTRO layer classification `maestro_layer` (Feature 084); canonical CSA layer names for L5/L6/L7 (Feature 136); agentic pattern classification `agentic_pattern` (Feature 142); optional list-of-RECORD `source_attribution` field citing F-A1 external-framework IDs (Feature 189 F-A2); `OI-{N}` ID prefix for `output-integrity` threat-agent findings (Feature 201 F-1); `MI-{N}` ID prefix for `misinformation` threat-agent findings (Feature 206 F-2); **schema unchanged in Feature 219 F-3** (`tool-abuse` agent enriched for OWASP Agentic ASI07:2026 — reuses existing `AG-{N}` prefix; first BLP-01 detection feature with no schema bump per [ADR-032](../02_ADRs/ADR-032-asi07-tool-abuse-enrichment.md) Decision 3, asymmetry with F-1/F-2 schema bumps); `TE-{N}` ID prefix for `human-trust-exploitation` threat-agent findings (Feature 224 F-4); **schema unchanged again in Feature 229 F-5** (`denial-of-service` + `model-theft` agents enriched for OWASP LLM10:2025 Unbounded Consumption at two-agent scope — reuses existing `D-{N}` and `LLM-{N}` prefixes; second BLP-01 detection feature with no schema bump per [ADR-034](../02_ADRs/ADR-034-llm10-unbounded-consumption-audit.md) Decision 4, asymmetry with F-1/F-2/F-4 schema bumps; first BLP-01 feature at two-agent enrichment scope with zero schema impact); **schema unchanged in Feature 232 F-6** (`tampering` + `data-poisoning` + `model-theft` agents enriched for OWASP ML Top 10:2023 Coverage Bundle at three-agent scope — reuses existing `T-{N}` + `D-{N}` + `LLM-{N}` prefixes; third BLP-01 detection feature with no schema bump per [ADR-035](../02_ADRs/ADR-035-ml-top-10-coverage-bundle.md) D-6); **schema unchanged in Feature 237 F-7** (`spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` STRIDE host agents enriched for OWASP Mobile Top 10:2024 Coverage Bundle at four-or-five-agent scope — reuses existing `S/T/I/E/R-{N}` STRIDE prefixes; fourth BLP-01 detection feature with no schema bump per [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md) D-6); **schema unchanged in Feature 241 F-241** (Web/API Coverage Attestation + Populator Wiring [Tier 3] at 11-host scope — `source_attribution` populated across all 11 detection-tier hosts; reuses existing 12-prefix `S/T/I/E/R/D/LLM/MI/TE/OI/AG/AGP` finding-ID family; **fifth BLP-01 detection feature with no schema bump** per [ADR-037](../02_ADRs/ADR-037-web-api-coverage-attestation-and-populator-wiring.md) D-13; **first at 11-host scope (full F-A3 closure)**; ADR-037 D-7 references the taxonomy YAML record-shape extension as **a separate-axis additive change** at taxonomy-tier NOT finding-tier — schema-bump asymmetry preserved; per-host edit cost stays linear at 11x scope, fulfilling ADR-035 + ADR-036 closing forward-scope-marker forecasts verbatim) | 15 fields: id, category, component, threat, likelihood, impact, risk_level, mitigation, references, dfd_element_type, maestro_layer (L1-L7 canonical CSA names or Unclassified, default Unclassified — L5 Evaluation and Observability, L6 Security and Compliance, L7 Agent Ecosystem per Feature 136), delta_status (NEW/UNCHANGED/UPDATED/RESOLVED), baseline_run_id (nullable), **agentic_pattern** (8-value enum per Feature 142: 6 canonical CSA MAESTRO patterns `agent_collusion`, `emergent_behavior`, `temporal_attack`, `trust_exploitation`, `communication_vulnerability`, `resource_competition` + `none` + `multiple`, default `none`), **source_attribution** (optional list-of-RECORD per Feature 189; each record `{taxonomy, id, relationship}` with closed 5-value `taxonomy` enum — `owasp` / `mitre-attack` / `mitre-atlas` / `nist-ai-rmf` / `cwe` external frameworks only — and closed 3-value `relationship` enum — `primary` / `related` / `derived` with default `primary`; field is absent when a finding cites no framework items; F-A2 ships the contract only, populator wiring deferred to F-A3 under 22-file zero-edit invariant; **first net-new producer is the `output-integrity` agent shipped in Feature 201 F-1** — every OI finding emits `source_attribution` citing OWASP LLM05:2025 primary + ≥1 CWE related; **second net-new producer is the `misinformation` agent shipped in Feature 206 F-2** — every MI finding emits `source_attribution` citing OWASP LLM09:2025 primary + ≥1 CWE related from `{CWE-345, CWE-223}`; **third net-new producer is the `human-trust-exploitation` agent shipped in Feature 224 F-4** — every TE finding emits `source_attribution` citing OWASP ASI09:2026 primary + ≥1 CWE related; F-3's `tool-abuse` enrichment was the first to use the enrichment-branch pattern instead of standalone, so F-A2 referential-integrity contract is now validated against three independent populators); `id.pattern` regex extended in Feature 142 to accept `AGP-\d+` prefix for net-new findings emitted by the Phase 3.6 synthesis engine, extended again in Feature 201 to accept `OI-\d+` prefix for `output-integrity` agent findings, extended a third time in Feature 206 to accept `MI-\d+` prefix for `misinformation` agent findings, and extended a fourth time in Feature 224 to accept `TE-\d+` prefix for `human-trust-exploitation` agent findings (regex `^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI|TE)-\d+$` — **12 alternation values**); schema_version 1.8 (bumped 1.7 to 1.8 in Feature 224 for additive regex-alternation prefix extension per ADR-030 Decision 8 — **3rd recorded application** of the regex-alternation minor-bump rule after F-1's 1st application and F-2's 2nd application; preceded by 1.6 to 1.7 bump in Feature 206; preceded by 1.5 to 1.6 bump in Feature 201; preceded by 1.4 to 1.5 bump in Feature 189; preceded by 1.3 to 1.4 bump in Feature 142; preceded by 1.2 to 1.3 bump in Feature 136); see `schemas/risk-scoring.yaml` for extension fields, [ADR-028](../02_ADRs/ADR-028-source-attribution-schema-extension.md) for the F-A2 contract, [ADR-030](../02_ADRs/ADR-030-output-integrity-agent.md) for the F-1 OI prefix extension (1st regex-alternation application), [ADR-031](../02_ADRs/ADR-031-misinformation-agent.md) for the F-2 MI prefix extension (2nd regex-alternation application, pure precedent application with no further rule extension), and [ADR-033](../02_ADRs/ADR-033-human-trust-exploitation-agent.md) for the F-4 TE prefix extension (3rd regex-alternation application, pure precedent application yielding the **6-prefix-family AI threat surface** = LLM family `LLM` / `OI` / `MI` + agentic family `AG` / `AGP` / `TE` with disjoint mitigation vocabularies across both AI-tier sub-categories) |
| `schemas/input.yaml` | Input validation -- accepted architecture description formats | 5 formats: ASCII, free-text, Mermaid, PlantUML, C4; includes recognition patterns and `format: auto` heuristic detection |
| `schemas/output.yaml` | Output structure -- sections required in generated threat model; includes SARIF severity mapping (Feature 012) | 8 sections + Section 4a/4b: System Overview, Trust Boundaries, STRIDE Tables, AI Threat Tables, **Correlated Findings (4a)**, **Resolved Findings (4b)** (Feature 074), Coverage Matrix, Risk Summary, Recommended Actions, **Delta Summary (8)** (Feature 104); SARIF severity mapping comment block with CVSS alignment (Feature 012) |
| `schemas/report.yaml` | Report output structure -- sections required in generated threat report (Feature 015, extended Feature 104) | 8 sections: Executive Summary, Architecture Overview, Threat Analysis, Cross-Cutting Themes, Attack Trees, Remediation Roadmap, Appendix: Finding Reference, **Delta Summary (8)** (Feature 104); schema_version 1.1; attack tree file naming convention `{finding-id}-attack-tree.md`; finding reference completeness rules; baseline frontmatter fields (baseline_source, baseline_date, delta_counts) |
| `schemas/infographic.yaml` | Infographic output structure -- sections required in generated threat infographic specification (Feature 018) | 6 sections: Metadata, Risk Distribution, Coverage Heat Map, Top Critical Findings, Architecture Threat Overlay, Visual Design Directives; CVSS color palette (#DC2626/#F97316/#EAB308/#4169E1/#6B7280); 16:9 landscape layout with three-zone structure |
| `schemas/risk-scoring.yaml` | Scored finding schema extending `finding.yaml` with quantitative risk scoring (Feature 035); baseline-aware fields `score_source` and `score_bounds` (Feature 074) | 4 scoring dimensions (cvss_base, exploitability, scalability, reachability), composite weights (0.35/0.30/0.15/0.20), 8 category CVSS defaults, 4 severity bands with SLA/disposition mapping, governance fields (risk_owner, remediation_sla, risk_disposition, review_date), score_source (inherited/fresh), score_bounds (min/max per category for NEW findings) |
| `schemas/compensating-controls.yaml` | Control finding IR extending `risk-scoring.yaml` with compensating control fields (Feature 036); baseline-aware fields `control_carry_forward` and `rescan_scope` (Feature 074) | 10 control categories (8 STRIDE-mapped + 2 AI-specific), effectiveness ratings (Strong/Moderate/Weak/None), residual risk calculation fields, control evidence and gap tracking, coverage matrix dimensions, control_carry_forward (boolean), rescan_scope (full/incremental) |
| `schemas/coverage-checklists.yaml` | Coverage gate configuration -- required threat categories per DFD element type and AI subtype (Feature 074) | 6 component types (external_entity, process, data_store, data_flow, llm_process, mcp_server), per-type required STRIDE/AI categories, AI subtype keyword detection (LLM/MCP), category-to-agent mapping table, validation rules (precedence, minimum requirements, targeted re-analysis) |
| `schemas/attack-chain.yaml` | Cross-layer attack chain schema -- cross-finding aggregates linking findings across MAESTRO layers (Feature 141) | Chain fields: chain_id, title, ordered layer progression, member findings with roles, causal narrative, chain-breaking controls, surfaced flag; schema_version 1.0; separate from finding.yaml (chains are aggregates, not individual findings); see [ADR-020](../02_ADRs/ADR-020-maestro-layer-classification.md) Phase 2 section |

**Taxonomy catalog + crosswalk** (Feature 180 F-A1, delivered 2026-04-17 via PR #181, squash commit `8b7c7bf`): [`schemas/taxonomy/`](../../../schemas/taxonomy/README.md) is a machine-readable directory of **9 files** — 7 catalog YAMLs (`owasp.yaml`, `mitre-attack.yaml`, `mitre-atlas.yaml`, `nist-ai-rmf.yaml`, `cwe.yaml`, `tachi-control-category.yaml`, `tachi-stride-ai-category.yaml`) + `crosswalk.yaml` (526 primary edges) + `README.md` (curation methodology, per-framework provenance, confidence calibration rubric with anti-drift rule, per-framework update procedures). **Record shape**: 5-field `{id, full_id, name, url, cwe_refs}` per FR-003 (`cwe_refs` omitted on `cwe.yaml` per the unidirectional OWASP→CWE rule). **Edge shape**: `{source: {taxonomy, id}, target: {taxonomy, id}, edge_type, confidence, citation}`. **Closed enums**: 7-value `taxonomy` (matches filename stems), 3-value `edge_type` (`primary` / `related` / `superseded` — only `primary` populated in F-A1), 3-value `confidence` (`high` / `medium` / `low`). **First pipeline consumer (Feature 194 F-B, delivered 2026-04-18)**: the 5 external-framework catalog YAMLs (`owasp.yaml`, `mitre-attack.yaml`, `mitre-atlas.yaml`, `nist-ai-rmf.yaml`, `cwe.yaml`) are loaded by `scripts/extract-report-data.py` at report-extraction time via `_load_framework_yaml_records()` (lazy `import yaml` inside the helper to preserve stdlib-only module-load invariant) to compute Covered / Partial / Gap classifications against per-finding `source_attribution` arrays; `crosswalk.yaml` remains unused at the pipeline tier (F-C follow-on feature will consume it). **Integrity enforcement**: `tests/schemas/test_taxonomy_integrity.py` enforces FR-027 through FR-032 (record shape, enum closure, referential integrity, URL/path citation, SC-013 parse perf). **Record-shape extension (Feature 241 F-241 BLP-01 Tier 3 closure, delivered 2026-05-01 via PR #242, squash commit `e8a5370`)**: 3 of the 5 external-framework catalog YAMLs (`owasp.yaml` + `mitre-atlas.yaml` + `mitre-attack.yaml`) gain two new defaulted-fields per ADR-037 D-7 — `out_of_scope: bool` (default `false`) + `out_of_scope_rationale: str` (default `""`) — extending the F-A1 5-field per-item shape to a backward-compatible 7-field shape. Pre-F-241 records that omit both fields parse identically under YAML default behavior (treated as `out_of_scope: false`). Catalog growth: ATT&CK Enterprise expansion 38 → 701 records (323 in-scope + 378 OOS) at T053 with 7 verbatim T040 tactical-grouping rationale strings (TA0005/7/8/9/10/11/40 — defensive-evasion / discovery / lateral-movement / collection / exfiltration / command-and-control / impact tactics span runtime/red-team scope, NOT design-time threat-modeling scope) + 2 derived TA0112 + TA0043 curator extensions for post-spec STIX 2.1 update + 1 per-item T1078.004 OOS override; ATLAS expansion 12 → 30 records (all 30 in-scope) at T053. **CWE substitution rule extension (ADR-037 D-7 inline)**: 8-CWE canonical mapping table for catalog-absent CWE-307 / CWE-204 / CWE-311 / CWE-319 / CWE-326 / CWE-913 / CWE-451 / CWE-732 with 12 distinct substitutions across T053 + T054 + T055 with 0 F-A2 referential-integrity errors — does NOT modify `cwe.yaml`; catalog growth beyond 53 records is reserved for follow-on features in the BLP-02 envelope. **Stream 4 aggregator filter consumer**: `scripts/extract-report-data.py:_load_framework_yaml_records()` line 1073 reads the new `out_of_scope` field at YAML load level (`in_scope_only: bool = False` parameter per Architect M-2 carry-forward) so that the Coverage Attestation page coverage-percentage formula `(covered_count / in_scope_record_count) * 100` excludes runtime-only / IR-only / design-time-irrelevant records. **F-A3 populator-wiring closure**: F-241 closes the four-feature F-A3 deferral lineage (ADR-032 + ADR-034 D-8 + ADR-035 D-10 + ADR-036 D-10) by populating `source_attribution` across all 11 detection-tier hosts (`spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` + `denial-of-service` + `tool-abuse` + `data-poisoning` + `model-theft` + `prompt-injection` + `agent-autonomy`) per Architect HIGH-A 11-host expansion. **ADR-027 receives forward-pointer addendum** at lines 336-355 cross-linking ADR-037 D-7 per Architect M-1 carry-forward — bidirectional cross-link verified at T060 + sanity-check T083. See [ADR-027](../02_ADRs/ADR-027-taxonomy-crosswalk-schema.md) for schema rationale, cadence-exception rationale (single-PRD aggregation of 9 files bounded to foundation-data features), and the Proposed→Accepted dual-commit governance protocol; see [ADR-037](../02_ADRs/ADR-037-web-api-coverage-attestation-and-populator-wiring.md) D-7 + D-8 for the F-241 record-shape extension and Stream 4 aggregator filter.

**Threat agent prompts**: `agents/` (14 agent prompt files + orchestrator + report agent + infographic agent + risk-scorer agent + control-analyzer agent); distributed to 5 target platforms via `adapters/` hub-and-spoke pattern (Feature 021). Right-sized in Feature 029: orchestrator (2,085->1,273 lines, -39%), report (801->472 lines, -41%), infographic (592->414 lines, -30%) via reference-extraction pattern -- consultation-only content moved to co-located reference files loaded on-demand via Read tool. Further domain knowledge extraction in Feature 075: orchestrator (1,273->769 lines), risk-scorer and control-analyzer reduced to under 1,000 lines each via skill extraction -- domain reference tables moved to standalone `.claude/skills/tachi-*` skill files with tiered loading (SKILL.md + references/). Baseline-aware pipeline extensions in Feature 074: orchestrator, risk-scorer, and control-analyzer agents enhanced with delta-aware logic (carry-forward, isolated discovery, merge+dedup, coverage gate); new baseline-correlation reference added to `tachi-orchestration` skill. Agent context optimization in Feature 078: all 6 methodology and report agents restructured from monolithic prompts to lean definitions with on-demand skill references, 4 new skill directories created (tachi-orchestration extended, tachi-report-assembly, tachi-threat-reporting, tachi-infographics), shared definitions extracted to `tachi-shared` skill (severity bands, STRIDE+AI categories, finding format), explicit `model: sonnet` field added to all 17 agent YAML frontmatter for intentional model-to-task matching, 40-60% prompt size reduction across all restructured agents. Detection tier lean refactor in Feature 082: all 11 threat agents (6 STRIDE + 5 AI) migrated to the lean + skill references pattern, aligning with the infrastructure tier established in Features 029/075/078. Each threat agent file drops from 113-201 lines to 50-114 lines (all STRIDE ≤120, all AI ≤150, hard cap FR-10 ≤180) with a single `**MANDATORY**: Read` directive at detection start that loads its companion `.claude/skills/tachi-<name>/references/detection-patterns.md` file. 11 new companion skill directories host the externalized pattern catalogs. Catalog enrichment: +30 new pattern categories added across the 11 agents during the refactor (22-category aggregate floor cleared with +8 margin), drawing from OWASP Top 10 2021, OWASP LLM Top 10 2025, OWASP AI Exchange, MITRE ATT&CK v15+, MITRE ATLAS v5.1+ (including the October 2025 agent techniques AML.T0058-T0062), CWE Top 25 2024, and NIST AI 600-1. Shared reference additive consolidation in `finding-format-shared.md` adds a "For Threat Agents" producer section. Feature 082 introduces ADR-023 recording the **sibling detection variant** of the lean pattern — tachi now documents two lean-agent shapes: the **methodology variant** (phase-gated `**MANDATORY**: Read` directives at each pipeline phase, used by the 6 infrastructure agents) and the **detection variant** (single-point load at detection start, used by the 11 threat agents). All 17 agents are now on the lean + skill references pattern. **Agent roster extended in Feature 201 F-1**: `output-integrity` added as 12th threat agent (6 STRIDE + 6 AI) — **first net-new threat detection agent authored under the ADR-023 lean pattern** (vs. the 11 agents retrofitted in Feature 082). Companion skill directory `.claude/skills/tachi-output-integrity/` brings the per-agent detection-patterns catalog count to 12. Zero edits to the 11 pre-existing detection agents or their companion `detection-patterns.md` files — additive-only under ADR-023 lineage. See [ADR-030](../02_ADRs/ADR-030-output-integrity-agent.md). **Agent roster extended again in Feature 206 F-2**: `misinformation` added as 13th threat agent (6 STRIDE + 7 AI) — **second net-new threat detection agent authored under the ADR-023 lean pattern** (two-execution-deep validation of the pattern after F-1). Companion skill directory `.claude/skills/tachi-misinformation/` brings the per-agent detection-patterns catalog count to 13. Zero edits to the 24 frozen files (22 original per ADR-023 + F-1's `output-integrity.md` agent + F-1's `tachi-output-integrity/` companion skill) — additive-only under ADR-023/ADR-030 lineage; 24-file zero-edit invariant grows to 26 post-F-2-merge (24 + F-2's 2 new files). F-2 operationalizes the Heuristic A three-way signal-class discipline established by ADR-030 Decision 1: `prompt-injection` / `data-poisoning` / `model-theft` / `agent-autonomy` / `tool-abuse` (input-side) + `output-integrity` (downstream-execution-sanitization — LLM05:2025) + `misinformation` (factual-integrity — LLM09:2025); an architecture exercising all three signal classes on the same LLM Process correctly produces three adjacent findings (`LLM-{N}`, `OI-{N}`, `MI-{N}`) with distinct `source_attribution` primaries — disjoint coverage, not duplication. See [ADR-031](../02_ADRs/ADR-031-misinformation-agent.md). **Existing `tool-abuse` agent enriched in Feature 219 F-3** for OWASP Agentic ASI07:2026 (Insecure Inter-Agent Communication) — **first execution of the Heuristic A enrichment branch** (vs. the new-agent branch validated by F-1 + F-2). Three additive edits to `tool-abuse.md` only (metadata `owasp_references += [ASI-07]` → `[ASI-02, ASI-04, MCP-03, MCP-05, LLM06:2025, ASI-07]`; `## Purpose` 1-line A2A/MCP-to-MCP surface naming; Detection Workflow Step 5 references += `[ASI-07, AML.T0060, CWE-287, CWE-345]`; line count 98 → 100, well under 150 cap). Two new Pattern Categories appended to companion `detection-patterns.md` (Category 9 Insecure Inter-Agent Communication / A2A; Category 10 MCP-to-MCP Trust Propagation) plus Pattern Category Disambiguation subsection per ADR-032 Decision 7 (boundary marker between Categories 1-8 single-agent tool-abuse and Categories 9-10 cross-agent trust propagation; explicit non-overlap carve between Category 6 LLM03 supply-chain and Category 10 runtime trust propagation); Categories 1-8 + Overview + Targeted DFD Element Types + Trigger Keywords held byte-identical to baseline (SC-006 BLOCKER). F-3 ships zero new agent files, zero new skill directories, zero schema edits, zero functional dispatch edits, zero consumers-list edits, zero new runtime dependencies — structurally the smallest BLP-01 detection delivery to date (5/5 dimensions smaller than F-2). 24-file zero-edit invariant grows to 26 post-F-2 (22 original + F-1's 2 + F-2's 2); F-3 edits 2 host files; remaining 24 stay byte-identical per FR-015 / SC-013 BLOCKER. Heuristic A discipline now operationalized across **two branches**: new-agent (F-1 OI + F-2 MI within LLM tier) and enrichment (F-3 within AG tier). AG-8 [NEW] Insecure Inter-Agent Communication finding emerges on the regenerated `agentic-app` example, citing OWASP ASI07:2026 + CWE-287 + MITRE ATLAS AML.T0060 — renders cohesively in `threat-report.md` §3.7 "Agentic Threats (AG-1 through AG-8)" as a single Agentic-category section (no fragmentation). See [ADR-032](../02_ADRs/ADR-032-asi07-tool-abuse-enrichment.md). **Agent roster extended again in Feature 224 F-4**: `human-trust-exploitation` added as 14th threat agent (6 STRIDE + 8 AI) — **third net-new threat detection agent authored under the ADR-023 lean pattern** (third execution of the standalone-branch path after F-1 / F-2; vs. F-3's enrichment-branch path). Companion skill directory `.claude/skills/tachi-human-trust-exploitation/` brings the per-agent detection-patterns catalog count to 14. Closes OWASP Agentic ASI09:2026 communication axis on the BLP-01 Coverage Matrix (Planned → Covered) while the autonomy axis remains attributed to `agent-autonomy`. **26-file zero-edit invariant preserved INCLUDING the `agent-autonomy.md` NOT-edit** despite the ASI09 sub-scope carve-up — the carve-up is documented at the ADR-033 D2 layer only; `agent-autonomy.md`'s `owasp_references` already lists ASI-09 (verified at T004) so no metadata edit was needed. F-4 = 22 original + F-1's 2 + F-2's 2 + F-3's 0 (enrichment-branch — modifies existing, no new files) + F-4's 2 = 26-file inventory + 2 net-new = 28 files post-merge. F-4 operationalizes a four-way Heuristic A signal-class boundary at the AI tier: `output-integrity` (machine-victim sanitization, LLM05) / `misinformation` (factual-integrity, LLM09) / `agent-autonomy` (autonomy axis, ASI09 autonomy-half) / `human-trust-exploitation` (communication axis, ASI09 communication-half) — vocabulary-disjoint, CWE-disjoint, mitigation-taxonomy-disjoint per ADR-033 D2 with empty-intersection vocabulary test in `.aod/results/heuristic-a-verification.md` §3.3. **Three-prefix-family discipline within agentic** (`AG` autonomy axis / `AGP` multi-agent topology / `TE` communication axis) combined with the LLM three-prefix family from F-2 (`LLM` input-side / `OI` output-sanitization / `MI` factual-integrity) yields a **6-prefix-family AI threat surface** with Heuristic A signal-class boundary preservation across both AI-tier sub-categories. New `examples/consumer-agent-app/` clean-slate baseline added per architect Wave 3 Step 1 Q5 lean decision (NOT agentic-app extension) — WellnessCompanionChatbot mental-health/wellness companion archetype with all 4 FR-006 emission indicators engaged (outgoing flow to End User External Entity, consumer-facing prose match on `chatbot`/`companion`/`coach`, persistent persona/multi-turn dialogue, wellness coaching authority); 5 TE findings (TE-1..TE-5, one per Pattern Category 1-5) emitted on regen; 19 total findings (1 Critical, 8 High, 7 Medium, 3 Low); pipeline regen byte-identical (40-page PDF, SHA-256 `7ac0b639...269bce5`) per [ADR-021](../02_ADRs/ADR-021-source-date-epoch-for-deterministic-pdf-comparison.md) `SOURCE_DATE_EPOCH=1700000000` invariant; 6/6 infographic JPEGs generated. Zero new runtime dependencies. See [ADR-033](../02_ADRs/ADR-033-human-trust-exploitation-agent.md). **Existing `denial-of-service` (STRIDE-tier) and `model-theft` (AI-tier) agents enriched in Feature 229 F-5** for OWASP LLM10:2025 Unbounded Consumption — **second execution of the Heuristic A enrichment branch** (after F-3 single-agent scope) and **first at two-agent enrichment scope**. Closes OWASP LLM10:2025 on the BLP-01 Coverage Matrix (Partial → Covered), completing the **OWASP LLM Top 10:2025 framework at 10/10 fully closed** (combined with F-4's ASI09 closure: 20/20 OWASP AI top-10 entries). Four additive file edits: (1) `denial-of-service.md` metadata `owasp_references += [OWASP LLM10:2025]`, `## Purpose` 1-3 line extension naming the LLM-inference-exhaustion surface, Detection Workflow Step 5 references += `OWASP LLM10:2025`; (2) companion `tachi-denial-of-service/references/detection-patterns.md` appends Pattern Category 12 (LLM Inference-Request Flooding and Token Exhaustion) + Pattern Category 13 (Context-Window Exhaustion Latency-Driven Variant — Q1 SPLIT Vector A) + Pattern Category Disambiguation subsection (Cat 9 generic vs. Cat 12/13 LLM-tier); (3) `model-theft.md` `owasp_references` audit-confirmed (LLM10 already present — zero net change), `## Purpose` 1-3 line extension naming the cost-amplification + denial-of-wallet surface; (4) companion `tachi-model-theft/references/detection-patterns.md` appends Pattern Category 10 (Cost Amplification via Recursive or Cost-Asymmetric Prompting) + Pattern Category 11 (Denial-of-Wallet via Context-Window Cost Amplification — Q1 SPLIT Vector B + broader DoW) + Pattern Category Disambiguation subsection (Cat 6 pre-existing vs. Cat 10/11 specific attack vectors). **Q1 SPLIT cross-agent vector decomposition** (first BLP-01 sub-pattern with cross-agent vector decomposition): context-window exhaustion decomposes into Vector A (latency-driven availability disruption) → DoS Cat 13 + Vector B (cost-amplification → economic damage) → model-theft Cat 11. **Q3 severity-floor 2-condition CRITICAL rule on Cat 11**: HIGH default; CRITICAL only when (a) multi-tenant freemium structurally evident AND (b) BOTH per-tenant token budget AND cost alerting absent. F-5 ships **zero new agent files**, **zero new skill directories**, **zero schema bumps**, **zero consumers-list edits**, **zero functional orchestrator/dispatch edits**, **zero `source_attribution` populator wiring extensions** (F-A3 deferral per ADR-028 Decision 6) — F-5 is structurally the second-smallest BLP-01 detection delivery (after F-3 single-agent scope) and the **first at two-agent enrichment scope**. **MITRE ATT&CK T1496 prose-only invariant** preserved (Resource Hijacking cited as prose context on Cat 10/11, NOT in `references` array — not catalog-resolvable). 22-file zero-edit invariant preserved (12 other agents + 7 infrastructure consumers + `human-trust-exploitation.md` + `tool-abuse.md` NOT-edit); F-5 = 28 detection-tier files post-F-4 inventory + 4 file edits + 0 net-new = 28 files post-merge. Heuristic A discipline now operationalized across **two enrichment scope tiers**: single-agent (F-3 within AG tier) and two-agent (F-5 across DoS STRIDE-tier + model-theft AI-tier). 4 NEW findings emerged on `examples/agentic-app/` regen — D-10 (Cat 12, Critical) + D-11 (Cat 13, Critical, Q1 SPLIT Vector A) + LLM-15 (Cat 10, Critical) + LLM-16 (Cat 11, High per Q3 default — single-tenant); cohesive category rendering in single Section 3.5 DoS + single Section 3.8 LLM (no fragmentation); correlation group CG-8 binds all 4 in Theme 5 of regenerated `threat-report.md`. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000` (web-app + microservices + ascii-web-api + mermaid-agentic-app + free-text-microservice + maestro-reference); LLM-serving topology gate (FR-015) properly filters Cat 12/13/10/11 on non-LLM-serving architectures. Test infrastructure update at `tests/scripts/test_backward_compatibility.py`: removed F-5 hosts from `DETECTION_AGENT_PATHS` (12 → 10) and added to `DETECTION_PATTERN_REF_ENRICHMENT_HOSTS` frozenset (extending F-3's single-host pattern to multi-host enrichment branch). See [ADR-034](../02_ADRs/ADR-034-llm10-unbounded-consumption-audit.md). **Existing `tampering` (STRIDE-tier) + `data-poisoning` (AI-tier) + `model-theft` (AI-tier) agents enriched in Feature 232 F-6** for OWASP ML Top 10:2023 Coverage Bundle — **third execution of the Heuristic A enrichment branch** (after F-3 single-agent and F-5 two-agent scopes) and **first at three-agent enrichment scope**. Closes OWASP ML Top 10:2023 framework at 10/10 fully closed (combined with pre-existing ML02/ML05/ML10 + F-1's ML09 documentation-only bundling), advancing the **OWASP three-framework total to 30/30 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 — OWASP AI security framework family fully closed by tachi). Six additive file edits across 6 host files (3 agents + 3 companion `detection-patterns.md` files): `tampering.md` + companion appends Cat 10 (Adversarial Input Manipulation — ML01); `data-poisoning.md` + companion appends Cat 8 (Transfer Learning — ML07) + Cat 9 (Model Skewing — ML08) + Cat 10 (corpus-side ML06 two-facet split — Predictive-ML Supply Chain Completeness over Public Dataset Repository / Internal Merchant Transaction History / Feast Feature Store); `model-theft.md` + companion appends Cat 12 (Model Inversion / Output Reconstruction — ML03) + Cat 13 (Membership Inference — ML04) + Cat 14 (artifact-side ML06 two-facet split — Predictive-ML Artifact Supply Chain over MLflow Model Registry / Weight Checkpoint Storage). **ML06 two-facet cross-agent decomposition** (ADR-035 D-4): AI Supply Chain entry decomposes into corpus-side (data-poisoning Cat 10) AND artifact-side (model-theft Cat 14); **second BLP-01 sub-pattern with cross-agent decomposition** after F-5's Q1 SPLIT (vector axis), first across two distinct host agents at three-agent scope (architectural-surface axis). **ML03 vs ML04 ATLAS-shared-but-mitigation-disjoint** (ADR-035 D-5): both target FraudDetectionML Prediction API and cite AML.T0024 (Exfiltration via ML Inference API), but ML03 → output-minimization mitigations vs ML04 → inference-privacy mitigations; **first BLP-01 feature where two OWASP entries share an ATLAS catalog reference but get independent Pattern Categories** with disjoint mitigation taxonomies. **ATLAS catalog gap propagation 3x F-5 scale**: 3 of 6 cited ATLAS techniques (AML.T0015 + AML.T0019 + AML.T0031) are NOT catalog-resolvable per `mitre-atlas.yaml` and ship as prose-only worked-example narratives (NOT in references arrays); 3 of 6 (AML.T0018 + AML.T0020 + AML.T0024) ship in references arrays. F-6 ships **zero new agent files**, **zero new skill directories**, **zero schema bumps** (third BLP-01 detection feature reusing existing `T` + `D` + `LLM` prefixes; first at three-agent scope), **zero consumers-list edits**, **zero functional orchestrator/dispatch edits**, **zero `source_attribution` populator wiring extensions** (F-A3 deferral per ADR-028 Decision 6 — second BLP-01 detection feature to defer populator wiring after F-5). **22-file zero-edit invariant** preserved (10 other agents + 7 infrastructure consumers + 5 non-F-6 detection companions NOT-edited); F-6 = 30 detection-tier files post-F-5 inventory + 6 file edits + 0 net-new. 9 NEW F-6 findings emerged on new `examples/predictive-ml-app/` baseline (7th example) — T-10 (tampering Cat 10 ML01) + D-8/D-9 (data-poisoning Cat 8/9 ML07/ML08) + D-10/D-11 (data-poisoning Cat 10 ML06 corpus-side) + LLM-1/LLM-2 (model-theft Cat 12/13 ML03/ML04 disjoint-tells) + LLM-3/LLM-4 (model-theft Cat 14 ML06 artifact-side); 43 total findings on regen. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000`; predictive-ML topology gate (FR-016) properly filters ML-tier categories on non-predictive-ML architectures. See [ADR-035](../02_ADRs/ADR-035-ml-top-10-coverage-bundle.md). **Existing `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` STRIDE host agents enriched in Feature 237 F-7** for OWASP Mobile Top 10:2024 Coverage Bundle — **fourth execution of the Heuristic A enrichment branch** (after F-3 single-agent + F-5 two-agent + F-6 three-agent) and **first at four-or-five-agent enrichment scope**. Closes OWASP Mobile Top 10:2024 framework at 10/10 fully closed (M1 + M3 → spoofing; M2 + M4 + M7 → tampering; M5 + M6 + M9 + M10 → info-disclosure; M8 dual-host → privilege-escalation + repudiation), advancing **OWASP four-framework total to 40/40 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 + Mobile 10/10 — second framework family closure after AI-security at F-6). Ten additive file edits across 10 host files (5 STRIDE agents + 5 companion `detection-patterns.md` files): `spoofing.md` + companion appends mobile-credential-usage and mobile-authn/authz Pattern Categories (M1 + M3); `tampering.md` + companion appends mobile-supply-chain-integrity (Cat 11 — M2) + mobile-IPC-input-validation (Cat 12 — M4) + insufficient-mobile-binary-protections (Cat 13 — M7); `info-disclosure.md` + companion appends insecure-mobile-communication + inadequate-mobile-privacy + insecure-mobile-data-storage + insufficient-mobile-cryptography Pattern Categories (M5 + M6 + M9 + M10); `privilege-escalation.md` + companion appends mobile-security-misconfiguration-privilege-gain Pattern Category (M8 privilege-gain variant); `repudiation.md` + companion appends mobile-security-misconfiguration-accountability-loss Pattern Category (M8 accountability-loss variant). **M8 dual-host disjoint architectural-tells** (ADR-036 D-4): M8 Security Misconfiguration decomposes across two owning STRIDE categories — privilege-gain variant (`privilege-escalation` host: debug endpoints + ContentProvider exports + missing app-attestation) AND accountability-loss variant (`repudiation` host: missing audit logging + disabled crash reporting + debug log leakage); **third BLP-01 sub-pattern with cross-agent decomposition** after F-5's Q1 SPLIT (vector axis) + F-6's ML06 two-facet split (corpus-side vs artifact-side); first at architectural-tell layer rather than entry-decomposition layer; mirrors ADR-035 D-4 precedent. **M4 cross-agent boundary with F-1 `output-integrity`** (ADR-036 D-5): Cat 12 Mobile IPC Input Validation (tampering host) owns mobile-IPC-input-side validation (Intent extras / URL-scheme parameters / deep-link payloads / exported ContentProvider gates / pasteboard injection); F-1's `output-integrity` agent owns LLM-output-side sanitization; disjoint architectural-tells prevent duplicate emission on hybrid LLM+mobile architectures; mirrors ADR-035 D-5 precedent at F-7 cross-axis layer. **ATT&CK Mobile catalog gap propagation 3-of-3 prose-only (worst-case scale)**: All 3 cited ATT&CK Mobile techniques (T1474 + T1626 + T1398) are NOT catalog-resolvable per `mitre-attack.yaml` (verified 0/0/0 at Wave 1.0 T012) and ship as prose-only worked-example narratives (NOT in references arrays); 0 of 3 catalog-resolvable — F-7 establishes worst-case scale extending F-5 1-of-1 + F-6 3-of-6 ATLAS-gap precedent. F-7 ships **zero new agent files**, **zero new skill directories**, **zero schema bumps** (fourth BLP-01 detection feature reusing existing `S` + `T` + `I` + `E` + `R` STRIDE prefixes; first at four-or-five-agent scope; **ADR-035 closing forward-scope marker line 77 forecast cited verbatim** in ADR-036 D-6), **zero consumers-list edits**, **zero functional orchestrator/dispatch edits**, **zero `source_attribution` populator wiring extensions** (F-A3 deferral per ADR-028 Decision 6 — third BLP-01 detection feature to defer populator wiring after F-5 + F-6). **28-file zero-edit invariant** preserved (10 modified — 5 host agents + 5 companion catalogs; 18 NOT-edited — 9 other agents + 7 infrastructure consumers + 2 non-F-7 detection companions); F-7 = 28 detection-tier files post-F-6 inventory + 10 file edits + 0 net-new. 16 NEW F-7 findings emerged on new `examples/mobile-banking-app/` baseline (7th example — WellnessBank Android banking app archetype with 6 mobile-platform topology indicators: exposed deep-link Intent extras + hybrid WebView + biometric step-up + secure-storage paths + cleartext HTTP residual + binary-protection gaps); 62 total findings on regen. **6/6 byte-identical baselines** under `SOURCE_DATE_EPOCH=1700000000`; mobile-platform topology gate (FR-016) properly filters mobile-tier categories on non-mobile architectures. See [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md). **F-A3 populator-wiring closure across all 11 detection-tier hosts in Feature 241 F-241** for Web/API Coverage Attestation [Tier 3] — **fifth execution of the Heuristic A enrichment branch** (after F-3 single-agent + F-5 two-agent + F-6 three-agent + F-7 four-or-five-agent) and **first at 11-host scope (full F-A3 closure)**; **eleventh and final BLP-01 feature**, **BLP-01 11-feature initiative CLOSES at F-241 squash-merge**. Closes OWASP Web/API combined Top 10:2021 + API Security 2023 framework at 10/10 fully closed (A05 + A06 + API6 + API8 + API9 + API10 Stream 2 closures), advancing **OWASP five-framework total to 50/50 Covered** (LLM 10/10 + Agentic 10/10 + ML 10/10 + Mobile 10/10 + Web/API 10/10 — third framework-family closure after AI-security at F-6 + Mobile at F-7; closing OWASP coverage milestone). Eleven host agents now populate `source_attribution`: `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` + `denial-of-service` + `tool-abuse` + `data-poisoning` + `model-theft` + `prompt-injection` + `agent-autonomy` per Architect HIGH-A 11-host expansion absorbing Team-Lead HIGH-1 — the aggregator reads `source_attribution[].id` directly with no implicit prefix-attribution path so prompt-injection and agent-autonomy must populate `source_attribution` despite their AI-tier provenance. **F-A3 populator-wiring debt CLOSED** across the four-feature deferral lineage (ADR-032 + ADR-034 D-8 + ADR-035 D-10 + ADR-036 D-10) at F-241. **22-edit grep checklist** (11 agent files + 11 companion catalogs implicit) is the structural discipline that scales the F-7 five-host pattern to 11-host scope without losing the additive-only edit invariant — **per-host edit cost stays linear** (~1 file edit per host) at 11x scope, fulfilling ADR-035 + ADR-036 closing forward-scope-marker forecasts verbatim. F-241 ships **zero new agent files**, **zero new skill directories**, **zero schema bumps** (fifth BLP-01 detection feature reusing existing 12-prefix `S/T/I/E/R/D/LLM/MI/TE/OI/AG/AGP` finding-ID family; first at 11-host scope), **zero consumers-list edits**, **zero functional orchestrator/dispatch edits** (5/5-dimension reduction confirmed at 11-host scope — pattern is now load-bearing-and-saturated, no further fan-out possible within the current detection-tier surface). Stream 4 aggregator extension at `scripts/extract-report-data.py:_load_framework_yaml_records()` line 1073 (NOT line 1144 per Architect M-2 carry-forward) introduces structural code work asymmetric to F-1..F-7 pattern-catalog-only edits; stdlib-only module-load invariant preserved by AST walk in `tests/scripts/test_pyyaml_deferred_import.py` (KB-037 invariant regression-guarded). Taxonomy YAML record-shape extension at ADR-037 D-7 — `owasp.yaml` + `mitre-atlas.yaml` + `mitre-attack.yaml` records gain `out_of_scope: bool` + `out_of_scope_rationale: str` fields with backward-compat defaults; ATT&CK Enterprise expansion 38 → 701 records (323 in-scope + 378 OOS); ATLAS expansion 12 → 30 records (all 30 in-scope); ADR-027 receives forward-pointer addendum at lines 336-355 cross-linking ADR-037 D-7 per Architect M-1 carry-forward — bidirectional cross-link verified at T060 + sanity-check T083. CWE substitution rule extension — 8-CWE canonical mapping table for catalog-absent CWE-307/204/311/319/326/913/451/732 with 12 distinct substitutions across T053 + T054 + T055 with 0 F-A2 referential-integrity errors. Surgical Section 9-only backfill approach (asymmetric to F-1/F-2/F-4 full-orchestrator regen pattern) reduces wall-clock cost vs full regen by ~60% on 8-baseline scope. **8 baselines validated** (web-app + microservices + ascii-web-api + mermaid-agentic-app + free-text-microservice + maestro-reference + new mobile-banking-app/sample-report + new predictive-ml-app/sample-report) — all render the new pipeline-generated `9. Coverage Attestation` page with non-empty per-finding `source_attribution` rows + non-zero OWASP coverage percentages; 6/6 prior baselines preserved byte-identity on non-Coverage-Attestation pages under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — **zero baseline drift on non-CA pages across the 11-host enrichment surface**. See [ADR-037](../02_ADRs/ADR-037-web-api-coverage-attestation-and-populator-wiring.md).
| Subdirectory | Count | Scope | Status |
|-------------|-------|-------|--------|
| `agents/stride/` | 6 agents | STRIDE categories: **Spoofing** (enriched in Feature 237 F-7 for OWASP Mobile Top 10:2024 M1 Improper Mobile Credential Usage + M3 Insecure Mobile Authentication/Authorization via 2 new Pattern Categories; reuses existing `S-{N}` finding ID prefix — no schema bump per ADR-036 Decision 6; mobile-platform topology gate enforces zero new mobile-Cat emissions on non-mobile architectures — see [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md)), **Tampering** (enriched in Feature 232 F-6 for OWASP ML01:2023 Adversarial Input Manipulation via 1 new Pattern Category — Cat 10 Adversarial Input Manipulation; reuses existing `T-{N}` finding ID prefix — no schema bump per ADR-035 Decision 6; first STRIDE-tier agent enriched at three-agent scope — see [ADR-035](../02_ADRs/ADR-035-ml-top-10-coverage-bundle.md); ALSO enriched in Feature 237 F-7 for OWASP Mobile Top 10:2024 M2 Inadequate Supply Chain Security + M4 Insufficient Input/Output Validation + M7 Insufficient Binary Protections via 3 new Pattern Categories — Cat 11 Mobile Supply Chain Integrity / Cat 12 Mobile IPC Input Validation / Cat 13 Insufficient Mobile Binary Protections; **M4 cross-agent boundary with F-1 `output-integrity`** per ADR-036 D-5 — mobile-IPC-input-side validation vs LLM-output-side sanitization on disjoint architectural-tells; reuses existing `T-{N}` finding ID prefix — no schema bump per ADR-036 Decision 6 — see [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md)), **Repudiation** (enriched in Feature 237 F-7 for OWASP Mobile Top 10:2024 M8 Security Misconfiguration accountability-loss variant via 1 new Pattern Category — Mobile Security Misconfiguration / Accountability-Loss Variant covering missing audit logging + disabled crash reporting + debug log leakage; **M8 dual-host disjoint architectural-tells** per ADR-036 D-4 — accountability-loss variant on `repudiation` host vs privilege-gain variant on `privilege-escalation` host; reuses existing `R-{N}` finding ID prefix — no schema bump per ADR-036 Decision 6 — see [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md)), **Info Disclosure** (enriched in Feature 237 F-7 for OWASP Mobile Top 10:2024 M5 Insecure Communication + M6 Inadequate Privacy Controls + M9 Insecure Data Storage + M10 Insufficient Cryptography via 4 new Pattern Categories — Insecure Mobile Communication / Inadequate Mobile Privacy Controls / Insecure Mobile Data Storage / Insufficient Mobile Cryptography; reuses existing `I-{N}` finding ID prefix — no schema bump per ADR-036 Decision 6 — see [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md)), **Denial of Service** (enriched in Feature 229 F-5 for OWASP LLM10:2025 Unbounded Consumption availability-axis surface via 2 new Pattern Categories — 12 LLM Inference-Request Flooding and Token Exhaustion and 13 Context-Window Exhaustion Latency-Driven Variant / Q1 SPLIT Vector A; `owasp_references` extended with `OWASP LLM10:2025`; reuses existing `D-{N}` finding ID prefix — no schema bump per ADR-034 Decision 4; emits D-{N} findings on LLM-serving topologies with inference-endpoint exhaustion / context-window-latency gaps; LLM-serving topology gate enforces zero Cat-12/13 emissions on non-LLM-serving architectures — preserves byte-identity on 6 non-LLM-serving baselines per ADR-021; first STRIDE-tier agent enriched under the Heuristic A enrichment branch — see [ADR-034](../02_ADRs/ADR-034-llm10-unbounded-consumption-audit.md)), **Privilege Escalation** (enriched in Feature 237 F-7 for OWASP Mobile Top 10:2024 M8 Security Misconfiguration privilege-gain variant via 1 new Pattern Category — Mobile Security Misconfiguration / Privilege-Gain Variant covering debug endpoints + ContentProvider exports + missing app-attestation; **M8 dual-host disjoint architectural-tells** per ADR-036 D-4 — privilege-gain variant on `privilege-escalation` host vs accountability-loss variant on `repudiation` host; reuses existing `E-{N}` finding ID prefix — no schema bump per ADR-036 Decision 6 — see [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md)) | Validated end-to-end (Feature 005; Denial of Service LLM10 enrichment added Feature 229; Tampering ML01 enrichment added Feature 232; Spoofing M1+M3 / Tampering M2+M4+M7 / Info Disclosure M5+M6+M9+M10 / Privilege Escalation M8 privilege-gain / Repudiation M8 accountability-loss enrichments added Feature 237) |
| `agents/ai/` | 8 agents | AI-specific threats: Prompt Injection, **Tool Abuse** (enriched in Feature 219 F-3 for OWASP Agentic ASI07:2026 Insecure Inter-Agent Communication via 2 new Pattern Categories — 9 Insecure Inter-Agent Communication / A2A and 10 MCP-to-MCP Trust Propagation; `owasp_references` extended to `[ASI-02, ASI-04, MCP-03, MCP-05, LLM06:2025, ASI-07]`; reuses existing `AG-{N}` finding ID prefix — no schema bump per ADR-032 Decision 3; emits AG-{N} findings citing `{owasp:ASI07,primary} + {cwe:CWE-287,related} + (optional) {atlas:AML.T0060,related}` for Cat 9 and `{owasp:ASI07,primary} + {cwe:CWE-345,related} + (optional) {owasp:LLM03,related}` for Cat 10; multi-agent / multi-MCP topology gate enforces zero Category-9/10 emissions on architectures lacking ≥2 agent Process components connected by a channel or a multi-hop MCP trust chain — single-agent topology preserves byte-identity on 5 non-multi-agent baselines per ADR-021), Data Poisoning, **Model Theft** (enriched in Feature 229 F-5 for OWASP LLM10:2025 Unbounded Consumption cost-amplification + denial-of-wallet surface via 2 new Pattern Categories — 10 Cost Amplification via Recursive or Cost-Asymmetric Prompting and 11 Denial-of-Wallet via Context-Window Cost Amplification with Q3 severity-floor 2-condition CRITICAL rule; reuses existing `LLM-{N}` finding ID prefix — no schema bump per ADR-034 Decision 4; emits LLM-{N} findings on LLM-serving topologies with cost-attribution / per-tenant-budget / cost-alerting gaps; LLM-serving topology gate enforces zero Cat-10/11 emissions on non-LLM-serving architectures — preserves byte-identity on 6 non-LLM-serving baselines per ADR-021), Agent Autonomy, **Output Integrity** (Feature 201 F-1 — OWASP LLM05:2025 Improper Output Handling; `OI-{N}` finding ID prefix; every finding emits `source_attribution` citing LLM05 primary + ≥1 CWE related from `{CWE-22, CWE-78, CWE-79, CWE-89, CWE-94, CWE-918}`), **Misinformation** (Feature 206 F-2 — OWASP LLM09:2025 Misinformation; `MI-{N}` finding ID prefix; every finding emits `source_attribution` citing LLM09 primary + ≥1 CWE related from `{CWE-345, CWE-223}`; 5 factual-integrity pattern categories — Ungrounded Factual Emission, Citation Fabrication, Overreliance/Missing HITL, Retrieval-Grounding Gaps, Confidence-Calibration Absence), **Human Trust Exploitation** (Feature 224 F-4 — OWASP Agentic ASI09:2026 communication axis closure; `TE-{N}` finding ID prefix; schema bump 1.7 → 1.8 as 3rd application of ADR-030 D8 regex-alternation rule; every TE finding emits `source_attribution` citing OWASP ASI09:2026 primary + ≥1 CWE related; 5 communication-axis pattern categories — Missing AI Disclosure, Authority-Attestation Without Verification, Persuasion-Tone Without Safeguards, Persona-Boundary Drift, Synthetic-Relationship Safeguards Absence; ASI09 sub-scope carve-up with `agent-autonomy` per ADR-033 D2 — autonomy axis attributed to `agent-autonomy` / communication axis attributed to `human-trust-exploitation`; **no edit to `agent-autonomy.md` required** preserving 26-file zero-edit invariant; vocabulary-disjoint per ADR-033 D2 four-way Heuristic A signal-class boundary {output-integrity, misinformation, agent-autonomy autonomy-axis, human-trust-exploitation communication-axis}; emits TE-{N} findings on Process DFD elements with consumer-facing AI emission patterns per ADR-033 D8 `dfd_targets: [Process]` only); two-layer keyword dispatch (AG-prefixed agentic, LLM-prefixed LLM categories; LLM **quintet** dispatch rule in `dispatch-rules.md` per [ADR-031](../02_ADRs/ADR-031-misinformation-agent.md) extending the LLM quartet established by [ADR-030](../02_ADRs/ADR-030-output-integrity-agent.md) — `prompt-injection, data-poisoning, model-theft, output-integrity, misinformation`; 5-callsite quintet reconciliation architect-owned per F-2 MEDIUM-4); **6-prefix-family AI threat surface** post-F-4 = LLM family `LLM` / `OI` / `MI` (input-side / output-sanitization / factual-integrity) + agentic family `AG` / `AGP` / `TE` (autonomy / multi-agent topology / communication) | Validated end-to-end (Feature 007; Output Integrity added Feature 201; Misinformation added Feature 206; Tool Abuse ASI07 enrichment added Feature 219; Human Trust Exploitation added Feature 224; Model Theft LLM10 enrichment added Feature 229) |
| `agents/orchestrator.md` | 1 agent | Central orchestrator implementing OWASP 4-phase workflow (Scope, Determine Threats, Determine Countermeasures, Assess) with Phase 5 (Report) integration, STRIDE-per-Element dispatch, AI keyword dispatch (Feature 003), cross-agent correlation detection with deduplicated coverage matrix and risk summary (Feature 010), SARIF 2.1.0 output generation for GitHub Code Scanning integration (Feature 012), and narrative report dispatch with Mermaid attack trees (Feature 015). Infographic generation extracted to standalone `/tachi.infographic` command (Feature 039). Baseline-aware pipeline (Feature 074): Phase 0 baseline detection and finding registry extraction, carry-forward with delta classification (NEW/UNCHANGED/UPDATED/RESOLVED), isolated discovery with coverage-only context, merge+dedup with fingerprint matching, and coverage gate using `schemas/coverage-checklists.yaml`. MAESTRO layer classification (Feature 084): Phase 1 keyword-based component classification using CSA seven-layer taxonomy, MAESTRO Layer column in Component Inventory and Dispatch Table, finding inheritance in Phase 3, "Risk by MAESTRO Layer" subsection in Risk Summary, MAESTRO tags in SARIF output. Cross-layer attack chain correlation (Feature 141): Phase 3.5 rule-based pattern matching identifies attack chains spanning multiple MAESTRO layers using a deterministic transition lookup table (`attack-chain-patterns-shared.md`), produces conditional `attack-chains.md` artifact, caps surfaced chains at top 5 by severity; see [ADR-020](../02_ADRs/ADR-020-maestro-layer-classification.md) Phase 2 section. Agentic pattern synthesis (Feature 142): Phase 3.6 runs after Phase 3.5 and before Phase 4 assessment — evaluates the multi-agent gate predicate against the architecture and, if true, evaluates a deterministic classification rule table (`maestro-agentic-patterns-shared.md` Sections 3-4) against each deduplicated finding, writing `agentic_pattern` field back to the finding IR (Hybrid Post-Hoc Synthesis / Option C per ADR-026); MAY also emit net-new findings with `AGP-` id prefix for the three previously-uncovered patterns (Agent Collusion, Emergent Behavior, Temporal Attack) when the architectural context matches AND no existing finding carries the pattern; zero-edit invariant preserved on the 11 detection agents (per ADR-026 Decision 1 / ADR-023 governance); see [ADR-020](../02_ADRs/ADR-020-maestro-layer-classification.md) Revision History 2026-04-16 and [ADR-026](../02_ADRs/ADR-026-pattern-classification-mechanism.md). Right-sized to 1,273 lines with 3 reference documents (Feature 029); domain knowledge extracted to `tachi-orchestration` skill (769 lines, Feature 075; extended with baseline-correlation reference, Feature 074) | Complete |
| `agents/threat-report.md` | 1 agent | Report generation agent transforming `threats.md` into narrative threat report with executive summary, Mermaid attack trees for Critical/High findings, prioritized remediation roadmap with effort estimates, and complete finding traceability (Feature 015). Delta-aware narrative generation (Feature 104): adapts executive summary and threat analysis to highlight baseline changes, generates Section 8 Delta Summary with lifecycle counts and remediation proof, populates baseline frontmatter fields. Cross-layer attack chain narrative (Feature 141): new Section 6 (Cross-Layer Attack Chains) with 150-300 word narratives per surfaced chain, conditional on `has-attack-chains` boolean from orchestrator Phase 3.5. **Parent-leaf decomposition (Feature 129)**: Section 5 attack tree generation and delta reconciliation extracted into the `tachi-attack-tree-delta` leaf sub-agent; the parent now dispatches to the sub-agent with four atomic inputs (Critical/High findings, delta_counts, baseline directory, output directory), reads the returned `.manifest.json`, and assembles inline Section 5 narrative using the new Section 5 Delta Annotations templates — parent prompt shrinks by -56 lines net while `threat-report.md` output remains byte-compatible for identical inputs. Right-sized to 472 lines with 1 reference document (Feature 029) | Validated end-to-end (Feature 015) |
| `.claude/agents/tachi/attack-tree-delta.md` | 1 sub-agent (leaf) | Attack tree generation and delta reconciliation sub-agent introduced by Feature 129 — first parent-leaf decomposition in the tachi pipeline. Invoked only by `threat-report` (parent) with four atomic inputs; does NOT produce inline Section 5 content. Writes standalone `attack-trees/{finding-id}-attack-tree.md` files plus a structured `attack-trees/.manifest.json` consumed by the parent. Deterministic rule dispatch on `delta_counts`: Rule 1 (all UNCHANGED → verbatim carry-forward), Rule 2 (any delta → fresh-all + Rule 3 reconcile UNCHANGED), no-baseline fallback (fresh-all). Rule 3 structural similarity algorithm uses named constants (`TOKEN_OVERLAP_THRESHOLD` ≥80% Jaccard on leaf labels, `GATE_TYPE_MATCH_REQUIRED`, `NODE_COUNT_DELTA_THRESHOLD` ≤20%) with the authoritative algorithm in the Baseline Reconciliation section of `attack-tree-construction.md`. Tools scope: Read, Write, Glob, Grep — writes confined to `attack-trees/` (cannot modify `threats.md` or `threat-report.md`). Loads `attack-tree-construction.md` at workflow start; loads `attack-tree-examples.md` before first fresh generation (Rule 1 skips this load). No new ADR required — agent decomposition within the existing Phase 5 pipeline shape | Complete |
| `agents/threat-infographic.md` | 1 agent | Infographic agent transforming threat model output into visual infographic specifications and images via Gemini API. Data extraction delegated to deterministic Python script (`scripts/extract-infographic-data.py`, Feature 071) replacing LLM-based parsing. Supports three-tier data source input: `compensating-controls.md` (residual risk), `risk-scores.md` (inherent risk), or `threats.md` (qualitative severity). Supports multiple templates: Baseball Card (risk summary dashboard), System Architecture (annotated architecture diagram with attack surface badges), Risk Funnel (4-tier progressive risk reduction), MAESTRO Stack (layered stack diagram), and MAESTRO Heatmap (layer x severity heat map). MAESTRO templates gated by `has-maestro-data` for backward compatibility; `maestro` shorthand dispatches both MAESTRO templates (Feature 091). Delta-aware extraction (Feature 104): extraction script reads baseline metadata and delta lifecycle fields from source artifacts, enabling infographic templates to display baseline comparison data. Risk labels adapt to data source type ("Residual Risk" / "Inherent Risk" / "Severity"). Right-sized to 414 lines with 2 reference documents (Feature 029, updated Feature 048, Feature 071) | Complete |
| `agents/tachi/risk-scorer.md` | 1 agent | Risk scoring agent applying four-dimensional quantitative assessment (CVSS 3.1 base, exploitability, scalability, reachability) to threat findings with weighted composite calculation, severity band mapping, and governance field assignment. Delta-aware scoring (Feature 074): inherits all scores for UNCHANGED findings (zero drift), re-scores UPDATED findings fresh, bounds NEW finding CVSS base scores within +/-1.0 of category defaults, and carries forward governance fields for persisting findings. MAESTRO layer propagation (Feature 084): passively reads and includes `maestro_layer` field in scored output. Produces dual output: `risk-scores.md` (human-readable) and `risk-scores.sarif` (machine-readable). Invoked via `/tachi.risk-score` command (Feature 035). Domain knowledge extracted to `tachi-risk-scoring` skill (994 lines, Feature 075; extended with baseline scoring rules, Feature 074) | Complete |
| `agents/tachi/control-analyzer.md` | 1 agent | Compensating controls analysis agent implementing 6-phase pipeline (parse, discover, detect, classify, recommend, output) to scan target codebases against scored threats. Detects existing security controls across 10 categories (8 STRIDE-mapped + 2 AI-specific), classifies effectiveness, recommends missing controls, calculates residual risk, and produces dual output: `compensating-controls.md` (human-readable) and `compensating-controls.sarif` (machine-readable). Delta-aware control analysis (Feature 074): carries forward control status, evidence, and residual risk for UNCHANGED findings; incremental re-scan scopes to NEW and UPDATED findings only. MAESTRO layer propagation (Feature 084): passively reads and includes `maestro_layer` field in control output. Invoked via `/tachi.compensating-controls` command (Feature 036). Domain knowledge extracted to `tachi-control-analysis` skill (935 lines, Feature 075; extended with carry-forward rules, Feature 074) | Complete |

**Agent reference documents** (Feature 029): Consultation-only content extracted from agent prompts into co-located reference files. Agents load references on-demand via Read tool at specific pipeline phases, keeping core prompts focused on always-needed execution logic. Applies the [On-Demand Reference File Segmentation](../03_patterns/README.md#pattern-on-demand-reference-file-segmentation) pattern at the agent level.

| Reference | Agent | Path | Loaded When |
|-----------|-------|------|-------------|
| SARIF Generation | orchestrator, risk-scorer | `adapters/claude-code/agents/references/sarif-generation.md` | Phase 4 completion; risk scoring SARIF generation (Feature 035 addendum: scored SARIF diff specification) |
| Validation Checklist | orchestrator | `adapters/claude-code/agents/references/validation-checklist.md` | Pipeline end |
| Error Templates | orchestrator | `adapters/claude-code/agents/references/error-templates.md` | Error condition |
| Report Templates | threat-report | `adapters/claude-code/agents/references/report-templates.md` | Attack tree generation |
| Gemini API Integration | threat-infographic | `adapters/claude-code/agents/references/infographic-gemini-api.md` | Image generation phase |
| Error Handling | threat-infographic | `adapters/claude-code/agents/references/infographic-error-handling.md` | Error condition |

**Tachi domain knowledge skills** (Feature 075, extended Feature 078): Domain reference tables and specifications extracted from methodology and report agents into standalone skill files using Read-tool loading per [ADR-002](../02_ADRs/ADR-002-prompt-segmentation.md). Each skill uses a tiered loading structure: SKILL.md (Level 2 metadata and loading table) plus references/ directory (Level 3 domain data loaded on-demand at specific pipeline phases). This extends the [On-Demand Reference File Segmentation](../03_patterns/README.md#pattern-on-demand-reference-file-segmentation) pattern to a third application level: agent-to-skill extraction. Feature 078 expanded scope from 3 methodology-agent skills to 7 skills covering all pipeline agents, plus a shared definitions skill consumed by multiple agents.

| Skill | Agent | References | Content |
|-------|-------|------------|---------|
| `.claude/skills/tachi-orchestration/` | orchestrator | `dispatch-rules.md`, `output-schemas.md`, `sarif-specification.md`, `baseline-correlation.md` (Feature 074), `format-detection.md`, `dfd-classification.md`, `trust-boundaries.md`, `coverage-requirements.md`, `coverage-matrix-model.md` (Feature 078) | STRIDE-per-Element dispatch tables, AI keyword mappings, output format specs, SARIF generation templates, validation checklists, error handling, baseline file detection, finding registry extraction, fingerprint correlation algorithm, delta classification rules, input format detection heuristics, DFD element classification rules, trust boundary notation, coverage requirements per component type, coverage matrix model |
| `.claude/skills/tachi-risk-scoring/` | risk-scorer | `cvss-vectors.md`, `scoring-dimensions.md`, `severity-bands.md`, `output-formatting.md`, `reachability-analysis.md`, `trust-zones.md` (Feature 078) | CVSS 3.1 base vector mappings, exploitability/scalability/reachability criteria, composite formulas, severity band thresholds, governance field derivation, scored output formatting rules, reachability analysis methodology, trust zone classification |
| `.claude/skills/tachi-control-analysis/` | control-analyzer | `control-categories.md`, `evidence-criteria.md`, `residual-risk.md` | 10 control category definitions with detection patterns, effectiveness classification criteria, residual risk calculation formulas, recommendation templates |
| `.claude/skills/tachi-report-assembly/` | report-assembler | `brand-asset-guidelines.md`, `typst-artifacts.md`, `typst-template-contract.md` (Feature 078) | Brand asset location and fallback rules, Typst artifact detection patterns with tier selection, Typst data variable contract with type specifications and image path resolution |
| `.claude/skills/tachi-threat-reporting/` | threat-report, attack-tree-delta (Feature 129) | `narrative-templates.md`, `attack-tree-construction.md`, `attack-tree-examples.md` (Feature 078) | Executive summary structure, per-category narrative templates, attack tree construction rules with Mermaid syntax, reference attack tree examples. Feature 129 additions: `attack-tree-construction.md` gains a **Baseline Reconciliation** section documenting the Rule 3 structural similarity algorithm (token-overlap ≥80%, gate-type match, node-count delta ≤20%) with named thresholds and decision table — single source of truth for the sub-agent's reconciliation logic. `narrative-templates.md` gains a **Section 5 Delta Annotations** subsection with narrative templates for how the threat-report parent describes `carried_forward`, `generated_fresh`, and `regenerated` trees inline from the sub-agent manifest. Both additions are byte-additive (no pre-existing content removed). |
| `.claude/skills/tachi-infographics/` | threat-infographic | `infographic-specifications.md`, `template-specific-formats.md`, `gemini-prompt-construction.md`, `visual-design-system.md` (Feature 078) | Infographic specification formats, template-specific section layouts (Baseball Card, System Architecture, Risk Funnel, MAESTRO Stack, MAESTRO Heatmap -- Feature 091), Gemini API prompt construction rules, visual design system tokens |
| `.claude/skills/tachi-shared/` | orchestrator, risk-scorer, control-analyzer, threat-report, all threat agents | `severity-bands-shared.md`, `stride-categories-shared.md`, `finding-format-shared.md` (Feature 078, producer section added Feature 082), `maestro-layers-shared.md` (Feature 084, corrected Feature 136), `attack-chain-patterns-shared.md` (Feature 141), `maestro-agentic-patterns-shared.md` (Feature 142), `nist-ai-rmf-mapping.md` (Feature 144) | Canonical severity band definitions (thresholds, SLA, disposition), STRIDE+AI category definitions (8 categories with DFD element applicability), finding IR format specification with both consumer (risk-scorer, control-analyzer, threat-report) and producer (11 threat agents) audience sections (producer section added Feature 082 as additive-only extension), CSA MAESTRO seven-layer taxonomy with keyword-to-layer mappings for component classification -- single-source-of-truth consumed by multiple agents to prevent cross-agent drift. Feature 136 corrected L5/L6/L7 layer names to canonical CSA names (L5 Evaluation and Observability, L6 Security and Compliance, L7 Agent Ecosystem), corrected the MAESTRO acronym expansion, reassigned keyword tables accordingly, and established the enum-value-only minor-bump rule for schema versioning (see ADR-020 Revision History). Cross-layer attack chain transition lookup table (Feature 141): deterministic (STRIDE category, MAESTRO layer) to valid successor pair mappings with causal vocabulary, consumed by orchestrator Phase 3.5 for chain assembly. Agentic pattern classification catalog (Feature 142): six canonical CSA MAESTRO patterns (Agent Collusion, Emergent Behavior, Temporal Attack, Trust Exploitation, Communication Vulnerability, Resource Competition) with CSA citations, coverage mapping to existing 11 detection agents (Section 2), deterministic 6-rule classification table R-01 through R-06 (Section 3), and multi-agent gate predicate spec with canonical `component_type` and `topology` indicator token lists (Section 4) consumed by orchestrator Phase 3.6 pattern synthesis and threat-report Agentic Pattern Analysis narrative; see [ADR-026](../02_ADRs/ADR-026-pattern-classification-mechanism.md) |
| `.claude/skills/tachi-<threat-agent>/` (14 directories) | 14 threat agents (6 STRIDE + 8 AI) | `detection-patterns.md` (Feature 082, extended Feature 201, extended Feature 206, extended Feature 219, extended Feature 224, extended Feature 229, extended Feature 232, extended Feature 237) | Per-agent detection pattern catalogs — externalized from the agent files as part of the Feature 082 detection tier lean refactor. Each companion skill directory (`tachi-spoofing`, `tachi-tampering`, `tachi-repudiation`, `tachi-info-disclosure`, `tachi-denial-of-service`, `tachi-privilege-escalation`, `tachi-prompt-injection`, `tachi-data-poisoning`, `tachi-model-theft`, `tachi-tool-abuse`, `tachi-agent-autonomy`) hosts a single `detection-patterns.md` reference file byte-preserved from the pre-refactor agent content plus +30 aggregate new categories sourced from OWASP Top 10 2021, OWASP LLM Top 10 2025, OWASP AI Exchange, MITRE ATT&CK v15+, MITRE ATLAS v5.1+ (incl. October 2025 agent techniques AML.T0058-T0062), CWE Top 25 2024, and NIST AI 600-1. Loaded via single `**MANDATORY**: Read` directive at detection start per the **detection variant** of the lean pattern — no phase-gated loads. **Feature 201 F-1 adds 12th directory `tachi-output-integrity/`** — net-new authored under the ADR-023 lean detection variant (vs. the 11 directories extracted during Feature 082). Pattern catalog has 5 numbered categories scoped to the encoding/sanitization signal class per ADR-030 Decision 2 Heuristic A Outcome B: (1) Client-Side Execution Sinks (XSS/DOM — CWE-79), (2) Server-Side Execution Sinks (SQLi/OS Command/Code — CWE-89/78/94), (3) SSRF from LLM-Synthesized URLs (CWE-918), (4) Template/Expression Injection (CWE-94, substituting for absent CWE-1336), (5) Path Traversal + Unsafe File Writes (CWE-22, substituting for absent CWE-73); primary citation OWASP LLM05:2025 on every pattern. **Feature 206 F-2 adds 13th directory `tachi-misinformation/`** — second net-new authored under the ADR-023 lean detection variant (two-execution-deep validation of the pattern after F-1). Pattern catalog has 5 numbered categories scoped to the factual-integrity signal class per ADR-031 Decision 2 Heuristic A three-way inheritance from ADR-030 Decision 1: (1) Ungrounded Factual Emission (CWE-345), (2) Citation Fabrication (CWE-345), (3) Overreliance / Missing HITL on Decision-Critical Output (CWE-223 + optional CWE-345), (4) Retrieval-Grounding Gaps (CWE-345), (5) Confidence-Calibration Absence (CWE-345); primary citation OWASP LLM09:2025 on every pattern; every pattern carries ≥1 anti-indicator (per architect MEDIUM-5) and ≥1 worked example with clearly-fictional framing per NFR-6 (no real institutional names or clinician/lawyer/advisor identities). MITRE ATLAS `AML.T0042 Verify Attack` and NIST AI 600-1 §2.4 remain **prose-only citations** (CONFIRMED ABSENT from F-A1 catalog YAMLs at PRD time). **Feature 219 F-3 extends pre-existing `tachi-tool-abuse/` directory** with 2 net-new Pattern Categories appended to `detection-patterns.md` (no new directory created — first execution of the Heuristic A enrichment branch via additive edits to an existing skill directory, vs. the new-directory pattern of F-1 + F-2): (9) Insecure Inter-Agent Communication / A2A — primary OWASP ASI07:2026 + related CWE-287 + AML.T0060; ≥4 indicators (target 5: ≥2 agents on a channel, no mutual auth, no message signing, no replay protection, agent-in-the-middle relay without taint propagation); ≥1 worked example (orchestrator → worker-agent over plain HTTP); named mitigations (mTLS, HMAC + nonce, replay-window, taint propagation, mutual JWT/API key fallback); explicit anti-indicator (single-agent topology emits zero Cat-9 findings). (10) MCP-to-MCP Trust Propagation — primary OWASP ASI07:2026 + related CWE-345 + LLM03:2025; ≥4 indicators (target 5: multi-hop MCP trust chain, no per-hop attestation, transitive authority assumption gaps, no trust-chain validator, undeclared cross-MCP supply-chain assumptions); ≥1 worked example (Agent → MCP-A → MCP-B without per-hop attestation); named mitigations (per-hop attestation, signed-capability handoff, MCP-trust-chain validator, supply-chain trust-chain enforcement, taint propagation across hops); anti-indicator (single-MCP topology emits zero Cat-10 findings). Plus **Pattern Category Disambiguation** subsection per ADR-032 Decision 7 (boundary marker Cat 1-8 single-agent vs. Cat 9-10 cross-agent; non-overlap carve Cat 6 LLM03 supply-chain upstream-ingestion vs. Cat 10 runtime trust propagation between already-registered MCPs) and **Primary Sources extension** appending OWASP ASI07:2026 + MITRE ATLAS AML.T0060. Categories 1-8 + Overview + Targeted DFD Element Types + Trigger Keywords held byte-identical to baseline (SC-006 BLOCKER). **Feature 224 F-4 adds 14th directory `tachi-human-trust-exploitation/`** — third net-new authored under the ADR-023 lean detection variant (third execution of the standalone-branch path after F-1 / F-2). Pattern catalog has 5 numbered categories scoped to the ASI09:2026 communication-axis signal class per ADR-033 D2 four-way Heuristic A boundary: (1) Missing AI Disclosure, (2) Authority-Attestation Without Verification, (3) Persuasion-Tone Without Safeguards, (4) Persona-Boundary Drift, (5) Synthetic-Relationship Safeguards Absence; primary citation OWASP ASI09:2026 on every pattern; ≥4 indicators + ≥1 worked example + ≥1 anti-indicator per category per ADR-033 D5. Vocabulary-disjoint from the four adjacent signal classes (`output-integrity` machine-victim sanitization / `misinformation` factual-integrity / `agent-autonomy` autonomy-axis / `tool-abuse` cross-agent-trust-propagation) per ADR-033 D2 with empty-intersection vocabulary test in `.aod/results/heuristic-a-verification.md` §3.3. ASI09 sub-scope carve-up with `agent-autonomy` (autonomy axis remains attributed to `agent-autonomy` / communication axis attributed to `human-trust-exploitation`) is documented at the ADR-033 D2 layer only — **no edit to `agent-autonomy.md` required** preserving SC-009 26-file zero-edit invariant. New `examples/consumer-agent-app/` clean-slate baseline added per architect Wave 3 Step 1 Q5 lean decision (NOT agentic-app extension) — WellnessCompanionChatbot mental-health/wellness companion archetype with all 4 FR-006 emission indicators engaged; emits 5 TE findings (TE-1..TE-5, one per Pattern Category 1-5) on regen; pipeline regen byte-identical (40-page PDF, SHA-256 `7ac0b639...269bce5`) per ADR-021 `SOURCE_DATE_EPOCH=1700000000` invariant. **Feature 229 F-5 extends BOTH pre-existing `tachi-denial-of-service/` and `tachi-model-theft/` directories** with 2+2 net-new Pattern Categories appended to their respective `detection-patterns.md` files (no new directory created — second execution of the Heuristic A enrichment branch via additive edits to existing skill directories at TWO-AGENT scope, vs. F-3's single-agent enrichment): `tachi-denial-of-service/` adds (12) LLM Inference-Request Flooding and Token Exhaustion — primary OWASP LLM10:2025 + related CWE-400; LLM-serving topology indicators (declared inference endpoint, LLM API gateway, per-tenant API key, token-counting middleware, cost-attribution layer) + named mitigations (per-tenant QPS rate limiting, output-token caps, request-timeout enforcement, queue-depth monitoring); explicit anti-indicator (non-LLM-serving topology emits zero Cat-12 findings) and (13) Context-Window Exhaustion Latency-Driven Variant — Q1 SPLIT Vector A targeting per-request latency budget; primary OWASP LLM10:2025 + related CWE-770; named mitigations (input-token caps, multi-turn context-window monitoring, latency-percentile alerting). Plus **Pattern Category Disambiguation** subsection per ADR-034 D7 (boundary marker Cat 9 generic resource-exhaustion vs. Cat 12/13 LLM-tier inference-exhaustion). `tachi-model-theft/` adds (10) Cost Amplification via Recursive or Cost-Asymmetric Prompting — primary OWASP LLM10:2025 + related CWE-400; LLM-serving topology indicators + cost-attribution-gap signals + named mitigations (output-token caps, recursive-prompt detection, cost-anomaly alerting); MITRE ATT&CK T1496 cited as prose context only (NOT in references array per ADR-034 D6) and (11) Denial-of-Wallet via Context-Window Cost Amplification — Q1 SPLIT Vector B + broader DoW; primary OWASP LLM10:2025 + related CWE-770; multi-tenant freemium structural indicators + per-tenant token-budget gaps; **Q3 severity-floor 2-condition CRITICAL rule** (HIGH default; CRITICAL only when multi-tenant freemium structurally evident AND BOTH per-tenant token budget AND cost alerting absent); named mitigations (per-tenant token budget enforcement, cost-alerting thresholds, freemium-tier rate limiting, cost-attribution per-call telemetry); MITRE ATT&CK T1496 cited as prose context only. Plus **Pattern Category Disambiguation** subsection per ADR-034 D7 (boundary marker Cat 6 pre-existing Unbounded Inference Consumption vs. Cat 10/11 specific cost-amplification + denial-of-wallet attack vectors below the abstraction level Cat 6 covers). Categories 1-11 (DoS) + Categories 1-9 (model-theft) + `## Overview` + `## Targeted DFD Element Types` + `## Trigger Keywords` (model-theft only — DoS companion has hybrid heading structure and lacks this section) held byte-identical to baseline (SC-014 BLOCKER). 4 NEW findings emerged on `examples/agentic-app/` regen — D-10 (Cat 12, Critical) + D-11 (Cat 13, Critical, Q1 SPLIT Vector A) + LLM-15 (Cat 10, Critical) + LLM-16 (Cat 11, High per Q3 default — single-tenant); cohesive category rendering in single Section 3.5 DoS + single Section 3.8 LLM (no fragmentation). **Feature 232 F-6 extends THREE pre-existing directories** (`tachi-tampering/` + `tachi-data-poisoning/` + `tachi-model-theft/`) with 1+3+3 net-new Pattern Categories appended to their respective `detection-patterns.md` files — third execution of the Heuristic A enrichment branch via additive edits to existing skill directories at THREE-AGENT scope (vs. F-3's single-agent + F-5's two-agent enrichment): `tachi-tampering/` adds Cat 10 (Adversarial Input Manipulation — primary OWASP ML01:2023 + related CWE-1039); `tachi-data-poisoning/` adds Cat 8 (Transfer Learning Attacks — primary OWASP ML07:2023) + Cat 9 (Model Skewing — primary OWASP ML08:2023) + Cat 10 (Predictive-ML Supply Chain Completeness — corpus-side ML06:2023 two-facet split per ADR-035 D-4 over Public Dataset Repository / Internal Merchant Transaction History / Feast Feature Store); `tachi-model-theft/` adds Cat 12 (Model Inversion / Output Reconstruction — primary OWASP ML03:2023 + AML.T0024 with output-minimization mitigations per ADR-035 D-5) + Cat 13 (Membership Inference — primary OWASP ML04:2023 + AML.T0024 with inference-privacy mitigations per ADR-035 D-5) + Cat 14 (Predictive-ML Artifact Supply Chain — artifact-side ML06:2023 two-facet split per ADR-035 D-4 over MLflow Model Registry / Weight Checkpoint Storage). Plus **Pattern Category Disambiguation** subsections per ADR-035 D-9 across all 3 catalogs (tampering Cat 10 vs Cat 1-9 + data-poisoning Cat 8/9/10 vs Cat 1-7 + model-theft Cat 12/13/14 vs Cat 1-11). 9 NEW F-6 findings emerged on new `examples/predictive-ml-app/` baseline (7th example) — T-10 + D-8 + D-9 + D-10/D-11 + LLM-1/LLM-2 + LLM-3/LLM-4; 43 total findings on regen. **Feature 237 F-7 extends FIVE pre-existing STRIDE directories** (`tachi-spoofing/` + `tachi-tampering/` + `tachi-info-disclosure/` + `tachi-privilege-escalation/` + `tachi-repudiation/`) with 2+3+4+1+1 net-new Pattern Categories appended to their respective `detection-patterns.md` files — fourth execution of the Heuristic A enrichment branch via additive edits to existing skill directories at FOUR-OR-FIVE-AGENT scope (vs. F-3's single-agent + F-5's two-agent + F-6's three-agent enrichment): `tachi-spoofing/` adds Improper Mobile Credential Usage (M1) + Insecure Mobile Authentication/Authorization (M3) Pattern Categories — primary OWASP M1:2024 + M3:2024 + related CWE-287 + CWE-308; `tachi-tampering/` adds Cat 11 Mobile Supply Chain Integrity (M2) + Cat 12 Mobile IPC Input Validation (M4) + Cat 13 Insufficient Mobile Binary Protections (M7) — primary OWASP M2:2024 + M4:2024 + M7:2024 + related CWE families; **M4 cross-agent boundary with F-1 `output-integrity`** per ADR-036 D-5 (Cat 12 mobile-IPC-input-side validation vs F-1's LLM-output-side sanitization on disjoint architectural-tells); `tachi-info-disclosure/` adds Insecure Mobile Communication (M5) + Inadequate Mobile Privacy Controls (M6) + Insecure Mobile Data Storage (M9) + Insufficient Mobile Cryptography (M10) Pattern Categories — primary OWASP M5/M6/M9/M10:2024 + related CWE-319/CWE-922/CWE-200/CWE-327; `tachi-privilege-escalation/` adds Mobile Security Misconfiguration / Privilege-Gain Variant (M8 privilege-gain) — primary OWASP M8:2024 + AML.T1626 (prose-only per ADR-036 D-7 catalog-absence); `tachi-repudiation/` adds Mobile Security Misconfiguration / Accountability-Loss Variant (M8 accountability-loss) — primary OWASP M8:2024 + AML.T1398 (prose-only per ADR-036 D-7 catalog-absence); **M8 dual-host disjoint architectural-tells** per ADR-036 D-4 (privilege-gain variant on `privilege-escalation` host vs accountability-loss variant on `repudiation` host — same OWASP entry split across two architectural-tell axes; mirrors ADR-035 D-4 ML06 two-facet precedent at architectural-tell layer rather than entry-decomposition layer). Plus **Pattern Category Disambiguation** subsections per ADR-036 D-9 across all 5 catalogs (each STRIDE companion gains a Disambiguation subsection delineating new mobile-tier Pattern Categories from pre-existing categories). **ATT&CK Mobile catalog gap propagation 3-of-3 prose-only** per ADR-036 D-7: T1474/T1626/T1398 ALL prose-only (NOT in references arrays — verified 0/0/0 catalog-absent per `mitre-attack.yaml`); 3-of-3 worst-case scale extending F-5 1-of-1 + F-6 3-of-6 ATLAS-gap precedent. 16 NEW F-7 findings emerged on new `examples/mobile-banking-app/` baseline (7th example — WellnessBank Android banking app archetype with 6 mobile-platform topology indicators); 62 total findings on regen. See [ADR-023](../02_ADRs/ADR-023-threat-agent-skill-references-pattern.md), [ADR-030](../02_ADRs/ADR-030-output-integrity-agent.md), [ADR-031](../02_ADRs/ADR-031-misinformation-agent.md), [ADR-032](../02_ADRs/ADR-032-asi07-tool-abuse-enrichment.md), [ADR-033](../02_ADRs/ADR-033-human-trust-exploitation-agent.md), [ADR-034](../02_ADRs/ADR-034-llm10-unbounded-consumption-audit.md), [ADR-035](../02_ADRs/ADR-035-ml-top-10-coverage-bundle.md), [ADR-036](../02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md). |

**Infographic templates** (Feature 029, extended Feature 091): Template files for multi-template infographic generation, co-located with the portable agent set.

| Template | Path | Purpose |
|----------|------|---------|
| Baseball Card | `templates/tachi/infographics/infographic-baseball-card.md` | Risk summary dashboard with key metrics |
| System Architecture | `templates/tachi/infographics/infographic-system-architecture.md` | Annotated architecture diagram with attack surface badges |
| Risk Funnel | `templates/tachi/infographics/infographic-risk-funnel.md` | 4-tier vertical funnel showing progressive risk reduction through tachi pipeline (Feature 053) |
| MAESTRO Stack | `templates/tachi/infographics/infographic-maestro-stack.md` | Layered stack diagram mapping findings to CSA MAESTRO seven-layer taxonomy (Feature 091) |
| MAESTRO Heatmap | `templates/tachi/infographics/infographic-maestro-heatmap.md` | Layer x severity heat map showing finding density across MAESTRO layers (Feature 091) |

**Portable agent set** (Feature 029, extended Feature 035, Feature 036, Feature 053, Feature 091): `.claude/agents/tachi/` contains the complete threat agent set (11 threat agents + orchestrator + report + infographic + risk-scorer + control-analyzer + 5 infographic templates) in Claude Code native format. This set is the Claude Code platform adapter output, installed at `.claude/agents/tachi/` for direct invocation. The portable set mirrors the `adapters/claude-code/agents/` content with paths adjusted for the installation depth.

**STRIDE agent capabilities** (Feature 005):
- Each agent enforces STRIDE-per-Element matrix targeting (DFD element type filtering)
- AI-specific threat patterns included per agent (e.g., credential theft of API keys for LLM services, model poisoning via tampered training data)
- OWASP API Security 2023 cross-references (API1-API10) alongside OWASP Top 10 2021, CWE, and MITRE ATT&CK
- Finding ID convention: S-N (Spoofing), T-N (Tampering), R-N (Repudiation), I-N (Info Disclosure), D-N (DoS), E-N (Privilege Escalation)
- Component-specific findings enforced -- generic/untargeted threats rejected by agent prompts

**Standards**: OWASP 3x3 risk matrix (likelihood x impact), STRIDE-per-Element methodology (DFD element mapping), OWASP API Security 2023 (API1-API10), OWASP Top 10 2021 (A01-A10), OWASP references (ASI-xx, MCP-xx, LLM0x:2025 for AI agents), CWE and MITRE ATT&CK cross-references, SARIF 2.1.0 (OASIS standard for static analysis results interchange, Feature 012; baseline correlation via `baselineState` property, Feature 074), Mermaid `flowchart TD` syntax (attack tree visualization, Feature 015), CVSS severity color palette (infographic visual encoding, Feature 018), CVSS 3.1 base scoring (quantitative risk scoring with full vector string auditability, Feature 035), CSA MAESTRO (Multi-Agent Environment, Security, Threat, Risk, and Outcome -- seven-layer taxonomy for agentic AI component classification, Feature 084; canonical layer names for L5 Evaluation and Observability, L6 Security and Compliance, L7 Agent Ecosystem aligned with Ken Huang's authoritative CSA definition in Feature 136; cross-layer attack chain correlation via deterministic transition lookup table, Feature 141; agentic pattern classification over the six canonical CSA MAESTRO patterns via deterministic rule table and multi-agent gate predicate in orchestrator Phase 3.6 synthesis, Feature 142), **OWASP LLM05:2025 Improper Output Handling** (output-side coverage closed by the `output-integrity` threat agent shipped in Feature 201 F-1 — detects XSS/DOM, SQLi, OS command injection, code injection, SSRF, template injection, and path traversal from LLM-generated output flowing into downstream execution sinks; every OI finding emits `source_attribution` citing LLM05 primary + ≥1 CWE related from `{CWE-22, CWE-78, CWE-79, CWE-89, CWE-94, CWE-918}`; ML09:2023 bundled in documentation-only prose per ADR-030 Decision 4 — not cited in `source_attribution` to preserve F-A1 catalog referential integrity), **OWASP LLM09:2025 Misinformation** (factual-integrity coverage closed by the `misinformation` threat agent shipped in Feature 206 F-2 — detects ungrounded factual emission, citation fabrication, overreliance/missing HITL on decision-critical output, retrieval-grounding gaps, and confidence-calibration absence; every MI finding emits `source_attribution` citing LLM09 primary + ≥1 CWE related from `{CWE-345, CWE-223}`; MITRE ATLAS `AML.T0042 Verify Attack` and NIST AI 600-1 §2.4 bundled in documentation-only pattern-catalog prose per ADR-031 — not cited in `source_attribution` as CONFIRMED ABSENT from F-A1 catalog YAMLs at PRD time), **OWASP Agentic Top 10 ASI07:2026 Insecure Inter-Agent Communication** (cross-agent / cross-MCP trust-propagation coverage closed by the `tool-abuse` threat agent enriched in Feature 219 F-3 — first execution of the Heuristic A enrichment branch; detects A2A channels without mutual auth / message signing / replay protection / taint propagation, and multi-hop MCP trust chains without per-hop attestation / signed-capability handoff / trust-chain validators; reuses existing `AG-{N}` finding ID prefix — no schema bump; every Cat-9/10 finding emits `source_attribution` citing OWASP ASI07:2026 primary + ≥1 CWE related from `{CWE-287, CWE-345}` + optional MITRE ATLAS `AML.T0060 Agent-in-the-Middle`; multi-agent / multi-MCP topology gate enforces zero emissions on single-agent / single-MCP architectures by construction — preserves byte-identity on 5 non-multi-agent baselines per ADR-021), **OWASP Agentic Top 10 ASI09:2026 Misuse and Abuse of Generative AI** (communication axis coverage closed by the `human-trust-exploitation` threat agent shipped in Feature 224 F-4 — third execution of the Heuristic A standalone-branch path after F-1 / F-2; detects missing AI disclosure, authority-attestation without verification, persuasion-tone without safeguards, persona-boundary drift, synthetic-relationship safeguards absence; `TE-{N}` finding ID prefix with schema bump 1.7 → 1.8 as 3rd application of ADR-030 D8 regex-alternation rule; every TE finding emits `source_attribution` citing OWASP ASI09:2026 primary + ≥1 CWE related; ASI09 sub-scope carve-up with `agent-autonomy` per ADR-033 D2 — autonomy axis attributed to `agent-autonomy` / communication axis attributed to `human-trust-exploitation` — preserves 26-file zero-edit invariant including `agent-autonomy.md` NOT-edit since its `owasp_references` already lists ASI-09; emits TE-{N} findings on Process DFD elements with consumer-facing AI emission patterns per ADR-033 D8), **OWASP LLM10:2025 Unbounded Consumption** (LLM-tier inference-exhaustion + cost-amplification + denial-of-wallet coverage closed by the `denial-of-service` STRIDE-tier and `model-theft` AI-tier agents enriched in Feature 229 F-5 — second execution of the Heuristic A enrichment branch after F-3, first at two-agent enrichment scope; detects LLM inference-request flooding + token exhaustion (DoS Cat 12), context-window exhaustion latency-driven variant (DoS Cat 13 — Q1 SPLIT Vector A), cost amplification via recursive or cost-asymmetric prompting (model-theft Cat 10), and denial-of-wallet via context-window cost amplification (model-theft Cat 11 — Q1 SPLIT Vector B + broader DoW); reuses existing `D-{N}` and `LLM-{N}` ID prefixes — **no schema bump** per ADR-034 D4 (second BLP-01 detection feature with no schema bump after F-3, first at two-agent enrichment scope, asymmetry with F-1/F-2/F-4 schema bumps); cites OWASP LLM10:2025 + LLM03:2025 (cost-flow-through-third-party adjacency) + CWE-400 (Uncontrolled Resource Consumption) + CWE-770 (Allocation of Resources Without Limits or Throttling) in `references` arrays; **MITRE ATT&CK T1496 prose-only invariant** (Resource Hijacking cited as prose context on Cat 10/11, NOT in `references` array — not catalog-resolvable per ADR-034 D6); F-A3 deferral on `source_attribution` populator wiring per ADR-028 D6 + ADR-034 D8 — `source_attribution: []` empty-array default preserved on Cat 10/11/12/13 emissions; **Q1 SPLIT cross-agent vector decomposition** (first BLP-01 sub-pattern with cross-agent vector decomposition: context-window exhaustion → DoS Cat 13 Vector A availability + model-theft Cat 11 Vector B economic damage); **Q3 severity-floor 2-condition CRITICAL rule** on Cat 11 (HIGH default; CRITICAL only when multi-tenant freemium structurally evident AND BOTH per-tenant token budget AND cost alerting absent); LLM-serving topology gate enforces zero Cat-12/13/10/11 emissions on architectures lacking LLM-serving indicators — preserves byte-identity on 6 non-LLM-serving baselines per ADR-021; **completes the OWASP LLM Top 10:2025 framework at 10/10 fully closed** combined with prior F-1 LLM05 + F-2 LLM09 closures + pre-F-1 LLM01-04/06-08 coverage). **Machine-readable taxonomy references** (Feature 180 F-A1) cite: OWASP LLM Top 10:2025 (LLM01-LLM10), OWASP Top 10 for Agentic Applications:2026 (ASI01-ASI10), OWASP Top 10:2021 (A01-A10), OWASP API Security Top 10:2023 (API1-API10), OWASP Mobile Top 10:2024 (M1-M10), OWASP Machine Learning Security Top 10:2023 (ML01-ML10), MITRE ATT&CK Enterprise matrix (38-seed techniques cited across the 13 threat-detection agents' `detection-patterns.md` files), MITRE ATLAS v5.4 (7-seed techniques + 5 October 2025 agent techniques AML.T0058-T0062 added via external curation), **NIST AI RMF 1.0 (NIST AI 100-1) Subcategory catalog — 72 Subcategories** (GOVERN 19 + MAP 18 + MEASURE 22 + MANAGE 13 per airc.nist.gov Playbook pages fetched 2026-04-17; primary-source-corrected from the PRD-era historical 68 count), **CWE Top 25 2025** (published 2025-12-11 by MITRE/CISA; 12 net-new entries beyond the 41-CWE seed cited across the 13 detection agents).

**Peer compliance frameworks (documented, not runtime dependencies)**: tachi records its posture relative to external agentic-AI compliance frameworks in ADRs without adding them as runtime dependencies. Peer frameworks currently documented: (1) **OWASP AIVSS v0.8** — tachi diverges at the present time (composite scoring structurally different from AIVSS amplification model); see [ADR-024](../02_ADRs/ADR-024-owasp-aivss-evaluation.md) (Feature 143). (2) **NIST AI RMF 1.0** (NIST AI 100-1) + **NIST AI 600-1 Generative AI Profile** — tachi adopts a documentation-only mapping posture (Option A) across three surfaces (AI RMF Functions × pipeline phases, AI RMF Subcategories × compensating-control categories, NIST AI 600-1 GAI risks × STRIDE+AI categories); the canonical mapping lives in the ADR and a companion skill reference (`.claude/skills/tachi-shared/references/nist-ai-rmf-mapping.md`); see [ADR-025](../02_ADRs/ADR-025-nist-ai-rmf-evaluation.md) (Feature 144). Neither framework is a runtime dependency — no new scripts, schemas, agents, or examples were introduced by either evaluation; both are additive documentation-only spikes.

**Output templates** (all under `templates/tachi/`):
- `templates/tachi/output-schemas/threats.md` -- canonical 8-section + Section 4a/4b threat model template with `schema_version: "1.4"` frontmatter (bumped from 1.3 in Feature 142 for additive `agentic_pattern` enum field on the finding IR per ADR-026 Schema Versioning Rule Extension; prior bump 1.2 → 1.3 in Feature 136 for canonical MAESTRO layer rename). Section 4a (Correlated Findings) added in Feature 010. Coverage matrix uses three-state cell model (deduplicated count, "---" analyzed-but-clean, "n/a" not-applicable). Risk summary shows deduplicated counts with raw count parenthetical when different. Baseline-aware extensions (Feature 074): baseline frontmatter block (baseline_source, baseline_date, baseline_finding_count), delta_status column in all threat tables, RESOLVED findings section (Section 4b), and coverage gate results section. Downstream propagation extensions (Feature 104): Section 7 Recommended Actions gains Status column carrying delta_status for downstream parser consumption; new Section 8 Delta Summary with finding lifecycle counts (NEW/UNCHANGED/UPDATED/RESOLVED), remediation proof, and baseline reference table. Agentic pattern extensions (Feature 142): new Pattern column in Section 7 findings table (after Category, before Component); new conditional Section 4b "Findings by Agentic Pattern" rendered iff `has-agentic-patterns: true` (set by orchestrator Phase 3.6); conditional per pattern subsection (suppressed for `none` and for patterns with zero findings).
- `templates/tachi/output-schemas/threats.sarif` -- SARIF 2.1.0 reference template for structural documentation (Feature 012). Complete JSON structure with placeholder values demonstrating rule definitions, result mapping, dual locations, relatedLocations for correlated findings, and partialFingerprints for stable tracking. Baseline correlation properties (Feature 074): `baselineRunId` in `partialFingerprints`, `baselineState` property per result (new/unchanged/updated/absent).
- `templates/tachi/output-schemas/threat-report.md` -- canonical 8-section narrative threat report template with `schema_version: "1.1"` frontmatter (Feature 015, extended Feature 104). Sections: Executive Summary, Architecture Overview, Threat Analysis (agent-by-agent narrative), Cross-Cutting Themes, Attack Trees (Mermaid `flowchart TD` with inline rendering and standalone file references), Remediation Roadmap (prioritized with effort estimates), Appendix: Finding Reference (complete traceability table), Delta Summary (baseline lifecycle breakdown with remediation proof). Baseline-aware extensions (Feature 104): frontmatter gains `baseline_source`, `baseline_date`, and `delta_counts` (new/unchanged/updated/resolved) fields; delta-aware narrative generation adapts executive summary and threat analysis to highlight changes since baseline; Section 8 Delta Summary with finding lifecycle table and baseline reference. Schema version bumped 1.0 to 1.1. Validated against `schemas/report.yaml`.
- `templates/tachi/output-schemas/risk-scores.md` -- scored threat model template with executive summary (highest/average composite, severity distribution), scored threat table (all 4 dimensions + composite + governance per finding), and methodology section documenting scoring weights and severity bands (Feature 035). Baseline-aware extensions (Feature 074): score_source column (inherited/fresh), baseline reference in frontmatter. Validated against `schemas/risk-scoring.yaml`.
- `templates/tachi/output-schemas/risk-scores.sarif` -- SARIF 2.1.0 scored output template extending `threats.sarif` structure with per-finding composite scores in `properties` bag, per-rule MAX composite in `security-severity`, and governance fields (Feature 035). Baseline-aware extensions (Feature 074): `score_source` property per result. Fingerprints preserved from source for GitHub Code Scanning alert supersession.
- `templates/tachi/output-schemas/compensating-controls.md` -- compensating controls analysis template with executive summary (control coverage percentage, residual risk), per-finding control inventory (detected controls with effectiveness rating, gaps, recommendations), coverage matrix (10 control categories x components), and residual risk summary (Feature 036). Baseline-aware extensions (Feature 074): control_carry_forward column, rescan_scope in frontmatter. Validated against `schemas/compensating-controls.yaml`.
- `templates/tachi/output-schemas/compensating-controls.sarif` -- SARIF 2.1.0 compensating controls output template extending `risk-scores.sarif` structure with control detection results, effectiveness classifications, and residual risk scores in `properties` bag (Feature 036). Baseline-aware extensions (Feature 074): `carry_forward` property per result. Fingerprints preserved from source for GitHub Code Scanning alert chain continuity.

**SARIF output** (Feature 012, extended Feature 074, Feature 084, Feature 142): The orchestrator produces `threats.sarif` alongside `threats.md` during Phase 4 (Assess). SARIF generation maps all finding IR data to SARIF 2.1.0 format with CVSS-aligned severity (Critical=error/9.0, High=error/8.0, Medium=warning/5.0, Low=note/2.0, Note=note/0.1), deterministic fingerprints (SHA-256 of ruleId + component_name) for stable GitHub Code Scanning alert tracking, dual physical/logical locations for component navigation, and optional OWASP/CWE taxonomy references. 8 rule IDs map to the combined STRIDE + AI category set (`tachi/stride/*` and `tachi/ai/*`). Baseline correlation (Feature 074): `baselineRunId` in `partialFingerprints` links findings to their baseline origin; `baselineState` property (new/unchanged/updated/absent) on each result enables cross-run tracking in SARIF-compatible tools. MAESTRO layer tags (Feature 084): `maestro-layer:{layer-id}` tag in `properties.tags[]` per result for layer-based filtering in SARIF-compatible tools. Agentic pattern tags (Feature 142): `maestro-pattern:{pattern_name}` tag in `properties.tags[]` per finding with non-`none` `agentic_pattern` — format matches the existing `maestro-layer:` convention (lowercase, colon-separator); findings with `agentic_pattern: none` receive no pattern tag.

**Report output** (Feature 015, extended Feature 104, extended Feature 141, extended Feature 129): After Phase 4, the orchestrator dispatches Phase 5 (Report, default-on, opt-out via `--skip-report` flag or `report: false` configuration) to the report agent (`agents/threat-report.md`). Phase 5 produces `threat-report.md` (8-section narrative report, schema_version 1.1) and `attack-trees/` (standalone Mermaid `flowchart TD` files, one per Critical/High finding). The report agent receives only `threats.md` as input in a fresh context (context isolation boundary). Downstream baseline propagation (Feature 104): the report agent reads baseline metadata and delta lifecycle counts from `threats.md` frontmatter and Section 8 Delta Summary, populates `threat-report.md` frontmatter with `baseline_source`, `baseline_date`, and `delta_counts` fields, and generates delta-aware narrative in the executive summary, threat analysis, and a new Section 8 Delta Summary. Cross-layer attack chain narrative (Feature 141): when `has-attack-chains` is true, the report agent generates Section 6 (Cross-Layer Attack Chains) with 150-300 word narratives per surfaced chain describing the causal progression across MAESTRO layers. **Parent-leaf decomposition (Feature 129)**: attack tree generation and delta reconciliation extracted into the `tachi-attack-tree-delta` leaf sub-agent. The threat-report parent dispatches to the sub-agent with four atomic inputs (Critical/High findings, `delta_counts`, baseline directory, output directory); the sub-agent writes `attack-trees/{finding-id}-attack-tree.md` files plus a structured `attack-trees/.manifest.json` describing per-tree decisions (`carried_forward` / `generated_fresh` / `regenerated` with similarity scores for Rule 3 reconciliations). Rule dispatch is deterministic on `delta_counts` — Rule 1 (all UNCHANGED → verbatim carry-forward), Rule 2 (any delta → fresh-all + Rule 3 reconcile UNCHANGED with token-overlap / gate-type / node-count algorithm), or no-baseline fallback (fresh-all). The parent reads the manifest and assembles inline Section 5 narrative using the new Section 5 Delta Annotations templates. `attack_tree_count` in `threat-report.md` frontmatter is unified across `schemas/report.yaml`, the output template, and the sub-agent manifest as "total attack trees produced (fresh + carried-forward)" — deliberately reverses the Feature 104 narrow-count interpretation. Pipeline shape and dispatch wiring unchanged — no new orchestrator phase, no new artifact; `attack-trees/` was already a Phase 5 output.

**Infographic output** (Feature 018, updated Feature 039, Feature 048, Feature 071): Infographic generation is a standalone `/tachi.infographic` command, no longer part of the orchestrator pipeline. Data extraction is delegated to a deterministic Python script (`scripts/extract-infographic-data.py`, Feature 071) that replaces LLM-based parsing, producing byte-identical JSON output for identical inputs. The script uses the shared `tachi_parsers.py` module for cross-output consistency with the security report pipeline. The `/tachi.infographic` command auto-detects the richest available data source using a three-tier priority hierarchy: `compensating-controls.md` (residual risk) > `risk-scores.md` (inherent risk) > `threats.md` (qualitative severity). Supports explicit file override with content-based type detection and template selection (`--template baseball-card|system-architecture|risk-funnel|maestro-stack|maestro-heatmap|maestro|all`). The `maestro` shorthand dispatches both MAESTRO templates (maestro-stack + maestro-heatmap); MAESTRO templates are gated by `has-maestro-data` and silently skipped when MAESTRO layer data is absent (Feature 091). The infographic agent invokes the extraction script, reads the resulting JSON, and generates the specification. Percentage values use the Largest Remainder Method to guarantee integer percentages summing to exactly 100. Risk labels adapt to source type: "Residual Risk" (compensating-controls), "Inherent Risk" (risk-scores), "Severity" (threats). Enhancement tips guide users toward richer pipeline tiers during auto-detection. Produces `threat-infographic-spec.md` (6-section visual risk specification conforming to `schemas/infographic.yaml`) and optionally `threat-infographic.jpg` (presentation-ready image via Google Gemini API, conditional on `GEMINI_API_KEY`). The specification is the primary deliverable; the image is best-effort with graceful degradation on API errors, rate limits, content policy rejections, or missing API key. See [ADR-014](../02_ADRs/ADR-014-gemini-api-optional-image-generation.md) for the external API integration decision and [ADR-017](../02_ADRs/ADR-017-deterministic-infographic-extraction.md) for the deterministic extraction decision.

**Risk scoring output** (Feature 035): The `/tachi.risk-score` command invokes the risk-scorer agent (`agents/tachi/risk-scorer.md`) as a standalone post-pipeline step. The risk-scorer parses `threats.md` (canonical) or `threats.sarif` (fallback), applies four-dimensional quantitative scoring per finding (CVSS 3.1 base, exploitability, scalability, reachability), calculates a weighted composite (0.35/0.30/0.15/0.20), maps to severity bands aligned with `output.yaml`, and assigns governance fields (risk_owner, remediation_sla, risk_disposition, review_date). Produces `risk-scores.md` (human-readable scored threat table) and `risk-scores.sarif` (machine-readable, supersedes `threats.sarif` in GitHub Code Scanning via preserved fingerprints). Schema: `schemas/risk-scoring.yaml`. The SARIF generation reference (`adapters/claude-code/agents/references/sarif-generation.md`) documents the structural differences between `threats.sarif` and `risk-scores.sarif`.

**Compensating controls output** (Feature 036): The `/tachi.compensating-controls` command invokes the control-analyzer agent (`agents/tachi/control-analyzer.md`) as the third stage of the threat analysis pipeline. The control-analyzer accepts `--target` (codebase path to scan), `--input` (risk-scores.md or risk-scores.sarif), and `--output-dir` flags. It executes a 6-phase pipeline: (1) parse scored threats, (2) discover target codebase structure, (3) detect existing security controls per component using 10 categories (8 STRIDE-mapped: authentication, integrity, audit-logging, data-protection, availability, access-control, input-validation, encryption-in-transit; 2 AI-specific: prompt-safety, agent-guardrails), (4) classify control effectiveness (Strong/Moderate/Weak/None), (5) recommend missing controls and calculate residual risk, (6) output dual-format results. Produces `compensating-controls.md` (human-readable control inventory with coverage matrix) and `compensating-controls.sarif` (machine-readable, supersedes `risk-scores.sarif` in GitHub Code Scanning alert chain). Schema: `schemas/compensating-controls.yaml`.

**Full pipeline output**: `threats.md` + `threats.sarif` (Phase 4) + `attack-chains.md` (Phase 3.5, conditional on chain detection, Feature 141) + `threat-report.md` + `attack-trees/*.md` (Phase 5). **Phase 3.6 pattern synthesis** (Feature 142) modifies the finding IR in-place (write-back) — surfaces the `agentic_pattern` field on findings via threats.md Pattern column and conditional Section 4b, threat-report.md Agentic Pattern Analysis section, and SARIF `maestro-pattern:<name>` tags; no new top-level artifact (pattern data lives on findings, not as a separate aggregate like attack-chains.md). **Post-pipeline**: `risk-scores.md` + `risk-scores.sarif` (via `/tachi.risk-score` command, Feature 035) + `compensating-controls.md` + `compensating-controls.sarif` (via `/tachi.compensating-controls` command, Feature 036) + `threat-infographic-spec.md` + `threat-infographic.jpg` (via `/tachi.infographic` command, Feature 039; image conditional on Gemini API key). **Baseline-aware mode** (Feature 074): When a baseline `threats.md` exists (auto-detected or via `--baseline` flag), all pipeline outputs include delta annotations (NEW/UNCHANGED/UPDATED/RESOLVED), inherited scores and control status for stable findings, and coverage gate results. First-run behavior (no baseline) is identical to the stateless pipeline. **Downstream baseline propagation** (Feature 104): Baseline metadata (source, date, finding count, run ID) and delta lifecycle counts from `threats.md` Section 8 are propagated through all downstream stages -- `threat-report.md` (delta-aware narrative, Section 8 Delta Summary), extraction scripts (`extract-report-data.py` Typst variables, `extract-infographic-data.py` JSON fields), and commands (`/tachi.infographic` and `/tachi.security-report` baseline data display). Parser functions in `tachi_parsers.py` (`parse_baseline_frontmatter`, `parse_resolved_findings`) provide the shared extraction layer.

**Example threat models**: `examples/` contains 3 standardized reference implementations (web-app, agentic-app, microservices) pairing Mermaid architecture diagrams with complete schema v1.1 threat model outputs (Feature 024). See `examples/README.md` for usage instructions and framework mapping.

---

### Python Scripts

**Python 3.9+** (stdlib only, no external dependencies)
- All `scripts/*.py` files must use only Python standard library modules
- Why: Zero-dependency deterministic processing; avoids pip/virtualenv overhead for adopters
- Constraints: No third-party packages, no f-strings requiring >3.9, pathlib-based paths

**Key scripts**:
| Script | Purpose | Added |
|--------|---------|-------|
| `scripts/tachi_parsers.py` | Shared parser module (~815 lines) providing deterministic parsers for markdown tables, YAML frontmatter, severity distributions, findings, scope data, compensating controls, and baseline metadata. Extracted from `extract-report-data.py` to enable cross-script consistency -- both report and infographic extraction scripts import the same parsing functions, guaranteeing identical interpretation of the same source artifacts. Baseline-aware extensions (Feature 104): `parse_baseline_frontmatter()` extracts baseline metadata from nested YAML frontmatter block, `parse_resolved_findings()` parses Section 4b Resolved Findings table, `parse_threats_findings()` extended with optional `delta_status` field extraction from Status column. Attack tree detection (Feature 112): `detect_artifacts()` extended to detect `attack-trees/` directory and return `has_attack_trees` boolean with directory path. Attack chain parsing (Feature 141): `parse_attack_chains()` parses `attack-chains.md` artifact into structured chain objects with member findings, layer progressions, and causal narratives. | Feature 071 (extracted from Feature 067), extended Feature 104, extended Feature 112, extended Feature 141 |
| `scripts/extract-report-data.py` | Deterministic extraction of structured data from tachi pipeline markdown artifacts (threats.md, risk-scores.md, compensating-controls.md, threat-report.md) into Typst data file (report-data.typ). Replaces LLM-based markdown parsing in the report-assembler agent. Supports 3-tier severity source hierarchy, validates internal consistency (severity sums, scope counts, unique finding IDs), and produces byte-identical output on identical inputs. MAESTRO data extraction (Feature 091): emits `has-maestro-data` boolean flag and per-layer finding variables for conditional `maestro-findings.typ` page inclusion. Baseline data extraction (Feature 104): emits baseline metadata variables (source, date, finding count, run ID) and delta lifecycle counts (new, unchanged, updated, resolved) from threats.md frontmatter and Section 8 Delta Summary; emits `has-baseline-data` boolean flag for conditional report section inclusion. Attack path extraction (Feature 112): `parse_attack_trees()` scans `attack-trees/` directory for Mermaid attack tree files, extracts metadata and cross-references findings; `render_mermaid_to_png()` converts Mermaid to PNG via `mmdc` subprocess (graceful fallback to raw text when `mmdc` unavailable); emits `has-attack-trees` boolean flag and structured attack tree array for conditional `attack-path.typ` page inclusion. Attack chain extraction (Feature 141): parses `attack-chains.md` via `parse_attack_chains()` from `tachi_parsers.py`, emits `has-attack-chains` boolean flag and structured chain array with layer progressions and Mermaid vertical MAESTRO layer stack diagrams for conditional `attack-chain.typ` page inclusion. **Coverage attestation aggregation (Feature 194 F-B)**: new aggregator surface (~291 lines) — `compute_has_source_attribution`, `build_per_finding_rows`, `build_per_framework_aggregates`, `load_framework_yaml_record_counts`, `classify_framework_items`, `_load_framework_yaml_records`, `_build_per_framework_aggregate` — scans parsed findings for non-empty `source_attribution` arrays (F-A2 contract), loads the 5 external-framework catalog YAMLs from `schemas/taxonomy/` via per-invocation local-dict cache, classifies every catalog item as Covered (≥1 `primary` citation) / Partial (zero `primary` AND ≥1 `related` / `derived`) / Gap (zero citations) per ADR-029 Decision 2, computes coverage percentage with catalog-length denominator per ADR-029 Decision 3, and emits `has-source-attribution` boolean + `per-finding-rows` + `per-framework-aggregates` Typst data variables for conditional `coverage-attestation.typ` page inclusion. `import yaml` is lazy-loaded inside `_load_framework_yaml_records` (post-merge refinement) to preserve the stdlib-only module-load invariant that enables the Feature 130 mmdc preflight CI gate to import the script without a pyyaml dev dependency. **Stream 4 aggregator extension (Feature 241 F-241 BLP-01 Tier 3 closure)**: `_load_framework_yaml_records()` extended at line 1073 (NOT line 1144 per Architect M-2 carry-forward at plan-day) with `in_scope_only: bool = False` parameter that filters records by `out_of_scope` field at YAML load level — NOT at the return statement of `_build_per_framework_aggregate()`; dual-emission `yaml_record_count` raw + `in_scope_yaml_record_count` filtered counters; coverage-percentage formula `(covered_count / in_scope_record_count) * 100` excludes runtime-only / IR-only / design-time-irrelevant records (e.g., 7 ATT&CK Enterprise tactics TA0005/7/8/9/10/11/40 per ADR-037 D-5); stdlib-only module-load invariant preserved by AST walk in `tests/scripts/test_pyyaml_deferred_import.py` (KB-037 invariant regression-guarded — pyyaml import remains lazy inside the function body, NOT at module top-level). See [ADR-037](../02_ADRs/ADR-037-web-api-coverage-attestation-and-populator-wiring.md) D-7 + D-8. Imports shared parsers from `tachi_parsers.py`. | Feature 067, refactored Feature 071, extended Feature 091, extended Feature 104, extended Feature 112, extended Feature 141, extended Feature 194, extended Feature 241 |
| `scripts/extract-infographic-data.py` | Deterministic extraction of structured infographic data (~1,100 lines) from tachi pipeline markdown artifacts into JSON data files for infographic templates (baseball-card, system-architecture, risk-funnel, maestro-stack, maestro-heatmap). Replaces LLM-based data extraction in the threat-infographic agent. Auto-detects richest data source (compensating-controls.md > risk-scores.md > threats.md), uses Largest Remainder Method for integer percentage rounding, and produces byte-identical JSON output on identical inputs. MAESTRO layer parsing extracts per-layer finding counts and severity distributions from `maestro_layer` field in source artifacts; gated by `has-maestro-data` flag (Feature 091). Baseline data extraction (Feature 104): extracts baseline metadata and delta lifecycle counts for infographic delta annotations; emits baseline fields in JSON output when baseline data is present. Imports shared parsers from `tachi_parsers.py`. | Feature 071, extended Feature 091, extended Feature 104 |

### Python Test Infrastructure (Feature 128)

**pytest 8.0+** (developer-only; not required by end users or the runtime pipeline)
- First-time addition of a Python test harness to tachi. Prior to Feature 128, `scripts/*.py` modules had no automated test coverage — ad-hoc manual verification only. Feature 128 bootstrapped the harness to cover the extraction pipeline (`extract-infographic-data.py`, `extract-report-data.py`, `tachi_parsers.py`) as part of the `executive-architecture` infographic template work.
- Why pytest (not `unittest`): fixture ergonomics, parametrized tests (one-to-many fixture coverage), rich assertion introspection, and `pytest-cov` coverage reporting — all standard choices for modern Python test suites. `unittest` would have required substantially more boilerplate for the same coverage.
- Why developer-only: runtime constraint from the Python Scripts section above (`scripts/*.py` must be stdlib-only, zero-dependency). The test harness lives outside runtime — adopters running `/tachi.threat-model`, `/tachi.security-report`, or `/tachi.infographic` do NOT install pytest. The harness is exclusively for tachi contributors verifying the extraction pipeline locally or in CI.

**Configuration files** (all added in Feature 128):

| File | Purpose |
|------|---------|
| `pyproject.toml` | Project config and `[tool.pytest.ini_options]` section; sets `testpaths = ["tests"]`, `python_files = ["test_*.py"]`, `addopts = "-ra --strict-markers"`. Non-disruptive to existing `scripts/*.py` runtime (no runtime imports of `pyproject.toml`). |
| `requirements-dev.txt` | Developer dependencies: `pytest>=8.0`, `pytest-cov>=4.1`. Installed via `pip install -r requirements-dev.txt`. |
| `Makefile` | `test:` target added in Feature 128 Wave 1 for `python3 -m pytest tests/` one-liner invocation. |
| `.gitignore` | Python patterns added in Feature 128 Wave 1 (`__pycache__/`, `.pytest_cache/`, `.coverage`, `htmlcov/`). |

**Test tree structure** (new in Feature 128):

| Path | Purpose |
|------|---------|
| `tests/conftest.py` | Shared pytest fixtures for all test modules |
| `tests/scripts/` | Test modules for `scripts/*.py` (9 test files covering extraction, parsing, output-shape contracts, mmdc preflight, and attack chain correlation) |
| `tests/scripts/fixtures/exec_arch/` | Input fixtures for the executive-architecture template (8 variations: with/without findings, single/multi-component scopes, tier-upgrade sources) |
| `tests/scripts/fixtures/report_data/` | Input fixtures for `extract-report-data.py` variants |
| `tests/scripts/fixtures/golden/` | Expected JSON payloads (5 golden files) for byte-identical regression testing |

**Running tests**:
```bash
pip install -r requirements-dev.txt      # one-time setup
make test                                  # or: python3 -m pytest tests/
python3 -m pytest tests/scripts/test_extract_infographic.py -v   # single module
python3 -m pytest tests/ --cov=scripts    # with coverage
```

**Backward-compatibility harness** (Feature 128 Wave 4): `tests/scripts/test_backward_compatibility.py` is a parametrized test that compiles the 5 unmodified example projects through the full PDF pipeline and compares the output byte-for-byte against committed `examples/*/security-report.pdf.baseline` files. The test sets `SOURCE_DATE_EPOCH=1700000000` before `typst compile` to neutralize PDF metadata timestamps — see [ADR-021](../02_ADRs/ADR-021-source-date-epoch-for-deterministic-pdf-comparison.md) for the reproducible-builds rationale.

**init.sh substitution test suite** (Feature 248, restructured Feature 250): `tests/scripts/test_init_sh_*.py` is a 4-file integration suite (`test_init_sh_substitution.py`, `test_init_sh_adversarial.py`, `test_init_sh_constitution.py`, `test_init_sh_self_delete.py`) plus 3 unit-level companion modules introduced in Feature 250 (`test_template_substitute_unit.py`, `test_init_input_unit.py`, `test_substitute_shim_canary.py`). The suite drives `scripts/init.sh` end-to-end inside isolated `tmp_path` clones via `tests/scripts/init_sh_helpers.run_init_in_clone` and asserts: (1) **metachar literal-preservation** across ≥13 adversarial inputs per NFR-003 (`AT&T`, `Cats & Dogs`, `\1\2 backref`, regex metachars `.*+?^$()`, pipe-bearing values, slashes — Cases 1-8 now covered at unit level by `test_template_substitute_unit.py` post-F-250); (2) **prompt-boundary input rejection** for newline (multi-line paste), NUL bytes, control characters, and over-length input (Cases 9-12 now covered at unit level by `test_init_input_unit.py` post-F-250 — uses bash process substitution `< <(printf ...)` to preserve `printf -v` caller-scope assignment per the F-250 R-1 mitigation); (3) **file-level byte-identity** post-substitution (Case 13 retained at integration tier in `test_init_sh_adversarial.py`); (4) **closed-contract residual scan** halting non-zero on any orphan canonical-12 placeholder in personalized-category files; (5) **constitution byte-equality** between `.aod/templates/constitution-clean.md` and `sed`-stripped `constitution-instructional.md` (T007 byte-equivalence proof); (6) **`shopt -u patsub_replacement` shim presence** as a permanent CI canary (`test_substitute_shim_canary.py` — F-250 TC-1 closure asserting the shim stays in `template-substitute.sh`). Fixture-replay tests use a recorded golden tree under `tests/fixtures/init-baseline-tree/` with `tests/fixtures/regenerate-baseline.sh` as the regen procedure when the canonical-12 set changes (lockstep with [ADR-038](../02_ADRs/ADR-038-placeholder-substitution-strategy.md) D-5 paragraph 4). **F-250 Phase 6 Option Z restructure**: baseline scope restricted from ~600 files to ~53 substitution-target files (eliminates "regenerate baseline on every doc edit" maintenance tax); file-set check converted from strict equality to ASYMMETRIC (drops are FAIL — substitution regression — additions are TOLERATED — repo growth is not a regression); `init.sh` invocations dropped 17 → 5 per CI run (FR-014 / SC-003) via session-scoped fixture promotion. **Subprocess + pytest timeout pair raised**: `init_sh_helpers.run_init_in_clone(timeout_sec=)` default 300s → 900s; `.github/workflows/tachi-pytest.yml` `pytest --timeout` 360s → 1080s — accommodates macos-latest cold-cache projection (~560-700s at 4× dev-hardware multiplier) with ~200s headroom on the worst observed scenario; the outer pytest cap is always ≥120s greater than the inner subprocess cap so the inner timeout fires first with diagnostic output. CI matrix execution at `.github/workflows/tachi-pytest.yml` on `[macos-latest, ubuntu-latest]` per the Bash 3.2 NFR-001 gate above; macos-latest wall time post-F-250 = 5m 19s on the closing run (≤15 min target met per SC-002).

**Session-scoped pytest fixtures pattern** (Feature 250 Phase 6 Option Z): the canonical `init.sh`-invoking fixture `init_run` lives in `tests/scripts/conftest.py` at session scope. Promoted from 5 module-scoped duplicates (substitution, constitution, self_delete, plus the function-scoped pattern in adversarial). The shared fixture invokes `scripts/init.sh` ONCE in a session-scoped `tmp_path_factory` clone; every consuming module asserts read-only properties (file existence, byte content, mode bits) of the post-init clone. Module-scoped duplicates would have multiplied the macos cold-cache cost across modules (~300-560s per invocation × 5 modules = ~25 min). The pattern reserves function-scoped invocation for tests that pre-seed fixtures into the clone before `init.sh` runs (e.g., `test_case_13_file_level_byte_identity` seeds adversarial-payload files; cannot share the canonical clone). Documented as a reusable pattern at [docs/architecture/03_patterns/README.md#pattern-session-scoped-init-sh-fixture](../03_patterns/README.md#pattern-session-scoped-init-sh-fixture).

**Asymmetric baseline file-set check pattern** (Feature 250 Phase 6 Option Z): `test_personalized_tree_bytes_match_baseline` in `test_init_sh_substitution.py` enforces an asymmetric set comparison — every file present in the baseline MUST be present in the post-init tree (drops are FAIL, indicating substitution regression), but additions to the post-init tree are TOLERATED (repo growth between baseline regenerations is not a regression). Combined with the ~53-file substitution-target restriction in `tests/fixtures/regenerate-baseline.sh`, this eliminates the recurring "regenerate baseline on every PR that edits any doc" maintenance tax that plagued F-248. Documented as a reusable pattern at [docs/architecture/03_patterns/README.md#pattern-asymmetric-baseline-file-set-check](../03_patterns/README.md#pattern-asymmetric-baseline-file-set-check).

### Shell Scripts

**Bash 3.2** (macOS default `/bin/bash`)
- All `.aod/scripts/bash/*.sh` files must be Bash 3.2 compatible
- Why: macOS ships Bash 3.2.57 due to GPLv3 licensing; portability is mandatory
- Constraints: No associative arrays, no `${var^^}`, no `readarray`/`mapfile`, no `&>` redirection, no lowercase parameter expansion `${var,,}`
- **CI matrix gate (Feature 248 NFR-001)**: `.github/workflows/tachi-pytest.yml` runs the F-248 init.sh suite (`tests/scripts/test_init_sh_*.py` — 20 tests) on a 2-runner matrix `[macos-latest, ubuntu-latest]`. macOS bash 3.2.57 is the OLDEST supported runtime and the **strictest gate** for compatibility constraints in `.aod/scripts/bash/template-substitute.sh` and `.aod/scripts/bash/init-input.sh`; ubuntu bash 5.x is the modern reference shell. A green Ubuntu run on top of a green macOS run proves the test suite is portable, not bash-3.2-quirk-locked. Both legs MUST pass green for [ADR-038](../02_ADRs/ADR-038-placeholder-substitution-strategy.md) §Test Coverage to close. Path-filtered to `scripts/init.sh` + `.aod/scripts/bash/init-input.sh` + `.aod/scripts/bash/template-{substitute,validate,git}.sh` + `.aod/templates/constitution-{clean,instructional}.md` + `.aod/template-manifest.txt` + `tests/scripts/test_init_sh_*.py` + `tests/scripts/init_sh_helpers.py` + `tests/fixtures/init-baseline-tree/**` + `tests/fixtures/regenerate-baseline.sh` + the workflow file itself

**Key scripts**:
| Script | Purpose | Added |
|--------|---------|-------|
| `.aod/scripts/bash/logging.sh` | Simple logging utility for timestamped log entries; provides `aod_log` function with configurable output path and graceful error handling | Feature 049 |
| `.aod/scripts/bash/run-state.sh` | Atomic read/write/validate for orchestrator state (`.aod/run-state.json`); includes compound helpers for incremental reads and governance caching | Feature 022, extended Feature 030 |
| `.aod/scripts/bash/github-lifecycle.sh` | GitHub Issue label management for stage transitions | Pre-022 |
| `.aod/scripts/bash/backlog-regenerate.sh` | Regenerate product backlog from GitHub Issues | Pre-022 |
| `scripts/generate-adapter-version.sh` | Generate `VERSION` manifest for platform adapters with source commit SHA, timestamp, and per-agent SHA-256 checksums for drift detection | Feature 021 |
| `scripts/install.sh` | Bash 3.2+ install script that copies all distributable files from tachi source to target project using INSTALL_MANIFEST.md machine-parseable section; supports `--source`, `--version` (git tag checkout with trap cleanup), and `--help` flags | Feature 066 |
| `.aod/scripts/bash/template-substitute.sh` | Per-file placeholder substitution helper using **bash parameter expansion** `${content//\{\{KEY\}\}/value}` (literal pattern + literal replacement, no regex interpretation); supersedes `find ... -exec sed -i ... +` per [ADR-038](../02_ADRs/ADR-038-placeholder-substitution-strategy.md) D-1; provides `aod_template_substitute_placeholders <src> <dest>` (canonical 12 placeholders, atomic write via `<dest>.tmp` + `mv`, file-mode preservation) and `aod_template_assert_no_residual <file>` (residual `{{KEY}}` scan, scoped to `personalized` category from `.aod/template-manifest.txt` per ADR-038 D-6); eager top-of-file source from `init.sh` with pre-flight re-init check; eliminates the `OSTYPE` macOS-vs-Linux sed branching | Feature 248 |
| `.aod/scripts/bash/init-input.sh` | Interactive `read -p` input validator implementing the **validation triplet pattern** per [ADR-038](../02_ADRs/ADR-038-placeholder-substitution-strategy.md) D-5: `regex-validate` → `reject-on-mismatch` → `printf -v` assignment; rejects newline (multi-line paste), NUL bytes, control characters, over-length input with re-prompt up to 3 strikes before non-zero exit; the function is interactive `read -p`-only (forward-looking F-2 BLP-02 Wave 2 `defaults.env` strict-KV parser reuses the PATTERN, NOT the function) | Feature 248 |
| `.aod/scripts/bash/template-validate.sh` | Companion validator for `template-substitute.sh` — closed-canonical-12 set verification and helper invariants for personalization.env contract | Feature 248 |
| `.aod/scripts/bash/template-git.sh` | Companion git helpers for `template-substitute.sh` — supports the personalization.env gitignored-by-default posture per [ADR-038](../02_ADRs/ADR-038-placeholder-substitution-strategy.md) D-4 (multi-tenant fork safety) | Feature 248 |

### CLI Dependencies

| Tool | Required By | Purpose | Install |
|------|-------------|---------|---------|
| `jq` | `run-state.sh` | JSON parsing and atomic state manipulation | `brew install jq` (macOS) / `apt-get install jq` (Linux) |
| `gh` | `github-lifecycle.sh`, `run-state.sh` (optional), `scripts/init.sh` (optional) | GitHub Issue/label management, Projects board creation during init | `brew install gh` / `gh auth login` |
| `python3` | `scripts/extract-report-data.py` (invoked by report-assembler agent) | Deterministic markdown-to-Typst data extraction for security report pipeline; stdlib only, no pip dependencies (Feature 067) | Pre-installed on macOS; `apt-get install python3` (Linux) |
| `typst` | `/tachi.security-report` command (report-assembler agent) | PDF compilation from modular `.typ` templates; renders security assessment reports with brand assets, auto-generated TOC, and conditional page inclusion (Feature 054, extended Feature 060); MAESTRO Findings page conditionally included via `has-maestro-data` flag (Feature 091); Attack Path pages conditionally included via `has-attack-trees` flag (Feature 112); Attack Chain pages conditionally included via `has-attack-chains` flag with vertical MAESTRO layer stack diagrams (Feature 141); **Coverage Attestation section** (single per-finding attribution table + 5 per-framework coverage matrix pages over the ADR-028 5-value external taxonomy enum) conditionally included via `has-source-attribution` flag using the `dictionary(module).at(name, default: ...)` guard idiom per ADR-029 Revision History (Feature 194) | `brew install typst` (macOS) / `cargo install typst-cli` / [typst.app](https://github.com/typst/typst/releases) |
| `mmdc` | `scripts/extract-report-data.py` (invoked by report-assembler agent) | Mermaid CLI for rendering attack tree Mermaid diagrams to PNG images for PDF report embedding; hard prerequisite as of Feature 130 when `attack-trees/` contains Critical/High findings — see [ADR-022](../02_ADRs/ADR-022-mmdc-hard-prerequisite.md) (introduced Feature 112, posture changed Feature 130) | `npm install -g @mermaid-js/mermaid-cli` |
| `pytest` | `tests/scripts/*.py` (developer-only; not runtime) | Python test runner for the `scripts/*.py` extraction pipeline and `tachi_parsers.py` shared module; invoked via `make test` or `python3 -m pytest tests/` (Feature 128) | `pip install -r requirements-dev.txt` |

**Note**: `gh` degrades gracefully -- the orchestrator falls back to artifact-only detection when `gh` is unavailable or unauthenticated. Similarly, `scripts/init.sh` skips GitHub Projects board creation when `gh` is missing, unauthenticated, or lacks the `project` OAuth scope, reporting status in the init summary with remediation guidance.

**Note**: `typst` is required only for PDF report generation. The `/tachi.security-report` command validates Typst installation before compilation and reports a clear error if unavailable. All other tachi commands operate without Typst.

**Note**: `mmdc` (Mermaid CLI) is a **hard prerequisite** when `attack-trees/` contains Critical/High findings. The `/tachi.security-report` pipeline aborts at preflight with a non-zero exit code and a canonical three-line error message (naming the missing tool, the reason, and the exact install command `npm install -g @mermaid-js/mermaid-cli`) if `mmdc` is not on `PATH`. Projects without attack trees continue to work without `mmdc` — the gate fires only when attack-tree artifacts are present. See [ADR-022](../02_ADRs/ADR-022-mmdc-hard-prerequisite.md) for the rationale and the silent-fallback-to-loud-failure migration (Feature 130).

### External API Dependencies (Optional)

| API | Required By | Purpose | Authentication | Graceful Degradation |
|-----|-------------|---------|----------------|----------------------|
| Google Gemini API (`gemini-3-pro-image-preview`) | `agents/threat-infographic.md` (via `/tachi.infographic` command) | Optional image generation for threat infographic visualization | `GEMINI_API_KEY` environment variable | Spec always produced; image skipped if API key missing, rate limited (429), timed out (60s), or content policy rejected. Fallback model: `gemini-3.1-flash-image-preview`. See [ADR-014](../02_ADRs/ADR-014-gemini-api-optional-image-generation.md). |

**Note**: The Gemini API is entirely optional. The `/tachi.infographic` command produces the infographic specification (`threat-infographic-spec.md`) regardless of API availability. The specification is self-contained and can be used by a designer to render the infographic manually. Image generation is a best-effort enhancement.

### Template Variables

`scripts/init.sh` performs `sed` substitution on user-facing template files during `make init`. The following placeholders are replaced at init time:

| Placeholder | Replaced With | Scope |
|-------------|---------------|-------|
| `tachi` | Adopter's project name (entered at `make init` prompt) | 12 template files (Feature 061) |
| `2026-03-21` | Current date at init time | Select files |

**`tachi` is a first-class template variable** (Feature 061). All user-facing template files in `.claude/`, `docs/`, `CLAUDE.md`, and `README.md` use this placeholder rather than hardcoding "Agentic Oriented Development Kit". After `make init`, adopters see their own project name throughout.

When adding a new user-facing template file to the kit, use `tachi` wherever the project name should appear and confirm the file is included in the `scripts/init.sh` substitution loop. See [ADR-009](../02_ADRs/ADR-009-template-variable-expansion-scope.md) and the [Template Variable Expansion pattern](../03_patterns/README.md#pattern-template-variable-expansion).

---

### Subagent Results Directory

**Directory**: `.aod/results/` (ephemeral session artifacts, gitignored)
- Architecture: File-based offloading for minimal subagent returns (Feature 073)
- Convention: Each subagent writes detailed findings to `.aod/results/{agent-name}.md` before returning
- Return policy: Subagents return only STATUS + ITEMS count + DETAILS path to the main context (max 10 lines / ~200 tokens)
- Overwrite semantics: Each invocation overwrites the prior results file for the same agent
- Initialization: Subagents create the directory if absent (self-healing, no pre-init required)
- See [ADR-010](../02_ADRs/ADR-010-minimal-return-architecture.md) for the design decision
- See [Minimal-Return Subagent pattern](../03_patterns/README.md#pattern-minimal-return-subagent) for implementation guidance

**Context budget impact**: A full Triad review cycle (3 reviewers) consumes less than 600 tokens in the main context (down from 1,500-6,000 tokens), enabling 90+ minute sustained orchestration sessions.

---

### Stack Packs System

**Directory**: `stacks/` (convention contracts, persona supplements, scaffold templates, rules)
- Architecture: Dual-surface injection pattern (Feature 058)
- Management skill: `.claude/skills/aod-stack/SKILL.md` (`/aod.stack use|remove|list|scaffold`)
- State file: `.aod/stack-active.json` (JSON, tracks active pack name and activation timestamp)
- Runtime rules surface: `.claude/rules/stack/` (copied on activation, cleaned on removal)
- See ADR-007 for the design decision behind dual-surface injection

**Shipped packs**:
| Pack | Status | Purpose |
|------|--------|---------|
| `stacks/nextjs-supabase/` | Full | Next.js + TypeScript + Supabase + Prisma + Vercel conventions |
| `stacks/fastapi-react/` | Full | Python FastAPI + SQLAlchemy 2.0 async + React 19 + TypeScript + Vite + Docker Compose (Feature 078) |
| `stacks/fastapi-react-local/` | Full | Python FastAPI + SQLAlchemy 2.0 async + aiosqlite + React 19 + Vite + Tailwind CSS 4 — zero external dependencies, local-first variant (Feature 085) |
| `stacks/swiftui-cloudkit/` | Skeleton | SwiftUI + CloudKit native iOS conventions |
| `stacks/knowledge-system/` | Full | Markdown + YAML + Claude Code for knowledge-intensive content systems (Feature 064) |

**Pack anatomy** (each pack directory):
| Path | Purpose |
|------|---------|
| `STACK.md` | Convention contract (required, ≤500 lines) |
| `agents/*.md` | Persona supplements for specialized/hybrid agents (≤100 lines each) |
| `rules/*.md` | Stack-specific coding rules (copied to `.claude/rules/stack/` on activation) |
| `scaffold/` | Project template files (optional, used by `/aod.stack scaffold`) |
| `skills/` | Stack-specific skills (optional, reserved for future use) |

**Context budget enforcement**:
| Component | Max Lines | Loaded When |
|-----------|-----------|-------------|
| STACK.md | 500 | Every agent invocation (if pack active) |
| Persona supplement | 100 | Specialized/hybrid agent invocations only |
| Stack rules (all files combined) | 200 | Every agent invocation (via rules discovery) |
| Total pack overhead | 800 | Maximum per invocation |

### Kickstart Skill

**Skill file**: `.claude/skills/~aod-kickstart/SKILL.md` (`/aod.kickstart`)
- Architecture: Three-stage interactive workflow (Idea Intake → Stack Selection → Guide Generation) (Feature 085)
- Output: `docs/guides/CONSUMER_GUIDE_{PROJECT_NAME}.md` — sequenced consumer guide with 6-10 seed features
- Stack detection: Reads `.aod/stack-active.json` to auto-detect active pack; falls back to manual selection
- Seed features structured for direct copy-paste into `/aod.discover`
- No infrastructure dependencies; pure methodology/template skill

### Orchestrator Skill Architecture

**Skill file**: `.claude/skills/~aod-run/SKILL.md` (~405 lines, core execution loop)
- Architecture: Segmented prompt with on-demand reference loading (Feature 030)
- Core file contains routing, state machine loop, and stage mapping
- Reference files loaded via Read tool only when needed:

| Reference File | Purpose | Loaded When |
|----------------|---------|-------------|
| `references/governance.md` | Governance gate detection, tiers, rejection handling | Governance cache miss |
| `references/entry-modes.md` | New-idea, issue, resume, status entry handlers | Mode routing |
| `references/dry-run.md` | Read-only preview handler | `--dry-run` flag |
| `references/error-recovery.md` | Corrupted state and lifecycle complete handlers | Error or completion |

- See ADR-002 for the design decision behind prompt segmentation

### Orchestrator State

**State file**: `.aod/run-state.json`
- Format: JSON (managed via `jq`)
- Atomicity: Write-then-rename pattern (`write to .tmp`, then `mv`) for crash safety
- Schema version: `1.0`
- Governance cache: Verdicts stored in `governance_cache` object to eliminate redundant artifact reads (Feature 030)
- Compound helpers: `aod_state_get_multi`, `aod_state_get_loop_context`, `aod_state_get_governance_cache` for incremental reads (Feature 030)
- See ADR-001 for the design decision behind atomic state management
- See ADR-006 for the design decision behind non-fatal error handling in observability operations

---

### Platform Adapter Output Formats (Feature 021)

**Directory**: `adapters/` (5 platform-specific subdirectories)
- Architecture: Hub-and-spoke distribution -- `agents/` is the immutable source of truth; each adapter transforms agents into a platform-native format
- See [ADR-015](../02_ADRs/ADR-015-platform-adapter-hub-and-spoke-distribution.md) for the design decision

| Adapter | Output Format | File Extension | Key Format Characteristics |
|---------|---------------|----------------|---------------------------|
| Claude Code | Markdown with Claude Code frontmatter | `.md` | `name` and `description` fields in YAML frontmatter; tachi metadata in `## Metadata` body section |
| Generic | Self-contained numbered markdown prompts | `.md` | No frontmatter; `{{ARCHITECTURE_INPUT}}` placeholder; sequential numbering (`00-` through `13-`) |
| Cursor | Cursor rule files | `.mdc` | `alwaysApply: true` (orchestrator) or Agent Requested (threat agents); `description` field for activation |
| Copilot | Copilot agent files | `.agent.md` | Size-split pattern for agents >30K chars: compact `.agent.md` + `.github/instructions/*.md` companion |
| GitHub Actions | GitHub Actions workflow | `.yml` | Workflow YAML triggering on architecture file changes; `codeql/upload-sarif@v3` for Code Scanning integration |

**SARIF integration** (GitHub Actions adapter): The CI workflow produces `threats.sarif` (SARIF 2.1.0 format, see Feature 012 / ADR-013) and uploads it to GitHub Code Scanning via the official `codeql/upload-sarif@v3` action. Findings appear as security alerts with CVSS-aligned severity.

**Drift detection**: Each adapter includes a `VERSION` manifest file (YAML format) containing source commit SHA, generation timestamp, and per-agent SHA-256 checksums. Generated by `scripts/generate-adapter-version.sh` (Bash 3.2 compatible).

---

**Template Instructions**: Replace all `{{TEMPLATE_VARIABLES}}` with your actual technology choices. Document the "Why" for each major decision.

# OKRs (Objectives and Key Results) - tachi

**Last Updated**: 2026-05-14 (Feature 292 delivered — F-292 Output-Integrity Cross-Sink Refinement (community-feedback follow-on to F-1 BLP-01 `output-integrity`, PR #202, v4.21.0) / single-agent Heuristic A enrichment-branch closing three pattern-catalog gaps surfaced by @armorer-labs in discussion #179: Gap 1 Cat 6 Vector-Filter / Search-DSL Injection (CWE-943 primary + OWASP LLM08:2025) + Gap 2 Cat 2/Cat 5 Package-Manager / CI-Workflow keyword extension + Gap 3 Cross-Agent Handoff Sinks prose subsection with Memory-Promotion Rules JSON-schema mitigation example (OWASP ASI06 anchor) / `examples/multi-tenant-rag-app/architecture.md` new baseline exercising Gap 1 vector-filter pattern / **ADR-045 Accepted** (8th BLP-01 Heuristic A enrichment ADR) / F-292 carve-out in `tests/scripts/test_backward_compatibility.py` zero-edit invariant test (F-241 precedent) / 25/36 tasks marked complete (11 incomplete are MANUAL-ONLY post-merge community-engagement tasks T005/T017/T019-T024/T026/T031/T034) / 3 user stories shipped (US-1 P1 Multi-Tenant RAG + US-2 P1 AI-Coding-Assistant Package-Manager + US-3 P2 Multi-Agent Handoff all passing) + 2 community-engagement stories in-flight via post-merge MANUAL-ONLY tasks (US-4 Maintainer + US-5 First-Time Contributor @armorer-labs handoff) / community provenance preserved per F-260 PR #262 @north-echo precedent / no `finding.yaml` schema bump / no new agent file / PR #293 squash-merged as `0629fa2` on main 2026-05-14 / release-please PR #294 `chore(main): release 4.36.0` opened post-merge under SC-11 ~30s SLO. Prior: 2026-05-10 (Feature 282 delivered — F-5 Pre-commit Secret-Scanning Defaults (BLP-02 Wave 4+) — **BLP-02 5-feature enterprise-hardening initiative CLOSED at 5/5 features delivered** — LinkedIn-thread punch-list (disclosure F-3 / permissions F-4 / secret-scanning F-5) fully retired. Prior: 2026-05-01 (Feature 241 delivered — F-8 + F-A3 Web/API Coverage Attestation + Populator Wiring [Tier 3] / fifth execution of Heuristic A enrichment branch at 11-host scope (full F-A3 closure across all detection-tier hosts) / six Partial OWASP items closed (A05 + A06 + API6 + API8 + API9 + API10) / pipeline-generated `9. Coverage Attestation` page emitted on all 8 baselines / **OWASP five-framework total = 50/50 Covered** — third framework family closure milestone / **BLP-01 11-feature initiative CLOSES at 11/11 features delivered** / F-A3 deferral debt cleared across ADR-032 + ADR-034 D-8 + ADR-035 D-10 + ADR-036 D-10 / fifth BLP-01 detection feature with zero schema bump / 29-day envelope collapsed to single-day delivery))
**Owner**: Product Manager (product-manager)
**Status**: Template - Complete after MVP launch

---

## When to Create This

**Create OKRs AFTER your MVP launches and you have baseline metrics.**

Before MVP:
- You don't have baseline numbers to improve
- Your key metrics may not be measurable yet
- Focus should be on shipping, not measuring

**Pre-MVP Goal**: Launch MVP
**Post-MVP**: Set quarterly OKRs based on real usage data

**First OKRs**: Typically set 4-6 weeks after MVP launch when you have initial metrics.

> **AOD Lifecycle Note**: OKR documents can be scaffolded via `/aod.okrs`, which
> generates a standard OKR template (Objective, Key Results, Initiatives) in this
> directory with PM sign-off. `/aod.define` reads current OKRs for PRD alignment,
> and `DOCS_TO_UPDATE_AFTER_NEW_FEATURE.md` includes OKR progress as a checklist
> item during `/aod.deliver`. You can still create and maintain OKR files manually.

---

## Overview

OKRs align the team around measurable goals. They answer:
- **Objective**: What do we want to achieve? (qualitative)
- **Key Results**: How do we know we achieved it? (quantitative)

---

## OKR Template

```markdown
# tachi OKRs - YYYY-QN

**Quarter**: [Q1/Q2/Q3/Q4] YYYY
**Status**: [Planning | In Progress | Complete]
**Review Date**: [YYYY-MM-DD]

## Objective 1: [Qualitative Goal]

**Why This Matters**: [Alignment with product vision]

### Key Result 1.1: [Measurable Outcome]
- **Baseline**: [Starting value]
- **Target**: [End of quarter goal]
- **Current**: [Current progress]
- **Status**: 🟢 On Track | 🟡 At Risk | 🔴 Off Track
- **Owner**: [Team member]

### Key Result 1.2: [Another Measurable Outcome]
[Same structure]

## Objective 2: [Another Qualitative Goal]
[Same structure]

---

## Progress Tracking

**Week of [Date]**:
- KR 1.1: [Update]
- KR 1.2: [Update]

**Risks**:
- [Risk 1]

**Actions**:
- [ ] [Action item to address risk]
```

---

## OKR Best Practices

### Objectives (Qualitative)
- ✅ Inspiring and motivational
- ✅ Aligned with product vision
- ✅ Achievable but ambitious
- ❌ Not a task list

**Example Good Objectives**:
- "Become the go-to platform for [user segment]"
- "Delight users with exceptional [experience]"
- "Establish market leadership in [category]"

### Key Results (Quantitative)
- ✅ Specific and measurable
- ✅ Time-bound (end of quarter)
- ✅ 70-80% achievable (stretch goal)
- ❌ Not activities (use metrics)

**Example Good Key Results**:
- "Increase active users from 1,000 to 5,000"
- "Achieve NPS score of 50+"
- "Reduce churn from 10% to 5%"

---

## Integration with Product Workflow

### OKRs Drive PRDs
- Each PRD should support at least one key result
- PRD success metrics align with OKR key results
- PRD prioritization based on OKR impact

### OKRs Inform Roadmap
- Roadmap phases deliver on quarterly OKRs
- Feature prioritization based on OKR contribution
- Roadmap adjusts if OKRs change

---

## Review Cadence

### Weekly Check-In (15 min)
- Update key result progress
- Identify blockers
- Adjust tactics if needed

### End of Quarter Review (2 hours)
- Score all key results (0.0 - 1.0 scale)
- Document learnings
- Plan next quarter OKRs

---

**Template Instructions**: Create a new OKR file each quarter (YYYY-QN.md). Delete this message after creating your first OKR document.

---

## Feature Delivery Log

> **Purpose**: Track feature deliveries for OKR alignment once quarterly OKRs are established.
> Features listed here should be mapped to Key Results when OKRs are created.

| Date | Feature | PRD | Impact |
|------|---------|-----|--------|
| 2026-03-21 | F-001: Project Skeleton & Interface Contract | [001](../02_PRD/001-project-skeleton-interface-contract-2026-03-21.md) | Foundation layer complete: 11 threat agent prompts, interface contract, output template, 3 schemas, 3 examples. Unblocks F-002 through F-010. |
| 2026-03-21 | F-003: Orchestrator Agent | [003](../02_PRD/003-orchestrator-agent-2026-03-21.md) | Central orchestrator agent prompt: parses 5 architecture formats, dispatches to STRIDE and AI threat agents via STRIDE-per-Element and keyword matching, assembles structured threats.md output. Unblocks F-003 (STRIDE Agents), F-004 (AI Agents), F-005 (Dedup & Risk Rating), F-009 (Platform Adapters). |
| 2026-03-22 | F-005: STRIDE Threat Agents | [005](../02_PRD/005-stride-threat-agents-2026-03-21.md) | 6 validated STRIDE threat agents (spoofing, tampering, repudiation, info-disclosure, denial-of-service, privilege-escalation) with AI-specific threat patterns, STRIDE-per-Element matrix targeting, and OWASP/CWE/MITRE ATT&CK references. Updated 3 example threat models. Unblocks F-004 (AI Agents). |
| 2026-03-22 | F-010: Deduplication & Risk Rating | [010](../02_PRD/010-deduplication-risk-rating-2026-03-22.md) | Cross-agent finding correlation with 5 deterministic rules (STRIDE-to-AI pairs), deduplicated coverage matrix with three-state cell model, OWASP 3x3 risk calibration matrix in output, schema v1.1. Completes Phase 1 Foundation pipeline. Unblocks F-009 (Platform Adapters). |
| 2026-03-22 | F-012: SARIF Output Generation | [012](../02_PRD/012-sarif-output-generation-2026-03-22.md) | SARIF 2.1.0 output co-generated with threats.md during Phase 4 (Assess). Finding IR mapped to SARIF results with 8 rule definitions (6 STRIDE + 2 AI), CVSS-aligned severity levels, correlated finding representation via relatedLocations, component navigation via logicalLocations, and stable partialFingerprints for cross-run tracking. Enables GitHub Code Scanning, VS Code SARIF Viewer, and Azure DevOps integration. Unblocks F-009 (Platform Adapters — SARIF upload automation). |
| 2026-03-23 | F-015: Threat Report Agent & Attack Trees | [015](../02_PRD/015-threat-report-agent-attack-trees-2026-03-23.md) | Narrative threat report with executive summary, Mermaid attack trees for Critical/High findings, remediation roadmap. Phase 5 (Report) in orchestrator pipeline. |
| 2026-03-23 | F-018: Threat Infographic Agent | [018](../02_PRD/018-threat-infographic-agent-2026-03-23.md) | Visual infographic agent producing threat-infographic-spec.md with risk distribution, coverage heat map, top findings summary, and CVSS color palette. Optional Gemini API image generation. Phase 6 (Infographic) in orchestrator pipeline with opt-out support. Completes the reporting layer: structured analysis (F-005/F-007) -> narrative report (F-015) -> visual communication (F-018). |
| 2026-03-23 | F-024: Example Threat Models | [024](../02_PRD/024-example-threat-models-2026-03-23.md) | 3 end-to-end example threat models (web-app, agentic-app, microservices) with architecture diagrams and complete threat model outputs matching schema v1.1. Demonstrates STRIDE, AI threat agents, and OWASP cross-references. 5 user stories delivered. |
| 2026-03-27 | F-035: Quantitative Risk Scoring | [035](../02_PRD/035-quantitative-risk-scoring-2026-03-27.md) | Four-dimensional quantitative risk scoring (CVSS base + exploitability + scalability + reachability) with composite scores, governance fields, dual output formats (risk-scores.md + risk-scores.sarif). |
| 2026-03-28 | F-036: Compensating Controls Analysis | [036](../02_PRD/036-compensating-controls-2026-03-27.md) | Codebase control detection with file:line evidence, compensating control recommendations, coverage matrix, residual risk calculation, and control effectiveness assessment. |
| 2026-03-28 | F-039: Standalone /tachi.infographic Command | [039](../02_PRD/039-standalone-infographic-command-2026-03-28.md) | Standalone `/tachi.infographic` command with auto-detection of richest data source (risk-scores.md preferred), explicit file path override, template selection. Phase 6 removed from `/tachi.threat-model` pipeline. All platform adapters updated to 5-phase pipeline. |
| 2026-03-28 | F-045: End-to-End tachi Instruction Manual | [045](../02_PRD/045-instruction-manual-2026-03-28.md) | Comprehensive developer guide covering the full 4-command pipeline (`/tachi.threat-model` -> `/tachi.risk-score` -> `/tachi.compensating-controls` -> `/tachi.infographic`). Includes Quick Start (first threat model in 5 minutes), pipeline walkthrough with OpenClaw worked example, output interpretation for all 12+ artifacts, and prompt spec for consistent regeneration. First documentation-focused deliverable after core pipeline completion. |
| 2026-03-28 | F-048: Infographic Tiered Detection & Residual Risk | [048](../02_PRD/048-infographic-tiered-detection-residual-risk-2026-03-28.md) | Three-tier data source auto-detection for `/tachi.infographic` (compensating-controls.md > risk-scores.md > threats.md), residual risk extraction from compensating controls Coverage Matrix, progressive enhancement tips at each pipeline tier, and risk label distinction (Residual Risk / Inherent Risk / Severity) across both infographic templates. Completes the visualization layer of the composable pipeline. |
| 2026-03-28 | F-053: Risk Reduction Funnel | [053](../02_PRD/053-risk-reduction-funnel-2026-03-28.md) | New `risk-funnel` infographic template visualizing progressive risk reduction through the tachi pipeline as a 4-tier vertical funnel (threats identified -> inherent risk scored -> controls applied -> residual risk). Supports graceful degradation: 4-tier from compensating-controls.md, 3-tier from risk-scores.md, 1-tier from threats.md. Metrics sidebar with risk reduction percentage and control coverage. |
| 2026-03-30 | F-067: Deterministic Report Data Extraction | [067](../02_PRD/067-deterministic-report-data-extraction-2026-03-30.md) | Replaced LLM-based markdown parsing with deterministic Python script (scripts/extract-report-data.py) for `/tachi.security-report` data extraction. 3-tier severity source selection, internal consistency validation, scope data extraction. Report-assembler agent updated to invoke script instead of inline parsing. Fixes non-deterministic output where identical inputs produced varying severity counts (0-20 Critical) and risk levels (HIGH/CRITICAL). |
| 2026-03-30 | F-071: Deterministic Infographic Extraction | [071](../02_PRD/071-deterministic-infographic-extraction-2026-03-30.md) | Replaced LLM-based data extraction in threat-infographic agent with deterministic Python script (scripts/extract-infographic-data.py). Shared tachi_parsers.py module extracted from extract-report-data.py. Produces byte-identical JSON output for baseball card, system architecture, and risk funnel templates. Ensures cross-output consistency between infographics and security reports. Completes deterministic extraction across all tachi pipelines. |
| 2026-03-31 | F-075: Tachi Agent Best Practices | [075](../02_PRD/075-tachi-agent-best-practices-2026-03-31.md) | Extracted domain knowledge from 3 methodology agents (orchestrator, risk-scorer, control-analyzer) into on-demand skill files (tachi-orchestration, tachi-risk-scoring, tachi-control-analysis). Audited all 17 tachi agents for Claude 4.6 prompting best practices. Created shared _TACHI_AGENT_BEST_PRACTICES.md with tier caps and compliance table. Trimmed threat-report to 800-line cap. Unblocks #74 (baseline-aware pipeline). |
| 2026-04-01 | F-074: Baseline-Aware Pipeline | [074](../02_PRD/074-baseline-aware-pipeline-2026-03-31.md) | Baseline-aware threat detection pipeline with 4-phase orchestration (carry-forward, isolated discovery, merge/dedup, coverage gate). Correlates findings across runs with stable IDs via SARIF fingerprints. Delta annotations (`[NEW]`, `[UNCHANGED]`, `[UPDATED]`, `[RESOLVED]`) on all outputs. Coverage checklists per STRIDE category. Extended orchestrator, risk-scorer, and control-analyzer agents. New coverage-checklists schema. Updated all output templates and SARIF properties. Unblocks #55 (Security Progression Summary). |
| 2026-04-02 | F-078: Agent Context Optimization | [078](../02_PRD/078-agent-context-optimization-2026-04-01.md) | Restructured 6 tachi agents (orchestrator, risk-scorer, control-analyzer, report-assembler, threat-report, threat-infographic) from monolithic prompts to lean definitions with on-demand skill references. Created 4 skill directories (tachi-orchestration, tachi-risk-scoring, tachi-report-assembly, tachi-shared) with 25+ granular reference files. Added explicit model fields to all 17 agent definitions. Reduced agent prompt sizes by 40-60%. Zero regression on threat model outputs. |
| 2026-04-06 | F-086: Automated Release Tagging via GitHub Actions | [086](../02_PRD/086-automated-release-tagging-via-github-actions-2026-04-06.md) | Google release-please GitHub Action for automated version tagging from conventional commits. Deliverables: release-please.yml workflow, release-please-config.json, .release-please-manifest.json (baseline v4.0.0), README Releases section. Eliminates manual `git tag` commands; maintainer controls release timing via Release PR merge. |
| 2026-04-08 | F-084: MAESTRO Layer Mapping | [084](../02_PRD/084-maestro-layer-mapping-2026-04-07.md) | CSA MAESTRO seven-layer taxonomy overlay for all threat findings. New schema field, shared reference, orchestrator keyword classification in Phase 1, SARIF tags, downstream agent propagation, layer-based risk summary. All 6 example outputs regenerated. 4 user stories delivered. |
| 2026-04-08 | F-104: Downstream Baseline Propagation | [104](../02_PRD/104-downstream-baseline-propagation-2026-04-08.md) | Propagates baseline delta_status downstream through threat-report, infographic, and PDF report pipelines. RESOLVED findings excluded from active counts, NEW findings highlighted, Delta Summary section in reports. Shared parser updated for delta extraction. Completes the baseline story started in F-074. 4 user stories delivered. |
| 2026-04-09 | F-121: Rename Tachi Commands to tachi.* Namespace | [121](../02_PRD/121-rename-tachi-commands-to-namespace-2026-04-09.md) | All 6 tachi pipeline commands renamed to tachi.* dot-namespace (e.g., /threat-model -> /tachi.threat-model). New /tachi.architecture command added. Cross-references updated across 160+ files. Install script handles cleanup of old command files. 5 user stories delivered. |
| 2026-04-09 | F-120: Architecture Lifecycle Command | [120](../02_PRD/120-architecture-lifecycle-command-2026-04-09.md) | Version tracking with YAML frontmatter (version, date, checksum) for architecture files. Archive mechanism preserving previous versions. Threat model snapshot integration copying architecture into output folder. Guided update mode for walking through architecture change categories. 4 user stories delivered. |
| 2026-04-09 | F-112: Attack Path Pages in Security Report PDF | [112](../02_PRD/112-attack-path-pages-in-pdf-2026-04-09.md) | Dedicated attack path visualization pages in PDF security reports. Extraction pipeline parses attack trees, renders Mermaid diagrams to PNG, feeds structured data to Typst template. Conditional inclusion gated by has-attack-trees boolean. Backward compatible with existing reports. 4 user stories delivered. |
| 2026-04-10 | F-128: Executive Threat Architecture Infographic | [128](../02_PRD/128-executive-threat-architecture-2026-04-09.md) | Sixth infographic template (executive-architecture) producing CISO-ready threat architecture visualization with early-page PDF placement immediately after Executive Summary. `all` shorthand and new `exec` alias include the template; graceful handling of threat models with no qualifying findings. 51/51 tasks across 8 phases; 4 user stories delivered. T038 PM usability check deferred with 5-business-day post-merge SLA. |
| 2026-04-10 | F-136: MAESTRO Canonical Layer Correctness Fix | [136](../02_PRD/136-maestro-canonical-layer-correctness-fix-2026-04-10.md) | Correctness fix aligning tachi's MAESTRO seven-layer taxonomy with the canonical CSA Ken Huang reference. Three layers renamed: L5 Security → L5 Evaluation and Observability, L6 Agent Ecosystem → L6 Security and Compliance, L7 User Interface → L7 Agent Ecosystem. Acronym expansion corrected ("Multi-Agent Environment, Security, Threat, Risk, and Outcome"). Schema version bumped 1.2 → 1.3 with documented old → new enum migration path in CHANGELOG. Keyword sets rebalanced: new L5 observability keywords (audit log, monitoring, anomaly detection, SIEM, forensics, telemetry), L6 retains security keywords (auth, WAF, guardrail, RBAC), L7 merges agent-ecosystem and user-facing keywords. Typst template three-way divergence ("Integration Services" L6 bug) corrected. Wave 0 pre-edit grep discovery report committed for audit trail. All six example outputs regenerated; five non-agentic-app baselines byte-deterministic under SOURCE_DATE_EPOCH=1700000000 with test_backward_compatibility.py passing. Observability components (audit loggers, SIEM, anomaly detection) now have a canonical L5 Evaluation and Observability home instead of being misrouted to Security or lost in Unclassified. ADR-020 amended with revision note. 45/45 tasks complete; 8 user stories delivered. Release-please will cut v4.10.0 minor release (feat(136) prefix). |
| 2026-04-11 | F-082: Threat Agent Skill References | [082](../02_PRD/082-threat-agent-skill-references-2026-04-11.md) | Completes the lean-agent architecture for all 17 tachi agents by migrating the remaining 11 threat detection agents (6 STRIDE + 5 AI) from self-contained inline shape to lean + skill references pattern. STRIDE agents reduced from 113-141 lines to 50-54 lines; AI agents reduced from 167-201 lines to 78-114 lines — every agent within FR-10 tier caps (STRIDE ≤120, AI ≤150, hard cap ≤180). 11 new companion skill directories created at `.claude/skills/tachi-<name>/references/` (spoofing, tampering, repudiation, info-disclosure, denial-of-service, privilege-escalation, prompt-injection, data-poisoning, model-theft, tool-abuse, agent-autonomy), each hosting a `detection-patterns.md` reference file loaded via a single `**MANDATORY**: Read` directive at detection start (new "detection variant" of the lean pattern, sibling to the methodology variant used by control-analyzer). Enrichment floor cleared: +30 new pattern categories added across the 11 agents against a ≥22 aggregate floor — +8 margin. Source attribution: OWASP Top 10 2021, OWASP LLM Top 10 2025, OWASP AI Exchange, MITRE ATT&CK v15+, MITRE ATLAS v5.1+ (including Oct 2025 agent techniques AML.T0058-T0062 — context poisoning, memory corruption, agent-in-the-middle, excessive agency runtime, cascading agent failures), CWE Top 25 2024, NIST AI 600-1. New ADR-023 records the sibling detection variant as a second documented lean-agent shape. Shared reference `finding-format-shared.md` gains a "For Threat Agents" producer section (additive-only); OWASP 3×3 risk matrix now lives in exactly one canonical file (`severity-bands-shared.md`, normalized to Unicode ×). T057 live regeneration on agentic-app confirmed +8 new findings (22 baseline → 30). Zero new runtime dependencies (SC-014 — empty diff on `pyproject.toml`, `requirements*.txt`, `package.json`). 68 tasks across 18 waves; 5 user stories delivered. PR #151 merged via squash (commit 6f9a40d). |
| 2026-04-12 | F-141: MAESTRO Phase 2 — Cross-Layer Attack Chain Analysis | [141](../02_PRD/141-maestro-cross-layer-attack-chains-2026-04-12.md) | Cross-layer attack chain correlation engine identifying multi-layer MAESTRO attack paths from threat findings. New attack-chains.md artifact, threat report Section 6 narrative, PDF chain diagram pages, schema additions (attack-chain.yaml), parser additions (tachi_parsers.py), 800+ lines test coverage. Transforms tachi from "STRIDE tool with MAESTRO labels" to "full MAESTRO implementation" with canonical CSA cross-layer deliverables. 6 user stories delivered across 7 implementation waves. |
| 2026-04-15 | F-143: MAESTRO Phase 4 — OWASP AIVSS Evaluation ADR | [143](../02_PRD/143-maestro-aivss-evaluation-adr-2026-04-14.md) | Documentation-only ADR spike evaluating OWASP AIVSS v0.8 against tachi's four-dimensional composite scoring model across three surfaces (dimensions, composite formula weights, severity band thresholds). Decision: **diverge** from AIVSS at present time — tachi's existing `(0.35 × CVSS 3.1) + (0.30 × Exploitability) + (0.20 × Reachability) + (0.15 × Scalability)` composite remains canonical; AIVSS v0.8 documented as a peer agentic-AI scoring framework tachi is aware of and intentionally non-aligned with. New **ADR-024** (`docs/architecture/02_ADRs/ADR-024-owasp-aivss-evaluation.md`, Status: Accepted) delivers the three-surface side-by-side comparison with Overlap/Gap/Conflict/No equivalent row labels, two worked examples quantifying score divergence, explicit *When to Re-Evaluate* clause (AIVSS v1.0 + at least one external adopter case study), and cross-references to ADR-020 (MAESTRO classification), ADR-019 (shared cross-agent definitions), and ADR-018 (baseline-aware scoring lineage). Companion 80–200 word **AIVSS Relationship** section added to `.claude/skills/tachi-risk-scoring/SKILL.md` reflecting the ADR decision (cross-surface consistency verified — both surfaces use "diverge"). **Zero production code changes** — no schemas, scripts, agents, example outputs, or pipeline dependencies modified. Closes umbrella MAESTRO compliance discovery [#136](https://github.com/davidmatousek/tachi/issues/136); Phases 1–3 delivered in F-136, F-141, F-082. Conditional follow-on (T023) N/A — Option C (diverge) chosen, no adopt-path implementation feature filed. 32 tasks complete + 1 N/A (T023 conditionally skipped per decision). 4 user stories delivered. PR #167 squash-merged to main 2026-04-15. |
| 2026-04-17 | F-180: F-A1 Taxonomy Crosswalk Collection | [180](../02_PRD/180-taxonomy-crosswalk-collection-2026-04-17.md) | Foundation data layer for tachi's cross-framework taxonomy integration. New `schemas/taxonomy/` directory with 9 files: 7 catalog YAMLs (owasp ≥60 records, mitre-attack ≥38 techniques, mitre-atlas ≥12 including Oct 2025 agent techniques AML.T0058-T0062, nist-ai-rmf exactly 72 Subcategories per FR-021 amendment from PRD-era 68 to authoritative count, cwe ≥53 including CWE Top 25 2025, tachi-control-category exactly 8, tachi-stride-ai-category exactly 11) + crosswalk.yaml (526 primary edges, exceeding ≥500 Tier 1 floor from PRD Risk R3) + README.md with runnable Python snippet, per-framework provenance, 3-level confidence calibration rubric with anti-drift rule, and "What F-A1 does NOT give you today" subsection. New **ADR-027** (Accepted) records per-item record shape, per-edge record shape, 7-value taxonomy enum, 3-value edge_type enum (primary/related/superseded), 3-value confidence enum (high/medium/low), and scope-exception rationale. New integrity test suite `tests/schemas/test_taxonomy_integrity.py` (4+1 tests) enforces FR-027 through FR-032 — referential integrity, closed-enum membership, citation shape validation. FR-022 NIST AI RMF Surface B verbatim transcription complete (27 edges from `nist-ai-rmf-mapping.md` Surface B); Surface C (GAI Risk → STRIDE+AI, 15 edges) deferred to F-A1.1 follow-on per architect Option (c) decision — Surface C target taxonomy (NIST AI 600-1 §2.X GAI Risks) is structurally distinct from AI RMF Subcategories and requires 8th taxonomy enum value + new `nist-ai-600-1.yaml` catalog. **Zero production code changes** — no modifications to `scripts/`, `.claude/agents/`, `.claude/skills/`, `.claude/commands/`, `templates/`, or existing `schemas/*.yaml` (except additive 2 cross-reference links to top-level `README.md` and `docs/architecture/00_Tech_Stack/README.md` per FR-038). Zero new runtime dependencies (pyyaml already in requirements-dev.txt). 3 follow-on Issues filed: F-A1.1 (Surface C), F-A1.2 (related/superseded edges), F-A1.3 (citation link-rot). Unblocks F-A2 (source_attribution schema field) and F-B (coverage attestation report section). 41/41 tasks complete across 5-day build structure; 5 user stories delivered. Governance: PM + Architect + Team-Lead sign-off (all APPROVED_WITH_CONCERNS with resolved concerns inline). Two PM sign-off amendments during feature execution: (1) FR-021 count amendment 68→72 under FR-024 primary-source-correction discipline, (2) Surface C Option (c) scope-narrow. PR #181 squash-merged to main 2026-04-17 (commit 8b7c7bf). |
| 2026-04-17 | F-189: F-A2 Source Attribution Schema Extension | [189](../02_PRD/189-source-attribution-schema-extension-2026-04-17.md) | **2nd of 11 features in BLP-01 multi-taxonomy coverage initiative (Foundation tier: F-A1 → F-A2 → F-A3)** — delivered same day as F-A1 (Feature 180) foundation data layer. Finding-side bridge closing the one-sided structural gap F-A1 left: tachi now has both a machine-readable framework vocabulary (F-A1's 7 catalog YAMLs + 526-edge crosswalk) AND machine-readable finding-to-framework citations (F-A2's `source_attribution` field), making coverage claims aggregable as data rather than free-text prose. `schemas/finding.yaml` minor bump 1.4 → 1.5 per ADR-026 additive-field rule; new optional `source_attribution` list-of-RECORD field with per-record shape `{taxonomy, id, relationship}`. Closed 5-value `taxonomy` enum `{owasp, mitre-attack, mitre-atlas, nist-ai-rmf, cwe}` — deliberately excludes the 2 tachi-internal taxonomies (`tachi-control-category`, `tachi-stride-ai-category`) from F-A1 because they are internal vocabulary, not external frameworks tachi claims coverage of. Closed 3-value `relationship` enum `{primary, related, derived}` with `primary` default. Parser round-trip in `scripts/tachi_parsers.py::parse_threats_findings` preserves conditional-key semantics: field omitted when absent from source (NOT defaulted to empty array — preserves Feature 104 `delta_status` precedent). Two-tier validation architecture: V1/V2/V3/V5 enum validation at parse time (unknown taxonomy, unknown relationship, malformed record shape, duplicate taxonomy-id pairs); V4 referential-integrity validation as separate callable phase (Phase 4 Assess caller) resolving each `{taxonomy, id}` against the F-A1 catalog YAMLs — separation preserves parser purity and enables downstream consumers to defer referential checking. New **ADR-028** (`docs/architecture/02_ADRs/ADR-028-source-attribution-schema-extension.md`, Status: Accepted) records the additive-optional-field decision under the ADR-026 minor-bump rule (naming this the "first list-of-RECORD precedent" — extends ADR-026's enum-typed-field precedent), the serialization-surface choice (new conditional Section 9 YAML block gated by `has-source-attribution` boolean, mirroring Feature 141 `has-attack-chains` precedent), the 5-value taxonomy enum scope restriction rationale, and the V4 separate-phase validation architecture. **Zero edits to the 22-file detection tier** (11 STRIDE+AI threat agents + 11 companion skill-reference files) — preserves the zero-edit invariant established in Feature 082 under ADR-023. **Zero new runtime dependencies** (empty diff on `pyproject.toml`, `requirements*.txt`, `package.json`). **Byte-identity SC-2 regression gate green**: all 5 non-agentic example PDF baselines regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — F-A2 is contract-only, findings in existing example threat models carry no `source_attribution` and serialize identically pre/post-F-A2. 36/36 tasks complete across 3-day build structure (2026-04-20 Monday → 2026-04-22 Wednesday, spanning a weekend gap from 2026-04-18 Saturday PRD approval). Governance: PM APPROVED_WITH_CONCERNS + Architect APPROVED + Team-Lead APPROVED_WITH_CONCERNS. 3 user stories delivered (US-189-1 multi-framework citation, US-189-2 parser round-trip backward compatibility, US-189-3 closed-enum relationship with referential integrity). Unblocks downstream **F-A3** (threat-detection agent populators — threat agents choosing to cite IDs during detection) and **F-B** (coverage attestation report section — aggregates `source_attribution` across findings to produce per-framework coverage percentages). PR #190 squash-merged to main 2026-04-17 (commit `6d5d890`). |
| 2026-04-18 | F-194: F-B Coverage Attestation Report Section | [194](../02_PRD/194-coverage-attestation-report-section-2026-04-18.md) | **3rd delivered feature in BLP-01 multi-taxonomy coverage initiative — first downstream consumer of the Foundation tier (F-A1 + F-A2).** Adopter-facing renderer tier: the demand-side feature that justifies the F-A1 + F-A2 foundation investment by giving adopters a visible product surface that reads from it. New conditional **Section 10 (Coverage Attestation)** in the PDF security report, composed of two views: (1) per-finding attribution table with columns `Finding ID | Title | Severity | OWASP refs | MITRE refs | NIST refs | CWE refs` where `relationship: primary` citations render bold vs `related`/`derived` plain (WCAG AA color-blind accessible — bold vs plain weight, not color alone), MITRE column grouping ATT&CK and ATLAS with per-ref prefix (`ATT&CK:T1070.001` vs `ATLAS:AML.T0051`); (2) aggregate per-framework coverage matrix with one page per external framework (OWASP, MITRE ATT&CK, MITRE ATLAS, NIST AI RMF, CWE — all 5 rendered when gate true, including 0% coverage pages per Q4 render-all-5 resolution), each item classified Covered (≥1 `primary` attribution) / Partial (zero `primary` AND ≥1 `related`/`derived`) / Gap (zero attributions) per ADR-029 Q1-A rule, coverage percentage = `K / N` where `N = len(yaml.safe_load(schemas/taxonomy/{framework}.yaml))` computed once per framework at extraction time per ADR-029 Q2-A denominator-authority convention, Partial count rendered alongside with equal visual weight (`Covered: 12/38 = 31.6% · Partial: 3 · Gap: 23`), Gap items highlighted with color + icon distinction. New Typst template `templates/tachi/security-report/coverage-attestation.typ` + aggregator helpers `compute_has_source_attribution` / `classify_finding_attributions` / `build_per_framework_aggregate` / `build_per_finding_row` in `scripts/extract-report-data.py`. Gated by new **`has-source-attribution`** boolean in the Typst data contract — emits `true` iff ≥1 finding carries a non-empty `source_attribution` array, `false` otherwise (no implicit defaults). `main.typ` §2b defaults block carries a default-value guard mirroring `has-maestro-data` / `has-attack-trees` / `has-attack-chains` precedent, preventing "variable not found" compile errors on stale `report-data.typ` files; the `#if has-source-attribution and per-finding-rows.len() > 0 { ... }` conditional block is a belt-and-suspenders gate omitting the entire section (no page break, no section header, no divider, no empty table) when either predicate is false. New **ADR-029** (`docs/architecture/02_ADRs/ADR-029-coverage-attestation-report-section.md`, Status: Accepted, committed under the Feature 180 / 189 Proposed → Accepted dual-commit governance pattern) records the new-Typst-page + aggregator + `has-source-attribution` boolean architectural pattern (cross-referenced to Feature 141 `has-attack-chains` precedent and Feature 128 new-Typst-page precedent), the 3-value classification with Q1-A rule, the Q2-A denominator-authority convention, the **zero-crosswalk-JOIN scope line** (F-B matrix is intra-framework only; cross-framework JOIN via `schemas/taxonomy/crosswalk.yaml` deferred to a follow-on feature), the Out-of-Scope-as-4th-classification deferral per Q6-D (MVP classification is 3-value — all non-cited items classify as Gap), the **22-file zero-edit invariant preservation** per ADR-023 / ADR-028 lineage (11 STRIDE+AI threat agents + 11 companion `detection-patterns.md` skill-reference files unmodified), the Partial disclosure rule, and the exclusion of the 2 tachi-internal taxonomies (`tachi-control-category`, `tachi-stride-ai-category`) from the matrix per FR-018. **Byte-identity SC-002 regression gate green**: all 5 non-agentic example PDF baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — F-B is a no-op PDF-wise on any architecture whose findings carry no attributions (the default state of every fresh pipeline run until F-A3 populators ship). **Zero new runtime dependencies** (empty diff on `pyproject.toml`, `requirements*.txt`, `package.json`). **Zero schema changes** — `schemas/finding.yaml` and `schemas/taxonomy/*.yaml` not modified; F-B is purely a renderer + aggregator over already-landed F-A2 (schema 1.5) contract. **Zero runtime touch on F-A3 upstream**: F-B reads whatever `source_attribution` findings carry; populator wiring is strictly F-A3 scope. 45/46 tasks complete (T044 post-merge SHA fill deliberately pending for direct commit to main after `/aod.deliver`). 3 user stories delivered (US-194-1 per-finding attribution table, US-194-2 aggregate per-framework coverage matrix, US-194-3 conditional inclusion backward compatibility). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS. Unblocks demand-visible downstream work: **F-A3** (threat-detection agent populators will activate F-B Section 10 rendering on real engagements as soon as populators ship), **coverage infographic templates** (Feature 128-style JPEG rendering the coverage matrix visually — follow-on), **cross-framework JOIN via crosswalk.yaml** (composite coverage claims spanning multiple frameworks — follow-on), **SARIF `source_attribution` propagation** (SARIF exporter currently treats `source_attribution` as post-F-A2 absent — follow-on), and **time-series coverage delta** (Feature 104 baseline-delta pattern applied to coverage). Closes the 3rd of 11 BLP-01 features; remaining 8 features (F-A3 populators + downstream consumers) deferred to next iteration. PR #195 squash-merged to main 2026-04-18 (commit `c4b8dc6`). |
| 2026-04-24 | F-206: F-2 Misinformation Threat Agent (OWASP LLM09:2025) | [206](../02_PRD/206-misinformation-threat-agent-2026-04-23.md) | **Second Tier 1 detection agent after F-1 (Feature 201), 2nd populator of the F-A2 `source_attribution` contract, and first Coverage Matrix Planned → Covered transition on LLM09 Misinformation.** New AI-tier `misinformation` agent closes the OWASP LLM09:2025 factual-integrity threat surface — the asymmetric gap where tachi previously shipped comprehensive input-side coverage (`prompt-injection` / LLM01) and output-side sanitization coverage (`output-integrity` / LLM05) but was silent on ungrounded-factual-emission / citation-fabrication / overreliance-missing-HITL / retrieval-grounding-gap / confidence-calibration-absence. Agent emits `MI-{N}` findings with `category: llm` and populated `source_attribution` citing OWASP LLM09:2025 primary plus `CWE-345 Insufficient Verification of Data Authenticity` related (primary CWE mapping for misinformation and citation fabrication) and `CWE-223 Omission of Security-relevant Information` related (applicable to missing disclosure / grounding labels) — both catalog-verified present in `schemas/taxonomy/cwe.yaml`. **Heuristic A three-signal-class discipline operationalized** per ADR-031 Decision 2 cross-referencing ADR-030 Decision 1: F-2 owns factual-integrity signal class (grounding / verification / HITL / calibration — human-victim and decision-cascade-victim, factual-content primitives); F-1 owns encoding/sanitization (ADR-030 Decision 1 — machine-victim, bytes/strings/syntax primitives); `prompt-injection` owns input-injection (machine-attacker, input primitives). ADR-031 explicitly resolves three non-subsumption boundaries: (a) misinformation not subsumed into `prompt-injection` (distinct signal class — attacker-controlled input vs. architecture-level grounding absence), (b) not subsumed into `output-integrity` (F-1 scope is downstream-execution-sanitization, not factual-integrity), (c) not deferred to a post-Tier-1 feature. **Schema bump `schemas/finding.yaml` 1.6 → 1.7** (minor, additive) per ADR-031 Decision 8 — second application of the ADR-030 Decision 8 "ADR-026 Complex-Shape Clarifier extended to regex-alternation prefix additions" rule. `id.pattern` extended from `^(S|T|R|I|D|E|AG|LLM|AGP|OI)-\d+$` to `^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI)-\d+$` — 10 pre-existing prefixes preserved verbatim; `MI` appended as 11th alternation value. New **ADR-031** (`docs/architecture/02_ADRs/ADR-031-misinformation-agent.md`, Status: Accepted) records 8 numbered decisions covering new-agent adoption, Heuristic A three-signal-class partition (signal-class distinctness at the vocabulary / CWE / mitigation-text layer), lean-agent ADR-023 detection-variant conformance (single-point load, ≤150 soft cap, zero MAESTRO), 24-file zero-edit invariant preservation (12 STRIDE+AI detection agents after F-1 + 12 companion `detection-patterns.md` files), Proposed → Accepted dual-commit governance protocol with post-merge SHA fill (5th consecutive per ADR-027/028/029/030/031 lineage), deliberate CWE-1039 exclusion rationale, `category: llm` enum reuse, and regex-alternation Complex-Shape Clarifier extension (2nd application). New agent `.claude/agents/tachi/misinformation.md` (120 lines, within ADR-023 ≤150 soft cap) + new companion skill `.claude/skills/tachi-misinformation/` with `README.md` + `references/detection-patterns.md` carrying 5 numbered pattern categories (Ungrounded Factual Emission / Citation Fabrication / Overreliance-Missing-HITL / Retrieval-Grounding Gap / Confidence-Calibration Absence) each with ≥5 indicators + ≥1 worked example + primary/related citations + trigger keywords + DFD element types. Mitigation text names specific grounding / verification / HITL / calibration mechanisms — `mandatory RAG grounding with per-claim source attribution`, `retrieval-strength metric declaration (hit-rate, recall@k)`, `confidence-calibration layer (temperature scaling + ECE monitor)`, `output-time citation verification against retrieved source URIs`, `strict citation-token constraint on decoder output`, `human-in-the-loop review queue on decision-critical output` (consumer-facing-HIGH/CRITICAL vs internal-MEDIUM distinction), `per-query retrieval-score threshold`, `versioned retrieval corpus with staleness policy`, `refusal pattern for low-confidence queries`, `calibrated-confidence exposure on output` — no generic "verify output" prose. **Quintet-reconciliation (F-1 carry-over fix)**: 3 F-1 carry-over callsites reconciled from pre-F-1 LLM trio to post-F-2 LLM quintet in a single edit per MEDIUM-3 / MEDIUM-4 architect-owned fix (`.claude/skills/tachi-orchestration/references/dispatch-rules.md:120` LLM list + table row, `.claude/agents/tachi/orchestrator.md:296` sequential-mode text, `.claude/agents/tachi/orchestrator.md:370` LLM Threats row) — dispatch consistency restored across all 5 callsites. `.claude/skills/tachi-shared/references/finding-format-shared.md` `consumers:` frontmatter list gains `- misinformation` (additive-only per ADR-023 Decision 3, ZERO `## ` heading changes — second additive-consumer-list insert after F-1). **24-file zero-edit invariant preserved** (ADR-023 lineage extended from F-1's 22-file to F-2's 24-file state after F-1 landed +2 files): zero modification edits to the 12 STRIDE+AI detection agents + 12 companion `detection-patterns.md` files, zero edits to 5 infrastructure-tier consumers (risk-scorer / control-analyzer / threat-report / threat-infographic / report-assembler) — verified via `git diff --diff-filter=M` grep-auditable enumeration. Test-harness fix bundled: `tests/scripts/test_backward_compatibility.py` `DETECTION_AGENT_PATHS` bump (11 → 12) + `--diff-filter=M` scope on the zero-edit invariant check to distinguish modifications from additions (F-2 adds a 13th agent + 13th patterns file as new; the 24 pre-existing files remain byte-identical). **F-A2 `source_attribution` 2nd populator** — F-2 is the 2nd net-new threat agent (after F-1) that emits non-absent `source_attribution` on every finding; all `MI-{N}` findings cite `{taxonomy: owasp, id: LLM09, relationship: primary}` plus `{taxonomy: cwe, id: CWE-345, relationship: related}` and/or `{taxonomy: cwe, id: CWE-223, relationship: related}` from the catalog-present set. **`AML.T0042 Verify Attack` prose-only citation rationale**: `schemas/taxonomy/mitre-atlas.yaml` carries 12 AML techniques (T0010, T0018, T0020, T0024, T0051, T0054, T0057, T0058, T0059, T0060, T0061, T0062); T0042 confirmed absent at PRD time (FR-5 verification), so pattern-catalog prose retains `AML.T0042 Verify Attack` for adversarial-grounding context but `source_attribution` anchors only on LLM09 + applicable CWE IDs. Catalog upgrade deferred to a future feature. **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. **Byte-identity regression gate green**: all 5 non-factual example PDF baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — two-part emission gate (FR-011) guarantees zero `MI-{N}` findings on these (no factual-output components), which guarantees unchanged `threats.md` / `risk-scores.md` / `compensating-controls.md` content, which guarantees byte-identical PDFs. `agentic-app` example regenerated as the Q4 PM-leaning target (extended with a Clinical Advisory Sub-Agent) and produced **3 MI findings** covering Categories 1 (Ungrounded Factual Emission), 3 (Overreliance/Missing HITL), and 4 (Retrieval-Grounding Gap) simultaneously — one realistic factual-output surface exercised multiple failure modes, validating the three-signal-class discipline more thoroughly than a single-category example. Extended architecture also preserved F-1's `OI-4` output-integrity finding and all 5 existing `LLM-{N}` findings, producing a natural three-adjacent-finding rendering (`LLM-{N}` / `OI-{N}` / `MI-{N}`) on the same LLM Process — correct three-signal-class coexistence, not duplication. 62/62 tasks complete across 6 waves over 2 working days (Thu 2026-04-23 18:25 → Fri 2026-04-24 09:37) — 3–4 days ahead of the PRD 2026-04-27 → 2026-04-29 envelope and ~30% under the 16h active-work estimate; buffer untouched (R2 agentic-app cumulative-state risk not fired). 3 user stories delivered (US-206-1 ungrounded-factual-output detection, US-206-2 named grounding/verification mitigation guidance, US-206-3 factual-integrity as first-class distinct threat class). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-23) — all fixes applied inline before sign-off. PR #207 squash-merged to main 2026-04-24 (commit `b703e52`). |
| 2026-04-19 | F-201: F-1 Output Integrity Threat Agent (OWASP LLM05:2025) | [201](../02_PRD/201-output-integrity-threat-agent-2026-04-18.md) | **First net-new threat detection agent shipped under BLP-01 Tier 1 governance** and **first downstream consumer of the F-A1 + F-A2 + F-B Foundation tier** (Features 180 / 189 / 194). New AI-tier `output-integrity` agent closes the OWASP LLM05:2025 output-handling threat surface — the asymmetric gap where tachi previously shipped comprehensive input-side coverage (prompt-injection) but was silent on the output side (XSS/DOM, SQLi, command injection, SSRF, template injection, path traversal from LLM-generated output flowing into downstream execution sinks). Agent emits `OI-{N}` findings with `category: llm` and populated `source_attribution` citing OWASP LLM05:2025 primary plus relevant CWE related (CWE-79, CWE-89, CWE-78, CWE-94, CWE-918, CWE-22). **F-1 is the first populator of non-absent `source_attribution` arrays** — all prior features shipped the contract (F-A2) or downstream consumers (F-B) but no detection-tier producer. Heuristic A (ASI09:2026 human-victim scope resolution) resolved **Outcome B (split)** per ADR-030 Decision 2: F-1 owns encoding/sanitization signal class (bytes / strings / syntax primitives on machine-victim output handling); F-4 `trust-exploitation` (future feature per BLP-01 §8) reserved for psychology/linguistics signal class (manipulative tone, fabricated authority, absence of uncertainty disclaimers). **Schema bump `schemas/finding.yaml` 1.5 → 1.6** (minor, additive) per ADR-030 Decision 8 "ADR-026 Complex-Shape Clarifier extended to regex-alternation prefix additions" — first such extension in tachi's schema evolution. `id.pattern` extended from `^(S|T|R|I|D|E|AG|LLM|AGP)-\d+$` to `^(S|T|R|I|D|E|AG|LLM|AGP|OI)-\d+$` — 9 pre-existing prefixes preserved verbatim; `OI` appended as 10th alternation value. New **ADR-030** (`docs/architecture/02_ADRs/ADR-030-output-integrity-agent.md`, Status: Accepted 2026-04-18, dual-commit lifecycle per ADR-027/028/029 precedent) records 8 numbered decisions covering new-agent adoption, Heuristic A Outcome B split scope, lean-agent ADR-023 detection-variant conformance (single-point load, ≤150 soft cap, zero MAESTRO), LLM05 + ML09:2023 documentation-only bundling per BLP-01 §4, 22-file zero-edit invariant preservation (11 STRIDE+AI detection agents + 11 companion `detection-patterns.md` files), Proposed → Accepted dual-commit governance protocol with post-merge SHA fill, `category: llm` enum reuse, and Complex-Shape Clarifier regex-alternation extension. New agent `.claude/agents/tachi/output-integrity.md` (120 lines, within ADR-023 ≤150 soft cap) + new companion skill `.claude/skills/tachi-output-integrity/` with `README.md` + `references/detection-patterns.md` (151 lines) carrying 5 numbered pattern categories (Client-Side Execution Sinks / Server-Side Execution Sinks / SSRF / Template Injection / Path Traversal) each with 5 indicators + ≥1 worked example + primary/related citations + trigger keywords + DFD element types. Mitigation text names specific encodings/libraries/patterns — `textContent` (not `innerHTML`), React `{value}` (not `dangerouslySetInnerHTML`), Django auto-escape `{{ value }}` (not `|safe`), CSP `script-src 'self' 'nonce-<nonce>'`, SQLAlchemy `text(sql).bindparams()`, psycopg2 parametrized `cursor.execute(sql, params)`, `subprocess.run([cmd, arg1, arg2], shell=False)`, URL allowlist + egress firewall blocking `169.254.169.254`, Jinja2 `SandboxedEnvironment`, `pathlib.Path(filename).resolve().is_relative_to(allowlisted_output_dir)` — no generic "sanitize output" prose (T021 Wave 2 specificity check green). Orchestrator-tier registration (additive-only per ADR-023 Decision 3 byte-identity preserved): `.claude/agents/tachi/orchestrator.md` AI-tier dispatch list gains `- output-integrity.md` after `tool-abuse.md`; `.claude/skills/tachi-orchestration/references/dispatch-rules.md` LLM trio (`prompt-injection` / `data-poisoning` / `model-theft`) extended to quartet with `output-integrity` plus new **FR-011 two-part emission gate rule** (keyword match AND ≥1 output data-flow into a component performing execution — both required; keyword-only match emits zero findings to prevent false positives); `.claude/skills/tachi-shared/references/finding-format-shared.md` `consumers:` frontmatter list gains `- output-integrity` between `- tool-abuse` and `- risk-scorer` (ZERO `## ` heading changes). **22-file zero-edit invariant preserved** (ADR-023 lineage / SC-009 / T048): zero edits to the 11 STRIDE+AI detection agents, zero edits to their 11 companion `detection-patterns.md` files, zero edits to 5 infrastructure-tier consumers (risk-scorer / control-analyzer / threat-report / threat-infographic / report-assembler) — verified via grep-auditable enumeration. **F-A2 `source_attribution` contract first populator** — F-1 is the first net-new threat agent that emits non-absent `source_attribution` on every finding; all 3 agent Example Findings (OI-1/OI-2/OI-3) and all 5 detection-patterns.md category worked examples cite `{taxonomy: owasp, id: LLM05, relationship: primary}` plus ≥1 `{taxonomy: cwe, id: CWE-*, relationship: related}` from the F-A1 catalog-present set `{CWE-22, CWE-78, CWE-79, CWE-89, CWE-94, CWE-918}`. **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. Backward-compat suite: 13/13 pass + 1 skipped (mermaid-agentic-app excluded per SC-003 narrowed interpretation) under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — 5 non-agentic baselines plus maestro-reference all byte-identical; zero-edit invariant + feature-142 backward-compat pattern defaults checks pass. New tests and fixtures: `tests/scripts/test_output_integrity.py` (27 tests), `tests/scripts/fixtures/output_integrity/valid_oi_finding.yaml` (F-A2 happy path), `tests/scripts/fixtures/output_integrity/invalid_attribution_finding.yaml` (F-A2 V4 referential-integrity rejection path). **Strategic significance — BLP-01 Tier 1 first-wave feature**: F-1 is the first of the BLP-01 Tier 1 cohort per `_internal/strategy/BLP-01.md` §8. With F-A1 / F-A2 / F-B (Features 180/189/194) delivering the Foundation tier machine-readable plumbing, F-1 is the first proof that the contract holds end-to-end against a live detection-tier populator. The three-layer chain now has producers (F-1 emits `source_attribution`), storage (F-A2 schema 1.6), and consumers (F-B coverage-attestation). Follow-on Tier 1 features in the sequence include F-2 (rag-poisoning expansion), F-3 (memory-corruption detection), F-4 (trust-exploitation — inherits the human-victim signal class reserved here at ADR-030 D2), F-5 (workflow-bypass detection). Extension of the ADR-026 Complex-Shape Clarifier to regex-alternation (D8) sets durable precedent for future BLP-01 agent families that introduce new ID prefixes. 55/55 tasks complete. 3 user stories delivered (US-201-1 LLM-output-to-downstream-sink detection, US-201-2 stack-specific mitigation guidance, US-201-3 Heuristic A resolution for ASI09 scope). Governance: PM + Architect + Team-Lead all APPROVED_WITH_CONCERNS on tasks.md (2026-04-18). PR #202 squash-merged to main 2026-04-19 (commit `558e75e`). |
| 2026-04-26 | F-219: F-3 ASI07 Tool-Abuse Agent Enrichment (OWASP ASI07:2026) | [219](../02_PRD/219-asi07-tool-abuse-enrichment-2026-04-25.md) | **Third Tier 1 detection feature in BLP-01 (after F-1 LLM05 → output-integrity, F-2 LLM09 → misinformation), first execution of the Heuristic A enrichment branch (vs. new-agent branch), and the first BLP-01 detection feature with zero schema bump and zero functional orchestrator-tier touches.** Closes the OWASP ASI07:2026 Insecure Inter-Agent Communication threat surface — the inter-agent / MCP-to-MCP gap where tachi previously shipped comprehensive single-agent tool-dispatch coverage (Categories 1-4) and MCP server poisoning coverage (Categories 5-8) but was silent on inter-agent channels and MCP-to-MCP trust propagation. **Heuristic A enrichment-branch architectural choice** per ADR-032 Decision 1: signal-class identity with existing `tool-abuse` coverage (message flow between agent-or-tool endpoints) made enriching the existing host the correct path vs. spawning a new `asi07-inter-agent-communication` agent; consolidation prevents Agentic-category section fragmentation in `threat-report.md` and preserves single-namespace `AG-{N}` ID continuity. **5/5-dimension reduction vs. F-2 (load-bearing reuse signal for downstream Tier 2 features F-6 ML / F-7 Mobile)**: zero new agent file (vs. F-2's `misinformation.md` +150 lines), zero new skill directory (vs. F-2's `tachi-misinformation/` +400 lines across 3 files), zero schema bump (vs. F-2's 1.6 → 1.7 with `MI` prefix added — F-3 reuses existing `AG` family from F-Foundation; first BLP-01 feature with zero schema bump), zero consumers-list edit (vs. F-2's `+ misinformation` insert in `finding-format-shared.md`), zero functional orchestrator/dispatch edit (vs. F-2's dispatch-rules entry — F-3 only does cosmetic Q2 annotation `tool-abuse (MCP-03)` → `tool-abuse (MCP-03, ASI-07)` per architect adjudication, ~30s edit). **Three additive edits to `tool-abuse.md` only** (98 → 100 lines, well under 150 cap): metadata `owasp_references += ASI-07` (now `[ASI-02, ASI-04, MCP-03, MCP-05, LLM06:2025, ASI-07]`); `## Purpose` extended with 1-line A2A/MCP-to-MCP surface naming; Detection Workflow Step 5 references list extended with `ASI-07, MITRE ATLAS AML.T0060, CWE-287, CWE-345`. **Two net-new Pattern Categories appended to `.claude/skills/tachi-tool-abuse/references/detection-patterns.md` after Category 8**: Category 9 "Insecure Inter-Agent Communication (A2A)" with ≥4 indicators (≥2 agents connected by communication channel, absence of mutual authentication, absence of message signing, absence of replay protection, agent-in-the-middle relay without taint propagation), worked example, `OWASP ASI07:2026` primary + `CWE-287` + `MITRE ATLAS AML.T0060` related, named mitigations (mTLS, HMAC message signing with per-call nonce, nonce-based replay prevention, taint propagation, per-channel mutual authentication); Category 10 "MCP-to-MCP Trust Propagation" with ≥4 indicators (multi-hop MCP trust chain, absence of per-hop attestation, transitive authority assumption gaps, absence of trust-chain validator, undeclared cross-MCP supply-chain assumptions), worked example, `OWASP ASI07:2026` primary + `CWE-345` + `OWASP LLM03:2025` related (inheriting supply-chain vocabulary from existing Category 6), named mitigations (per-hop MCP attestation, signed-capability handoff, MCP-trust-chain validator, supply-chain trust-chain enforcement, taint propagation across MCP hops). New **Pattern Category Disambiguation subsection** (formalized as ADR-032 Decision 7) explicitly carves Categories 1-8 (single-agent tool-abuse) vs. Categories 9-10 (cross-agent trust propagation) and Category 6 (upstream ingestion at registration time) vs. Category 10 (runtime trust propagation between already-registered MCP servers at invocation time) — both findings emit on architectures exhibiting both signals; not duplicates and MUST NOT be merged. Categories 1-8 + `## Overview` + `## Targeted DFD Element Types` + `## Trigger Keywords` sections preserved **byte-identical** pre/post edit per ADR-023 Decision 3 additive-only invariant (SC-006 BLOCKER satisfied via grep-checkable structural diff). New **ADR-032** (`docs/architecture/02_ADRs/ADR-032-asi07-tool-abuse-enrichment.md`, Status: Accepted at PR #220 merge per Proposed → Accepted dual-commit lifecycle; 6th consecutive ADR per ADR-027/028/029/030/031/032 lineage) records 7 numbered Decisions covering D1 enrichment-vs-new-agent (signal-class identity rationale), D2 additive-only edits (3 anchor points: metadata `owasp_references`, `## Purpose`, Detection Workflow Step 5), D3 no-schema-bump asymmetry to ADR-031 D8 (no new `id.pattern` prefix; reuses `AG` family), D4 zero consumer edits (24-file invariant preserved per ADR-023 lineage), D5 zero functional orchestrator edit (cosmetic Q2 annotation only), D6 public-only governance (no internal docs changed; no Layer 2 / tachi Cloud / commercial framing per Option C governance contract), D7 Pattern Category Disambiguation. **24-file zero-edit invariant preserved** (ADR-023 lineage extended through F-3): zero modification edits to the 12 other STRIDE+AI detection agents (`spoofing`, `tampering`, `repudiation`, `info-disclosure`, `denial-of-service`, `privilege-escalation`, `prompt-injection`, `data-poisoning`, `model-theft`, `agent-autonomy`, `output-integrity`, `misinformation`) + 12 companion `detection-patterns.md` files, zero edits to 5 infrastructure-tier consumers (risk-scorer / control-analyzer / threat-report / threat-infographic / report-assembler) — verified via `git diff --diff-filter=M` grep-auditable enumeration. F-3 modifies only `tool-abuse.md` + companion `detection-patterns.md` (the two host files). **F-A2 `source_attribution` 3rd populator and 1st enrichment-of-existing-populator** — F-3 is the 3rd net-new producer flow of `source_attribution` (after F-1 OI / F-2 MI) and the **first enrichment** of an existing populator: the `AG-{N}` ID space already populated through Categories 1-8, F-3 extended it without renumbering. Validator exercised against Cat-9 valid fixture (`owasp:ASI07` primary + `cwe:CWE-287` related + optional `atlas:AML.T0060` related), Cat-10 valid fixture (`owasp:ASI07` primary + `cwe:CWE-345` related + optional `owasp:LLM03` related), and negative fixture (Cat-9/10 finding citing absent `cwe:CWE-99999` correctly rejected). Three independent populators (F-1 OI / F-2 MI / F-3 AG enrichment) now validate the F-A2 referential-integrity contract across both new-agent and enrichment branches. **Wave 3 pipeline regen on `examples/agentic-app/`**: AG-8 [NEW] surfaced as the Insecure Inter-Agent Communication finding on Inter-Agent Communication Channel component, citing `OWASP ASI07:2026 + CWE-287 + MITRE ATLAS AML.T0060`. **Cohesive Agentic-category rendering verified**: AG-8 [NEW] from Category 9 renders adjacent with Categories 1-8 findings (AG-1 through AG-7) in `threat-report.md` §3.7 "Agentic Threats (AG-1 through AG-8)" — single Agentic-category section (no fragmentation), single-namespace `AG-{N}` ID continuity preserved (SC-019 satisfied). **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. **Byte-identity SC-010 BLOCKER green**: 5/5 non-multi-agent baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — multi-agent topology gate per FR-011 guarantees zero Category-9/10 emissions on these architectures (no inter-agent channel, no MCP-to-MCP relay), which guarantees unchanged downstream artifacts. **Strategic significance — Heuristic A enrichment-branch first execution and BLP-01 Tier 1 third feature**: F-3 establishes the precedent for F-6 (Tier 2 ML attacks) and F-7 (Tier 2 Mobile attacks) where target taxonomies may also map cleanly to existing detection agents. The 5/5-dimension reduction is now a documented cost-of-feature metric: features satisfying all 5 reductions are 1-day envelope candidates; features needing >2 dimensions are 2-day envelope (F-2 baseline). For F-6/F-7, the architect adjudicates new-agent vs. enrichment-branch at SDR time per signal-class identity check. ADR-032 D3 explicit asymmetry to ADR-031 D8 sets durable precedent for future BLP-01 enrichment features that introduce no new ID prefixes. **BLP-01 Coverage Matrix transition**: ASI07:2026 transitions Planned → Covered with F-3 (Feature 219) named as the closure feature; OWASP Agentic Top 10:2026 framework row coverage advances to 6/10 (ASI-01, ASI-02, ASI-04, MCP-03, MCP-05, ASI-07 covered; ASI09 reserved for F-4, post-Tier-1 features track remaining 4). 63/67 tasks complete (4 intentional post-merge: T031 SHA fill, T062 internal BLP-01 update, T063 release-please verification, T064 contingent buffer). 3 user stories delivered (US-219-1 inter-agent channel detection A2A, US-219-2 MCP-to-MCP trust propagation detection, US-219-3 cohesive Agentic-category rendering with Heuristic A enrichment pattern). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-25). 1-day envelope held: build completed in <24 hours of clock time (Sat 2026-04-25 → Sun 2026-04-26 ~01:14 EDT) with retrospective filed Day 2 AM per PRD HIGH-1 buffer-day budget model; Buffer Day 2 (Thursday 2026-04-30) **not consumed** — F-3 + F-4 + F-5 sequencing-collision hedge unused. PR #220 squash-merged to main 2026-04-26 (commit `f7bf204`). |
| 2026-04-25 | F-212: Improve Executive-Architecture Infographic | [212](../02_PRD/212-improve-executive-architecture-infographic-2026-04-24.md) | **Quality follow-up to PRD #128 — fourth iteration in the executive-architecture infographic arc (#128 established position → #136/#145 corrected MAESTRO layer accuracy → #212 upgrades the visual from inventory to diagnosis).** Transforms the executive-architecture page (PDF pages 2–3, the most-prominent visual in the security report booklet) from a bulleted text inventory of components inside three pastel layer bands into an OpenClaw-style system flow diagram with rounded-rectangle nodes, top-to-bottom directional arrows, and 6–8 system-wide callouts on leader lines anchored to specific nodes. Three implementation levels bundled in a single PRD (1-week envelope across 3 user stories): **Level 1** (US1 P1) — locked verbatim Gemini prompt rewrite in `.claude/skills/tachi-infographics/references/gemini-prompt-construction.md` and `executive-architecture.md` (no runtime composition of aesthetic language) instructing rounded-rectangle node rendering with layer-coded fill + colored border, directional top-to-bottom arrows with explicit arrowheads opening with the `schematic diagram with shapes and arrows` directive to defeat the text-only Gemini failure mode, leader-lined callout anchoring on specific nodes, and compact factual-badge rendering for qualifying-but-empty layers (≤15% page height, replacing the prior 30% full-band placeholder waste); per-layer border-accent colors inherit unchanged from the severity / layer palette in `visual-design-system.md`. **Level 2** (US2 P1) — `_select_critical_high_callouts()` rewritten from per-layer dedup to system-wide Largest Remainder Method allocation: returns 6–8 callouts drawn system-wide (not one per layer), per-layer floor rule guarantees every qualifying layer with ≥1 Critical/High finding gets ≥1 callout (regression-tested as a pytest invariant on a fixture matrix), per-layer ceiling caps any single layer at 4 callouts with a compact "+ N more in this layer" annotation absorbing the remainder, total-floor short-circuits to all qualifying findings when the system-wide qualifying count is < 6 (no synthetic inflation, no empty slots). Stable deterministic tie-break produces byte-identical `callouts[]` arrays across reruns on identical input. **Level 3** (US3 P2) — `_build_executive_architecture_payload()` extended with structural data forwarded from `parse_scope_data`: `flow_edges[]` (each entry `{source, destination}` with `destination` aligned to the existing `_compute_data_flows` producer field name — not `target`) sorted by `(source.lower(), destination.lower())` and truncated to 50 entries after sort with logged warning per the sort-then-truncate ordering policy; `clusters[]` (each entry `{name, members}` derived from `trust_boundaries[].zone` + `trust_boundaries[].components`) sorted by `(_TRUST_LEVEL_ORDER, name.lower())` with case-insensitive `members` ordering matching the existing `_compute_trust_zones` pattern. Empty-source fixtures emit empty arrays (not `null`, not missing keys); the Gemini prompt updated in the same Level 3 landing references `flow_edges` and `clusters` by name and no longer instructs Gemini to infer flow or clustering from component names alone. **Backward compatibility preserved**: PRD #128 skip behavior maintained — threat models with zero Critical/High findings continue to skip the template entirely and produce byte-identical PDF output to the pre-F-212 baseline (zero behavior change on empty-finding architectures). **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements*.txt`, `package.json`; Level 3 reuses the existing `parse_scope_data` shared scope parser. **Reference dataset**: in-repo fixture `tests/scripts/fixtures/exec_arch/agentic_app/threats.md` serves as the CI-validated source of truth; sibling-repo validation (`~/Projects/second-brain-mcp/docs/security/2026-04-23T23-02-25/`) is a local developer affordance, not a CI gate. 37/37 tasks complete across 6 waves (Phase 1 prompt rewrite, Phase 2 callout rework, Phase 3–5 US3 implementation, Phase 6 polish T033–T037 with PDF byte-identity check + post-US3 runtime verification). 3 user stories delivered (US-212-1 OpenClaw-style rendering, US-212-2 denser system-wide callout distribution with per-layer floor rule, US-212-3 structural data in payload). Governance: PM APPROVED_WITH_CONCERNS + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-24) — all concerns resolved inline before sign-off (palette inheritance reframed as 3-way per Design Q1, per-layer floor rule mechanically enforceable invariant added per architect HIGH concern, L3 schema field names aligned to existing `_compute_data_flows` producer per architect MEDIUM concern, clusters[] member ordering determinism declared alphabetical-case-insensitive matching `_compute_trust_zones` per architect MEDIUM concern). PR #213 squash-merged to main 2026-04-25 (commit `3df035b`). |
| 2026-04-27 | F-224: F-4 ASI09 Human-Agent Trust Exploitation (OWASP ASI09:2026 communication axis) | [224](../02_PRD/224-trust-exploitation-threat-agent-2026-04-26.md) | **Fourth Tier 1 detection feature in BLP-01 (after F-1 LLM05 → output-integrity, F-2 LLM09 → misinformation, F-3 ASI07 → tool-abuse enrichment), third execution of Heuristic A standalone-new-agent branch, and the feature that closes OWASP Agentic Top 10:2026 framework row coverage to 10/10.** Closes the OWASP ASI09:2026 **communication axis** — the agent-to-human trust-exploitation surface where consumer-facing AI agents emit content directly to end users (chatbots, mental-health companions, eldercare coaches, financial-advisory assistants, AI tutors, customer-support agents) without architectural mechanisms preventing impersonation, over-persuasion, undisclosed AI authorship, or synthetic-relationship exploitation. The autonomy axis of ASI09 (excessive-autonomy / missing-HITL / goal-drift / cascading-failures) remains attributed to the existing `agent-autonomy` agent; F-4 carves up ASI09 between the two agents at the ADR-033 D2 layer with **zero diff to `agent-autonomy.md`** despite the sub-scope assignment. New AI-tier `human-trust-exploitation` agent emits `TE-{N}` findings with `category: agentic` and populated `source_attribution` citing OWASP ASI09:2026 primary plus relevant CWEs related (CWE-223 Omission of Security-Relevant Information, CWE-287 Improper Authentication, CWE-290 Authentication Bypass by Capture-Replay, CWE-345 Insufficient Verification of Data Authenticity — all four catalog-verified present in `schemas/taxonomy/cwe.yaml`). **Heuristic A four-way signal-class discipline operationalized** per ADR-033 Decision 2 cross-referencing ADR-030 D1 + ADR-031 D2 + ADR-032 enrichment-eligibility test: F-4 owns communication-axis signal class (AI-disclosure / authority-claim / persuasion / persona / synthetic-relationship — agent-to-human trust patterns); F-1 owns output-sanitization (machine-victim, bytes/strings/syntax primitives); F-2 owns factual-integrity (machine-consumer, factual-content primitives); `agent-autonomy` autonomy axis owns agent-own-behavior boundaries (excessive-autonomy / missing-HITL — vocabulary-disjoint per ADR-032's enrichment-eligibility test). ADR-033 explicitly resolves four scope boundaries plus the ASI09 sub-scope carve-up: (a) not subsumed into `output-integrity` (machine-victim sanitization), (b) not subsumed into `misinformation` (machine-consumer factual-integrity), (c) not an enrichment of `agent-autonomy` (vocabulary-disjoint), (d) not a hypothetical merge with `agent-autonomy` (autonomy axis vs. communication axis are distinct sub-axes of ASI09 with non-overlapping mitigation vocabularies). **Schema bump `schemas/finding.yaml` 1.7 → 1.8** (minor, additive) per ADR-033 D8 — **third application** of the ADR-030 Decision 8 "ADR-026 Complex-Shape Clarifier extended to regex-alternation prefix additions" rule. `id.pattern` extended from `^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI)-\d+$` to `^(S|T|R|I|D|E|AG|LLM|AGP|OI|MI|TE)-\d+$` — 11 pre-existing prefixes preserved verbatim; `TE` appended as 12th alternation value. New **ADR-033** (`docs/architecture/02_ADRs/ADR-033-human-trust-exploitation-agent.md`, Status: Accepted at PR #225 merge per Proposed → Accepted dual-commit lifecycle; 7th consecutive ADR per ADR-027/028/029/030/031/032/033 lineage) records **10 numbered body items**: D1-D8 PRD-original (new-agent adoption, Heuristic A four-way partition, lean-agent ADR-023 detection-variant conformance, regex-alternation Complex-Shape Clarifier 3rd application, 26-file zero-edit invariant preservation including critical zero-diff on `agent-autonomy.md` despite ASI09 sub-scope carve-up, Proposed → Accepted dual-commit governance), D9 **Naming Disambiguation** (HIGH-1 — explicit `human-trust-exploitation` agent name hyphen-cased / agent-to-human ASI09 scope vs. existing `agentic_pattern: "trust_exploitation"` schema-enum value underscore-cased / agent-to-agent multi-agent-topology scope per Feature 142 / `maestro-agentic-patterns-shared.md` R-04), D10 **DFD Target Decision** (BLOCKING-1 — `dfd_targets: [Process]` only, mirroring F-1 / F-2 single-target precedent; no External Entity declaration; future AI-tier agents wishing to declare External Entity must update this ADR and document the orchestrator-tier dispatch-rules External Entity inclusion pattern explicitly). New agent `.claude/agents/tachi/human-trust-exploitation.md` (122 lines, within ADR-023 ≤150 soft cap) + new companion skill `.claude/skills/tachi-human-trust-exploitation/` with `README.md` + `references/detection-patterns.md` carrying 5 numbered pattern categories (Undisclosed AI Authorship / Authority-Claim Emission Without Attestation / Persuasive-Tone Manipulation / Persona-Boundary Violations on Long-Running Dialogues / Synthetic-Relationship Exploitation) each with ≥4 indicators + ≥1 worked example + primary/related citations + trigger keywords + DFD element types. Mitigation text names specific AI-disclosure / confidence-calibration / refusal-pattern / persona-boundary / synthetic-relationship-safeguard mechanisms — `mandatory AI-generation disclosure banner on every user-facing turn`, `pre-conversation AI-disclosure splash with explicit consent confirmation`, `confidence-threshold gate on authority-claim emission with confidence display`, `source-attestation requirement on legal/medical/financial claims with citation rendered to user`, `temperature-bounded decoder for high-stakes responses`, `persuasion-pattern classifier with refusal on detected manipulation tactics`, `persona-memory timeout`, `identity-impersonation refusal pattern`, `session-length cap with mandatory break / escalation-to-human path`, `dependency-risk classifier`, **`vulnerable-population safeguards layer`** (mental health / eldercare / minors / cognitive-impairment users get additional safeguards: escalation-to-human on high-emotional-distress detection, mandatory professional-care referral, parental-consent for minors, session-context expiry) — no generic "be transparent" prose. **Six-edit Wave 2.0 grep-checklist** (architect MEDIUM-5 — promoted from convention to verifiable artifact at F-4): orchestrator.md 3 edits (AI-tier dispatch list addition, sequential-mode text addition, Agentic Threats row addition) + dispatch-rules.md 3 edits (Agentic list addition, table row addition, table column annotation) — all 6 edits landed with explicit grep verification. **26-file zero-edit invariant preserved** (ADR-023 lineage extended through F-4): zero modification edits to the 13 other STRIDE+AI detection agents (including critical zero-diff on `agent-autonomy.md` despite the ASI09 sub-scope carve-up — the carve-up is documented at the ADR-033 D2 layer only; `agent-autonomy.md`'s `owasp_references` already lists ASI-09 so no metadata edit was needed) + 13 companion `detection-patterns.md` files, zero edits to 5 infrastructure-tier consumers (risk-scorer / control-analyzer / threat-report / threat-infographic / report-assembler). **F-A2 `source_attribution` 3rd net-new producer** — F-4 is the 3rd net-new threat agent (after F-1 OI / F-2 MI; F-3 was first to use the enrichment-branch pattern instead of standalone), proving F-A2 referential-integrity contract against three independent populators. **New `examples/consumer-agent-app/` baseline** (Q5 lean per architect Wave 3 Step 1 decision — clean-slate baseline for F-4 trigger validation, NOT agentic-app extension): WellnessCompanionChatbot mental-health/wellness companion archetype with all 4 FR-006 emission indicators engaged (outgoing flow to End User External Entity, consumer-facing prose match on `chatbot`/`companion`/`coach`, persistent persona/multi-turn dialogue, wellness coaching authority); **5 TE findings (TE-1..TE-5, one per Pattern Category 1-5) emitted on regen**; 19 total findings (1 Critical, 8 High, 7 Medium, 3 Low); pipeline regen byte-identical (40-page PDF, SHA-256 `7ac0b639...269bce5`) per ADR-021 `SOURCE_DATE_EPOCH=1700000000` invariant; 6/6 infographic JPEGs generated (baseball-card / system-architecture / executive-architecture / risk-funnel / maestro-stack / maestro-heatmap). **Three-prefix-family discipline within agentic** demonstrated (`AG` autonomy-axis / `AGP` multi-agent-topology / `TE` communication-axis render as distinct signal classes with disjoint mitigation vocabularies); combined with the LLM three-prefix family from F-2 (`LLM` input-side / `OI` output-sanitization / `MI` factual-integrity) yields a **6-prefix-family AI threat surface** with Heuristic A signal-class boundary preservation across both AI-tier sub-categories. **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`; schema 1.7 → 1.8 is purely additive (regex broadens; existing IDs remain valid). **Byte-identity regression gate green**: 5/5 non-consumer-facing baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — bonus 6/6 with maestro-reference baseline = 6/6 byte-identity preserved; two-part emission gate per FR-013 (AI keyword match AND human-user-facing emission indicator both required) guarantees zero `TE-{N}` findings on these architectures, which guarantees unchanged downstream artifacts. **R12 release-please belt-and-suspenders held** (T058 pre-merge title check + T063 PR ready + T066 post-merge release verification — `v4.23.0` published cleanly at 2026-04-26T15:32:02Z without empty-marker-commit fallback; **3 consecutive clean releases F-2 + F-3 + F-4** post-F-212 incident). **R11 Naming Disambiguation grep test PASS** (vacuous-pass on consumer-agent-app baseline because zero AGP carry-over; test infrastructure validated for substantive multi-agent regression coverage in future regenerations). 67/71 tasks complete pre-merge + 4 deferred to Wave 7 buffer-day post-merge tail (T022 ADR-Accepted flip, T024 verification companion, T025 SHA fill, T067 SHA companion). **16-parallel SC verification at Wave 6** (F-4 architect-validated parallelism — NEW PRECEDENT): T046-T061 dispatched in a single 16-parallel agent call (15 senior-backend-engineer + 1 code-reviewer for T054); all 15 SC checks returned PASS in one verification cycle; no remediation cycle needed. **Strategic significance — BLP-01 Tier 1 fourth feature and OWASP Agentic Top 10:2026 row 10/10 closure**: F-4 closes ASI09 communication axis on the BLP-01 Coverage Matrix (autonomy axis remains attributed to `agent-autonomy`); BLP-01 progress: 7/11 features delivered (Foundation + F-1 + F-2 + F-3 + F-4); Tier 1 at 4/5 (F-5 LLM10 remains). OWASP Agentic Top 10:2026 framework row coverage now **10/10** (ASI-01 through ASI-10 all covered: ASI-01, ASI-02, ASI-04, MCP-03, MCP-05, ASI-07 covered pre-F-4; ASI-09 communication axis covered with autonomy axis already covered post-F-4; ASI-03/05/06/08/10 implicitly covered via existing `agent-autonomy` and `tool-abuse` enrichment scope). LLM Top 10:2025 framework row at **9/10** (LLM10 Unbounded Consumption remains — closes with F-5). Three-execution-deep standalone-branch pattern validation (F-1 + F-2 + F-4) plus one enrichment-branch validation (F-3) provides STABLE pattern lineage for F-5/F-6/F-7/F-8: lean-agent shape (≤150 lines), 5-category companion catalog, regex-alternation minor-bump rule, Proposed → Accepted dual-commit ADR, additive-only shared-reference edits, F-A2 referential-integrity contract, two-part emission gate, 26-file zero-edit invariant, Heuristic A signal-class partition, ADR-024-style Naming Disambiguation (NEW from F-4), DFD Target Decision section (NEW from F-4), R12 release-please belt-and-suspenders, 16-parallel SC verification (NEW from F-4). 3 user stories delivered (US-224-1 human-trust communication-axis detection on consumer-facing AI agents, US-224-2 named human-trust mitigation guidance specific to ASI09 communication axis, US-224-3 trust-exploitation communication-axis as a first-class distinct threat class with stable ADR crosswalk — all P0). Governance: PM APPROVED + Architect APPROVED + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-26) — all 3 architect plan-stage residuals absorbed at task-decomposition layer (MEDIUM-A schema-touch via gh pr diff post-filter, MEDIUM-B Q5 expected-diff manifest, LOW-C F-5 forward-pointer). Build duration: ~9h active dev work in single 9-hour session (Wave 1.1 atomic gate at 16:04 → Wave 6 closeout at 22:18 on 2026-04-26); ~22h elapsed wall clock from PRD authored to PR merge — **>50% under** PRD 2-day + buffer envelope; buffer untouched (R2 / R6 / R7 not fired; clean Q5 lean path; clean false-positive check; clean NFR-006/7 review). PR #225 squash-merged to main 2026-04-26 (squash commit `feaeb95019340a85681b65198a4b42e3a92b16a4`); release **v4.23.0** tagged 2026-04-26T15:32:02Z. |
| 2026-04-27 | F-229: F-5 LLM10 Unbounded Consumption Verification (OWASP LLM10:2025) | [229](../02_PRD/229-llm10-unbounded-consumption-verification-2026-04-27.md) | **Fifth Tier 1 detection feature in BLP-01 (after F-1 LLM05 → output-integrity, F-2 LLM09 → misinformation, F-3 ASI07 → tool-abuse enrichment, F-4 ASI09 → human-trust-exploitation), second execution of the Heuristic A enrichment branch (after F-3 single-host scope), first execution at two-agent scope (DoS + model-theft jointly host the LLM10 surface), and the feature that closes OWASP LLM Top 10:2025 framework row coverage to 10/10 — completing OWASP AI top-10 = 20/20 Covered combined with F-4's ASI09 closure.** Closes the OWASP LLM10:2025 **Unbounded Consumption** threat surface — the 5-sub-pattern surface (inference-request flooding, token-budget exhaustion, context-window exhaustion, cost amplification via recursive prompts, denial-of-wallet) where tachi previously shipped partial signal through the existing `denial-of-service` STRIDE-tier agent (Categories 1-11 covered web-app DoS, algorithmic complexity, network flooding, cascade failures but did not name LLM-specific patterns) and the existing `model-theft` AI-tier agent (Category 6 covered general unbounded inference but did not name cost amplification or denial-of-wallet) without auditing either against the full LLM10 sub-pattern taxonomy. **Heuristic A enrichment-branch architectural choice at two-agent scope** per ADR-034 Decision 1 cross-referencing ADR-030 D1 (signal-class taxonomy in LLM tier — applied at two-agent scope) + ADR-031 D8 (regex-alternation rule cross-referenced as the **asymmetry** F-5 does NOT invoke — first BLP-01 feature with zero schema bump at two-agent scope) + ADR-032 (direct precedent at single-agent scope; lines 84+182 forecast that F-5 will not need a schema bump — **fulfilled**) + ADR-033 D2 (sub-scope carve-up structural sibling — ADR-033 carved one OWASP entry across two host agents at the documentation layer; ADR-034 enriches one OWASP entry across two host agents at the pattern-catalog layer): infrastructure-resource-exhaustion sub-patterns (inference-request flooding, token-flooding, context-window-exhaustion as DoS) are same-class as `denial-of-service`'s availability-degradation surface; extraction-driven-resource-abuse sub-patterns (cost amplification, denial-of-wallet) are same-class as `model-theft`'s unbounded-inference-consumption surface (already at Category 6). Authoring a hypothetical `unbounded-consumption` agent would fragment LLM10 findings across three locations (the new agent + DoS adjacency + model-theft adjacency); enrichment of the two existing host agents preserves the signal-class consolidation per Heuristic A and matches the rule SDR-001 Decision 4 locked. **Q1 SPLIT cross-agent vector decomposition** (first BLP-01 sub-pattern with cross-agent vector decomposition within a single OWASP catalog entry): Cat 13 Context-Window Exhaustion bifurcates into Vector A (latency-DoS, owned by `denial-of-service`) and Vector B (cost-DoW, owned by `model-theft`). The same architecture surface — an unbounded context window — manifests both vectors but with disjoint mitigation vocabularies. **Q3 severity-floor 2-condition CRITICAL rule** on Cat 11 (Denial-of-Wallet): defaults to HIGH; CRITICAL only fires when (a) multi-tenant freemium structure is structurally evident AND (b) BOTH per-tenant token budget AND cost alerting are absent — first BLP-01 finding-emission rule with a **2-condition gated severity floor** (vs. single-condition severity assignments of F-1 / F-2 / F-4); encoded in worked example narrative (model-theft companion Cat 11) and in 1 of the 5 fixtures (`valid_category_11_critical_floor_freemium_finding.yaml`). **T1496 prose-only on Cat 10/11**: MITRE ATT&CK T1496 Resource Hijacking appears in worked-example narrative prose on Cat 10/11 but is explicitly absent from any `references` array entry across both companions and all 5 fixtures (T1496 is not catalog-resolvable per `schemas/taxonomy/mitre-attack.yaml`; ADR-034 D6 fixes the rule — 2 prose mentions / 0 references-array entries verified across the full corpus by T038/T039/T056). **5/5-dimension reduction at two-agent scope (vs. F-3 single-host scope)**: zero new agent file (vs. F-1 / F-2 / F-4's ~150 lines × 2 = ~300 lines authoring + review), zero new skill directory (vs. F-1 / F-2 / F-4's ~400 lines × 2 = ~800 lines authoring + review), **zero schema bump** (vs. F-1 / F-2 / F-4's `schemas/finding.yaml` minor bumps; F-5 reuses existing `D` (DoS) and `LLM` (model-theft) prefixes; schema stays at v1.8 — the post-F-4 baseline; F-5 = second BLP-01 detection feature with zero schema bump after F-3 + first at two-agent scope), zero consumers-list edit (24-file zero-edit invariant on the 24 non-target detection-tier files preserved per ADR-023 lineage), zero functional orchestrator/dispatch edit (Q2 cosmetic dispatch annotation default-NO; both agents already fully registered in `dispatch-rules.md` lines 73/109/157 and `orchestrator.md` lines 37/42/298/372; cosmetic annotation `denial-of-service (CWE-400)` → `denial-of-service (CWE-400, LLM10)` at default-NO per architect adjudication). **4-edit additive-only Wave 2 grep checklist** (vs. F-3's 2-edit single-host pattern): `denial-of-service.md` 3 additive edits (metadata `owasp_references += LLM10:2025`, `## Purpose` 1-3 line LLM-inference-exhaustion surface naming, Detection Workflow Step 5 references list extension); `denial-of-service` companion `detection-patterns.md` Pattern Categories 12 (LLM Inference-Request Flooding and Token Exhaustion) + 13 (Context-Window Exhaustion — Latency-Driven Variant) appended after Cat 11 with byte-identical preservation of lines 1-83 (Overview / Targeted DFD Element Types / 8 thematic groups) + lines 84-155 (Cat 9/10/11) + Primary Sources section additively extended; `model-theft.md` 2 additive edits (`## Purpose` 1-3 line cost-amplification + denial-of-wallet surface naming; metadata is a no-op — `owasp_references` already includes LLM10:2025 per PRD-time grep at line 17, asymmetric to denial-of-service's one-line append); `model-theft` companion `detection-patterns.md` Pattern Categories 10 (Cost Amplification via Recursive or Cost-Asymmetric Prompting) + 11 (Denial-of-Wallet via Context-Window Cost Amplification) appended after Cat 9 with byte-identical preservation of Categories 1-9 + Overview + Targeted DFD Element Types + Trigger Keywords; both companions gain new **Pattern Category Disambiguation subsections** (DoS Cat 9 vs Cat 12/13 boundary + model-theft Cat 6 vs Cat 10/11 boundary) to prevent dispatch-mode confusion across single-agent denial-of-service-tier and AI-tier model-theft signals. New **ADR-034** (`docs/architecture/02_ADRs/ADR-034-llm10-unbounded-consumption-audit.md`, Status: Accepted at squash commit `e086d31` per Proposed → Accepted dual-commit lifecycle; 8th consecutive ADR per ADR-027/028/029/030/031/032/033/034 lineage) records 9 numbered Decisions covering D1 enrichment-vs-new-agent at two-agent scope; D2 additive-only edits across **4 host files** (extending F-3's 2-edit single-host pattern to multi-host enrichment branch); D3 **canonical 5-row LLM10 sub-pattern → owning-agent mapping table populated COMPLETE with severity-hint annotation column** (audit deliverable per team-lead MEDIUM-1 — Inference flooding/Cat 12 → DoS Critical; Context-window latency/Cat 13 Vector A → DoS Critical; Cost amplification/Cat 10 → model-theft Critical; Denial-of-wallet/Cat 11 Vector B → model-theft HIGH default + CRITICAL on Q3 2-condition; Context-window cost-amplification cross-vector → model-theft Cat 11 sub-pattern); D4 no schema bump (second BLP-01 detection feature reusing existing `D` and `LLM` prefixes; first at two-agent scope; explicit asymmetry to F-1 / F-2 / F-4 minor bumps; explicit asymmetry to ADR-031 D8 regex-alternation rule); D5 no consumers-list edit; D6 no functional orchestrator/dispatch edit (cosmetic Q2 annotation only); D7 Pattern Category Disambiguation across two companion catalogs; D8 no `source_attribution` populator wiring extension (F-A3 deferral); D9 public-only governance per SDR-001 Option C. **22-file zero-edit invariant preserved INCLUDING `human-trust-exploitation.md` NOT-edit + `tool-abuse.md` NOT-edit** (12 other agents + 7 infrastructure consumers + the 2 most-recent F-3 / F-4 enrichment / new-agent files NOT touched despite F-3 being a sibling enrichment-branch and F-4 having just shipped the prior day); F-5 = 28 detection-tier files post-F-4 inventory + 4 file edits + 0 net-new = 28 files post-merge. **F-5 deliberately does NOT extend `source_attribution` populator wiring on the host agents** — that scope belongs to F-A3 (deferred per `schemas/finding.yaml` lines 230-238 and ADR-028 Decision 6); LLM10 citations on every emitted Cat-12/13 (`D-{N}`) and Cat-10/11 (`LLM-{N}`) finding live in the prose-level `references:` array (existing field present in finding YAML schema since v1.0). **F-A3 dependency direction is one-way (F-5 → F-A3 inheritance); F-5 does not require F-A3 to ship and does not block on it.** When F-A3 ships, the LLM10 citations established by F-5 will flow into `source_attribution` naturally because the catalog references are already correct. **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. **Byte-identity SC-9 regression gate green at 6/6 baselines**: all 6 non-LLM-serving example PDF baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`, `maestro-reference`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — LLM-serving topology gate per FR-015 guarantees zero Cat-12/13 / Cat-10/11 emissions on these architectures (no inference endpoint, no token-counting middleware, no cost-attribution layer, no LLM API gateway), which guarantees unchanged downstream artifacts. **`agentic-app` mutation candidate** per Q5 architect adjudication (extended with multi-component LLM topology already exercised by F-3): **4 NEW findings emerged on regen** — D-10 (Cat 12 Inference-Flooding, Critical), D-11 (Cat 13 Context-Window Latency Vector A, Critical), LLM-15 (Cat 10 Cost-Amplification, Critical), LLM-16 (Cat 11 Denial-of-Wallet Vector B, High per Q3 default — single-tenant); cohesive category rendering preserved (single Section 3.5 DoS + single Section 3.8 LLM in `threat-report.md` with no fragmentation; correlation group CG-8 binds all 4 in Theme 5 of regenerated narrative). **Test infrastructure update** at `tests/scripts/test_backward_compatibility.py`: removed F-5 hosts from `DETECTION_AGENT_PATHS` (12 → 10) and added to `DETECTION_PATTERN_REF_ENRICHMENT_HOSTS` frozenset (extending F-3's single-host pattern to multi-host enrichment branch); 26 enrichment tests at `tests/scripts/test_llm10_unbounded_consumption_enrichment.py` (line caps + MAESTRO grep + MANDATORY Read directive + Pattern Categories + Disambiguation + T1496 prose-only + per-fixture references-array + Q3 severity-floor); pipeline regen produces 79-page security-report.pdf reflecting Tier 1 residual risk. **R12 release-please belt-and-suspenders held** (T077 pre-merge title check + T078 PR ready + post-merge release verification — release-please PR #226 (v4.24.0) opened cleanly within ~30s of squash-merge; **4 consecutive clean releases F-2 + F-3 + F-4 + F-5** post-F-212 incident; no empty-marker fallback invoked). **Strategic significance — BLP-01 Tier 1 fully delivered at 5/5 and OWASP LLM Top 10:2025 row 10/10 closure + 20/20 OWASP AI top-10 milestone**: F-5 closes LLM10:2025 on the BLP-01 Coverage Matrix (Partial → Covered); BLP-01 progress: **8/11 features delivered** (Foundation + F-1 + F-2 + F-3 + F-4 + F-5); Tier 1 **fully delivered at 5/5**. OWASP LLM Top 10:2025 framework row coverage now **10/10** (LLM01-LLM09 covered pre-F-5 via prompt-injection / data-poisoning / model-theft / output-integrity / misinformation / etc.; LLM10 covered post-F-5 via two-agent enrichment of denial-of-service + model-theft). Combined with F-4's OWASP Agentic Top 10:2026 row 10/10 closure (post-F-4) yields **OWASP AI top-10 = 20/20 fully Covered** across both AI-tier sub-frameworks — a **strategic milestone marker** that pairs cleanly with the BLP-01 8/11 features delivered count and unlocks downstream Tier 2 features F-6 (ML attacks) + F-7 (Mobile attacks) bundles where target taxonomies may also map cleanly to existing detection agents at multi-agent scope. Two-execution-deep Heuristic A enrichment-branch validation (F-3 single-host + F-5 two-host) combined with three-execution-deep standalone-branch validation (F-1 + F-2 + F-4) provides STABLE pattern lineage across both enrichment scopes for downstream F-6 / F-7 / F-8 ML-extraction + Mobile bundles + post-Tier-1 follow-ons. F-5 = first BLP-01 sub-pattern with cross-agent vector decomposition within a single OWASP catalog entry (Q1 SPLIT) and first BLP-01 finding-emission rule with 2-condition gated severity floor (Q3 Cat 11 denial-of-wallet). 85/85 tasks complete across 4 waves (Wave 1 ADR-Proposed + DoS agent edit + 5 LLM10 fixtures + companion authoring + Wave 2 model-theft companion edit + Disambiguation subsections + agentic-app regen + LLM10 verification + Wave 3 tester verification + code review + ADR-034 Accepted + Wave 4 SC sweep + CLAUDE.md update + quickstart smoke + post-merge polish T042 ADR-034 SHA fill + T079 delivery retrospective). 3 user stories delivered (US-229-1 full LLM10 surface coverage via two-agent enrichment, US-229-2 LLM-specific mitigation naming on all new findings, US-229-3 audit-confirmed additive-only transition with canonical mapping table — all P0). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-27). Build duration: ~3.85h plan-to-Wave-4-close clock (Sun 2026-04-27 11:30 EDT plan stage → 15:21 EDT Wave 4 close) + post-merge polish at 16:49 EDT — well under PRD 1.5-day envelope; F-3+F-4+F-5 sequencing-collision hedge unused. PR #230 squash-merged to main 2026-04-27 (squash commit `e086d31e4bead0dd7cb3de3fd63e4a120da59133`); release **v4.24.0** released-please PR #226 opened cleanly within ~30s of squash-merge. |
| 2026-04-29 | F-237: F-7 Mobile Top 10 Coverage Bundle [Tier 2] (OWASP Mobile Top 10:2024) | [237](../02_PRD/237-mobile-top-10-coverage-bundle-2026-04-28.md) | **Second Tier 2 detection feature in BLP-01 (after F-6 ML Top 10), fourth execution of the Heuristic A enrichment branch (after F-3 single-agent / F-5 two-agent / F-6 three-agent), first execution at four-or-five-agent scope (5 host agents jointly host the Mobile Top 10 surface: `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation`), and the feature that closes OWASP Mobile Top 10:2024 framework row coverage to 10/10 — completing OWASP four-framework total = 40/40 Covered combined with F-4's ASI09 closure + F-5's LLM10 closure + F-6's ML Top 10 closure.** Closes the OWASP Mobile Top 10:2024 **mobile-platform threat surface** — the 10-sub-pattern surface (M1 Improper Credential Usage, M2 Inadequate Supply Chain Security, M3 Insecure Authentication/Authorization, M4 Insufficient Input/Output Validation, M5 Insecure Communication, M6 Inadequate Privacy Controls, M7 Insufficient Binary Protections, M8 Security Misconfiguration, M9 Insecure Data Storage, M10 Insufficient Cryptography) where tachi previously had **zero mobile-platform detection coverage** — empirical grep across `.claude/agents/tachi/*.md` and `.claude/skills/tachi-*/references/*.md` returned zero matches for "OWASP Mobile", "MASTG", or "MASVS" at PRD time. **Heuristic A enrichment-branch architectural choice at four-or-five-agent scope** per ADR-036 D-1 cross-referencing ADR-030 D-1 (signal-class taxonomy applied at four-or-five-agent scope) + ADR-031 D-8 (regex-alternation rule cross-referenced as the **asymmetry** F-7 does NOT invoke — fourth BLP-01 feature with zero schema bump after F-3 + F-5 + F-6; first at four-or-five-agent scope) + ADR-032 (single-agent precedent at single-agent scope) + ADR-033 D-2 (sub-scope carve-up structural sibling — ADR-033 carved one OWASP entry across two host agents at the documentation layer; ADR-036 carves TEN OWASP entries across five host agents at the pattern-catalog layer) + ADR-034 (direct two-agent precedent) + ADR-035 (direct three-agent precedent; closing forward-scope marker forecast that F-7 will not need a schema bump — **fulfilled at four-or-five-agent scope**): M1+M3 sub-patterns (mobile credential storage / mobile session handling) are same-class as `spoofing`'s identity/credential signal class; M2+M4+M7 sub-patterns (mobile SDK supply-chain / mobile IPC input validation / mobile binary protections) are same-class as `tampering`'s data-integrity / supply-chain signal class; M5+M6+M9+M10 sub-patterns (mobile transport security / mobile privacy controls / mobile secure storage / mobile cryptography) are same-class as `info-disclosure`'s confidentiality signal class; M8 splits across `privilege-escalation` (privilege-gain variant — exposed admin/debug interfaces, default permissive ContentProvider exports, missing app-attestation, missing root-detection) and `repudiation` (accountability-loss variant — missing audit logging, disabled crash reporting, debug log leakage, missing tamper-evident timestamping). Authoring a hypothetical `mobile-platform` agent would fragment Mobile Top 10 findings across eleven adjacencies (one new agent + four-or-five existing-agent adjacencies); enrichment of the five existing host agents preserves the signal-class consolidation per Heuristic A and matches the rule SDR-001 Decision 4 locked. **M8 dual-host disjoint architectural-tells operationalized** at the F-7 layer per ADR-036 D-4: privilege-gain variant routes to `privilege-escalation` host; accountability-loss variant routes to `repudiation` host — same OWASP catalog entry surfaces with disjoint mitigation vocabularies on two distinct STRIDE-tier hosts. **Second BLP-01 sub-pattern with cross-agent decomposition** after F-5's Q1 SPLIT (vector axis) and F-6's ML06 corpus-vs-artifact (architectural-surface axis) — first across two distinct STRIDE-tier hosts at architectural-tell axis. **M4 cross-agent boundary with F-1 `output-integrity`** per ADR-036 D-5: mobile IPC input validation routes to `tampering` Cat 12; LLM-output sanitization remains owned by `output-integrity` — disjoint-tells split prevents duplicate detection on hybrid LLM-plus-mobile architectures. **MITRE ATT&CK Mobile catalog gap propagation 3-of-3 worst-case scale**: 3 of 3 cited ATT&CK Mobile techniques (T1474 Supply Chain Compromise on M2, T1626 Abuse Elevation Control Mechanism on M7/M8 privilege-gain, T1398 Boot or Logon Initialization Scripts on M3 and M8 accountability-loss) are NOT in `schemas/taxonomy/mitre-attack.yaml` and ship as **prose-only** worked-example narratives explicitly absent from any `references` array entry across all 10 Pattern Categories and all 5 fixtures (3-of-3 prose-only worst-case scale per F-A2 referential-integrity contract; ADR-036 codifies the rule per F-5 T1496 + F-6 ATLAS-gap precedents). **5/5-dimension reduction at four-or-five-agent scope (vs. F-6 three-agent scope)**: zero new agent file (vs. F-1 / F-2 / F-4 standalone-branch ~150 lines × 1 = ~150 lines authoring + review per agent), zero new skill directory (vs. F-1 / F-2 / F-4's ~400 lines × 1 = ~400 lines authoring + review per agent), **zero schema bump** (fourth BLP-01 detection feature reusing existing `S` `T` `I` `E` `R` STRIDE prefixes; schema stays at v1.8 — the post-F-4 baseline; F-7 = fourth BLP-01 detection feature with zero schema bump after F-3 + F-5 + F-6 + first at four-or-five-agent scope), zero consumers-list edit (24-file zero-edit invariant on the non-target detection-tier files preserved per ADR-023 lineage), zero functional orchestrator/dispatch edit (Q-decision cosmetic dispatch annotation default-NO; all 5 host agents already fully registered in `dispatch-rules.md` and `orchestrator.md`). **10-edit additive-only build path** (vs. F-6's 6-edit three-host pattern): 5 host agent files (`spoofing.md` + `tampering.md` + `info-disclosure.md` + `privilege-escalation.md` + `repudiation.md`) each gain 3 additive edits (metadata `owasp_references`, `## Purpose`, Detection Workflow Step 5 references list); 5 companion `detection-patterns.md` files each gain 1-4 new Pattern Categories (spoofing 2 / tampering 3 / info-disclosure 4 / privilege-escalation 1 / repudiation 1 = 11 net-new Pattern Categories total) appended after their existing last category with byte-identical preservation of pre-existing categories per ADR-023 Decision 3 additive-only invariant; all 5 companions gain new **Pattern Category Disambiguation subsections** (spoofing Cat N+1/N+2 vs Cat 1-N + tampering Cat 11/12/13 vs Cat 1-9 + info-disclosure Cat N+1..N+4 vs Cat 1-N + privilege-escalation Cat N+1 vs Cat 1-N + repudiation Cat N+1 vs Cat 1-N) to prevent dispatch-mode confusion across STRIDE-tier signals. New **ADR-036** (`docs/architecture/02_ADRs/ADR-036-mobile-top-10-coverage-bundle.md`, Status: Accepted at squash commit `e962a0e` per Proposed → Accepted dual-commit lifecycle; 9th consecutive ADR per ADR-027/028/029/030/031/032/033/034/035/036 lineage) records 9-or-10 numbered Decisions covering D-1 enrichment-vs-new-agent at four-or-five-agent scope; D-2 additive-only edits across **10 host files**; D-3 canonical Mobile Top 10 sub-pattern → owning-agent mapping table populated COMPLETE with severity-hint annotation column (audit deliverable per ADR-035 D-3 precedent); D-4 M8 dual-host split decision (privilege-gain vs accountability-loss disjoint architectural-tells); D-5 M4 cross-agent boundary with F-1 `output-integrity` (disjoint-tells preventing duplicate detection on hybrid LLM-plus-mobile architectures); D-6 no schema bump (fourth BLP-01 detection feature reusing existing prefixes; explicit asymmetry to F-1 / F-2 / F-4 minor bumps; explicit asymmetry to ADR-031 D8 regex-alternation rule); D-7 ATT&CK Mobile catalog gap rule (3-of-3 prose-only at worst-case scale); D-8 Pattern Category Disambiguation across 5 companion catalogs; D-9 no `source_attribution` populator wiring extension (F-A3 deferral); D-10 (if 10-decision structure adjudicated per architect MEDIUM-2) public-only governance per SDR-001 Option C. **22-file zero-edit invariant preserved INCLUDING ALL F-1..F-6 NOT-edit** (10 other agents + 7 infrastructure consumers + the 5 prior BLP-01 detection feature outputs — `output-integrity.md`, `misinformation.md`, `tool-abuse.md`, `human-trust-exploitation.md`, `denial-of-service.md` from F-5, `model-theft.md` from F-5/F-6, `data-poisoning.md` from F-6, `tampering.md` from F-6 NOT touched despite being a sibling enrichment-branch agent extended at three-agent scope just one day prior); F-7 = 28+ detection-tier files post-F-6 inventory + 10 file edits + 0 net-new = 28+ files post-merge. **F-7 deliberately does NOT extend `source_attribution` populator wiring on the host agents** — that scope belongs to F-A3 (deferred per `schemas/finding.yaml` lines 230-238 and ADR-028 Decision 6); Mobile Top 10 citations on every emitted finding live in the prose-level `references:` array (existing field present in finding YAML schema since v1.0). **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. **Byte-identity SC-9 regression gate green at 6/6 baselines**: all 6 non-mobile example PDF baselines (`web-app`, `microservices`, `ascii-web-api`, `mermaid-agentic-app`, `free-text-microservice`, `maestro-reference`) regenerate byte-identically under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — mobile-tier topology gate per FR-006 (mobile client process AND mobile-tier indicator pair both required) guarantees zero Mobile Pattern Category emissions on these architectures, which guarantees unchanged downstream artifacts. **New `examples/mobile-banking-app/` baseline** (mobile-banking archetype with all 4 FR-006 mobile-tier indicators engaged: mobile client process, credential-handling component using SharedPreferences/NSUserDefaults, secure-storage data store, mobile-backend API process, third-party mobile SDK integration, exposed debug endpoint): **16 net-new mobile-tier findings emerged on regen** spanning all 10 Mobile Top 10 rows across all 5 host agents — cohesive category rendering preserved (each finding renders in its STRIDE host's category section without fragmentation; M8 dual-host findings render adjacent in `privilege-escalation` and `repudiation` sections respectively). **Strategic significance — BLP-01 Tier 2 second feature delivered + OWASP Mobile Top 10:2024 row 10/10 closure + OWASP four-framework total 40/40 milestone (second framework family closure)**: F-7 closes Mobile Top 10:2024 on the BLP-01 Coverage Matrix (10 rows transition Planned → Covered in a single bundle); BLP-01 progress: **10/11 features delivered** (Foundation + F-1 + F-2 + F-3 + F-4 + F-5 + F-6 + F-7); only F-8 Web/API Tier 3 remains. OWASP Mobile Top 10:2024 framework row coverage now **10/10**. Combined with F-4's OWASP Agentic Top 10:2026 row 10/10 + F-5's OWASP LLM Top 10:2025 row 10/10 + F-6's OWASP ML Top 10:2023 row 10/10, F-7 yields **OWASP four-framework total = 40/40 fully Covered** — the **second framework family closure milestone** after the AI-security family (LLM + Agentic + ML at 30/30 post-F-6) — pairs cleanly with the BLP-01 10/11 features delivered count and unlocks the final BLP-01 feature F-8 (Web/API Tier 3) bundle. Four-execution-deep Heuristic A enrichment-branch validation (F-3 single-agent + F-5 two-agent + F-6 three-agent + F-7 four-or-five-agent) combined with three-execution-deep standalone-branch validation (F-1 + F-2 + F-4) provides STABLE pattern lineage across all enrichment scopes for downstream F-8 Web/API Tier 3 + post-Tier-2 follow-ons. F-7 = first BLP-01 sub-pattern with cross-agent decomposition across two distinct STRIDE-tier hosts at architectural-tell axis (M8 privilege-gain vs accountability-loss); first BLP-01 feature to operationalize a cross-agent boundary with a previously-shipped BLP-01 agent (M4 boundary with F-1 `output-integrity`); first BLP-01 detection feature with worst-case 3-of-3 ATT&CK catalog-gap propagation (T1474 / T1626 / T1398 prose-only across all fixtures). 73 build-phase tasks complete + close-out tasks T074-T082 executing post-merge per `/aod.deliver` flow. 3 user stories delivered (US-237-1 mobile threat coverage across five existing host agents, US-237-2 lean enrichment discipline across five host agents, US-237-3 backward compatibility preservation under largest BLP-01 enrichment scope — all P0). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-04-28). PR #238 squash-merged to main 2026-04-29 (squash commit `e962a0e5b06d69c45aa3aa6217ebf9945b01f09e`). |
| 2026-05-01 | F-241: F-8 + F-A3 Web/API Coverage Attestation + Populator Wiring [Tier 3] | [241](../02_PRD/241-web-api-coverage-attestation-2026-04-29.md) | **Eleventh and final BLP-01 feature; fifth execution of Heuristic A enrichment branch at 11-host scope (full F-A3 closure across all detection-tier hosts: `spoofing` + `tampering` + `info-disclosure` + `privilege-escalation` + `repudiation` + `denial-of-service` + `tool-abuse` + `data-poisoning` + `model-theft` + `prompt-injection` + `agent-autonomy` per Architect HIGH-A 11-host expansion), fifth BLP-01 detection feature with zero schema bump, and the feature that CLOSES the BLP-01 11-feature initiative at 11/11 features delivered.** Closes the OWASP **Web/API combined Top 10:2021 + API Security 2023** surface — six Partial OWASP items transition Partial → Covered (A05 Security Misconfiguration + A06 Vulnerable and Outdated Components + API6 Server-Side Request Forgery + API8 Lack of Protection from Automated Threats + API9 Improper Inventory Management + API10 Unsafe Consumption of APIs) with pipeline-generated coverage percentages emitted on a new visible `9. Coverage Attestation` page in `security-report.pdf` across all 8 baselines (`web-app` + `microservices` + `ascii-web-api` + `mermaid-agentic-app` + `free-text-microservice` + `maestro-reference` + new `mobile-banking-app/sample-report` + new `predictive-ml-app/sample-report`). **F-A3 Populator Wiring closes the deferral debt** accumulated across ADR-032 (F-3 single-agent deferral) + ADR-034 D-8 (F-5 two-agent deferral) + ADR-035 D-10 (F-6 three-agent deferral) + ADR-036 D-10 (F-7 four-or-five-agent deferral) — `source_attribution` is now populated on all 11 detection-tier host agents with primary OWASP + related CWE citations rendered per-finding in the Coverage Attestation YAML block. **Headline outcome**: **OWASP five-framework total = 50/50 Covered** combined with F-2 + F-3 + F-4 + F-5 (LLM Top 10:2025 → 10/10) + F-3 + F-4 (Agentic Top 10:2026 → 10/10) + F-6 (ML Top 10:2023 → 10/10) + F-7 (Mobile Top 10:2024 → 10/10) + F-241 closures (Web/API combined OWASP Top 10:2021 + API Security 2023 → 10/10) — the **third framework family closure milestone** after the AI-security family (LLM + Agentic + ML at 30/30 post-F-6) and the four-framework total (40/40 post-F-7). **Heuristic A 5/5-dimension reduction re-holds at 11-host scope (maximum tested)** per ADR-037 D-1: zero new agent file (asymmetry to F-1 / F-2 / F-4 standalone-branch new-agent introductions; asymmetry to F-7 zero new agents at four-or-five-agent scope), zero new skill directory, **zero schema bump** (fifth BLP-01 detection feature reusing existing 12-prefix `S` `T` `I` `E` `R` `D` `LLM` `MI` `TE` `OI` `AG` finding-id family from post-F-4 baseline; schema `finding.yaml` `id.pattern` regex unchanged at v1.8 — first BLP-01 feature at 11-host scope to confirm Heuristic A precedent at maximum scope; closes ADR-035 + ADR-036 closing forward-scope-marker forecasts verbatim), zero consumers-list edit, zero functional orchestrator/dispatch edit. **Stream 4 aggregator extension at line 1073** (asymmetry to F-1..F-7 pattern-catalog-only edits): `scripts/extract-report-data.py:_load_framework_yaml_records()` line 1073 plus dual-emission `yaml_record_count` raw + `in_scope_yaml_record_count` filtered counters — first BLP-01 feature combining structural code work with the enrichment branch (per Architect M-2 carry-forward; T044/T045 Wave 4.3); stdlib-only module-load invariant preserved by AST walk in `tests/scripts/test_pyyaml_deferred_import.py` (KB-037 invariant regression-guarded). **Taxonomy YAML record-shape extension** (ADR-037 D-7) at separate-axis additive change tier: `owasp.yaml` + `mitre-atlas.yaml` + `mitre-attack.yaml` records gain two new fields — `out_of_scope: bool` (default `false`) + `out_of_scope_rationale: str` (default `""`) — backward-compat via YAML defaults; ATT&CK Enterprise expansion 38 → 701 records (323 in-scope + 378 OOS) at T053 with 7 verbatim T040 tactical-grouping rationale strings (TA0005/7/8/9/10/11/40) + 2 derived TA0112 + TA0043 curator extensions for post-spec STIX 2.1 update + 1 per-item T1078.004 OOS override; ATLAS expansion 12 → 30 records (all 30 in-scope). **CWE substitution rule** (ADR-037 D-7 extension) — 8-CWE canonical mapping table for catalog-absent CWE-307 / CWE-204 / CWE-311 / CWE-319 / CWE-326 / CWE-913 / CWE-451 / CWE-732; 12 distinct substitutions across T053 + T054 + T055 with 0 F-A2 referential-integrity errors. **Surgical Section 9 backfill approach** (ADR-037 D-11) — asymmetric to F-1 / F-2 / F-4 full-orchestrator regen pattern: surgical Section 9-only backfill for the 6 pre-existing baselines reduces wall-clock cost vs full regen by ~60% on 8-baseline scope; Tier 1 + Tier 3 parser code paths both read Section 9 via the same `_extract_source_attribution_block` regex; M8 dual-host runtime validation FIRST at T055 closes ADR-036 D-4 contract; V6 absent-key semantic codified for 6 findings without `source_attribution` keys. New **ADR-037** (`docs/architecture/02_ADRs/ADR-037-web-api-coverage-attestation.md`, Status: Accepted at squash-merge commit `e8a5370` per Proposed → Accepted **third execution** of dual-commit governance protocol after F-6 ADR-035 + F-7 ADR-036; SHA backfilled at Wave 6.3 T068 commit `8432cb5`; 10th consecutive ADR per ADR-027/028/029/030/031/032/033/034/035/036/037 lineage) records 13 D-numbered Decisions covering D-1 enrichment-vs-new-agent at 11-host scope; D-2 additive-only edits at 11-host scope; D-3 ATT&CK 38 → 701 expansion strategy; D-4 Web/API six Partial closure rule; D-5 ATT&CK tactical-grouping OOS strategy with 7 verbatim T040 + 2 curator extensions + 1 per-item override; D-6 BLP-01 §8 Quality Bar codification; D-7 taxonomy YAML record-shape extension + CWE substitution rule; D-8 Stream 4 aggregator at line 1073; D-9 stdlib-only module-load invariant + AST walk regression guard; D-10 Coverage Attestation as first-class deliverable at per-baseline rendering tier; D-11 surgical Section 9 backfill asymmetric pattern + V6 absent-key semantic; D-12 OWASP-only Tier-2 closure rationale (cross-framework primary attribution at 0.00% across ATT&CK / ATLAS / NIST AI RMF / CWE is a CHOICE, not oversight); D-13 dual-commit protocol third execution with provisional-date overwrite asymmetry. **22-edit grep checklist** (11 host agent files + 11 companion catalogs implicit) is the structural discipline that scales the F-7 five-host pattern to 11-host scope without losing the additive-only edit invariant; cost-per-host stays linear (~1 file edit per host) at 11x scope. **F-A3 deferral debt CLOSES at F-241** (ADR-032 + ADR-034 D-8 + ADR-035 D-10 + ADR-036 D-10 all marked CLOSED in ADR-037 D-1). **Zero new runtime dependencies** — empty diff on `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, `package.json`. **Byte-identity SC-15 regression gate green at 6/6 prior baselines on non-Coverage-Attestation pages** under `SOURCE_DATE_EPOCH=1700000000` per ADR-021 — intentional baseline updates only on the new `9. Coverage Attestation` page (SC-015 8/8 PASS); 2 new baselines added (`mobile-banking-app/sample-report` + `predictive-ml-app/sample-report`) bring the byte-identity gate scope to 8 baselines total. **Strategic significance — BLP-01 11-feature initiative CLOSES + OWASP Web/API five-framework total 50/50 milestone (third framework family closure)**: F-241 closes the BLP-01 Coverage Matrix at 11/11 features delivered (Foundation + F-1 + F-2 + F-3 + F-4 + F-5 + F-6 + F-7 + F-241); only post-BLP-01 forward-scope items remain (cross-framework primary attribution at ATT&CK / ATLAS / NIST AI RMF / CWE per ADR-037 D-12 — deferred to hypothetical F-9+ scope as BLP-02 envelope candidates); Heuristic A enrichment branch is now precedent-validated at single-agent (F-3) + two-agent (F-5) + three-agent (F-6) + four-or-five-agent (F-7) + 11-host (F-241) scopes — pattern is now **load-bearing-and-saturated**; no further fan-out is possible within the current detection-tier surface (11/11 hosts populate `source_attribution`). Coverage Attestation is now a first-class deliverable at the per-baseline rendering tier — pipeline-generated coverage percentages replace the hand-curated `_internal/strategy/BLP-01-threat-coverage.md` §6 Coverage Matrix (demoted to historical at T061 Wave 6.2). **29-day envelope collapsed to single-day delivery** (2026-05-01 plan-day → 2026-05-01 close-out) with all reserve days untouched — steepest acceleration in BLP-01 vs F-7 (~19h) + F-6 (~2.5 days). **Single-PR delivery** — symmetry to F-7's clean single-PR delivery; asymmetry to F-6's two-PR cherry-pick recovery. 4 user stories delivered (US-241-1 per-finding source attribution renders in PDF P1, US-241-2 six Partial Web/API items close to Covered with citation evidence P1, US-241-3 §6 Coverage Matrix demoted to historical with pipeline-generated attestation P1, US-241-4 populator wiring closes F-A3 deferral debt P1 — all P1). 84/84 build + close-out tasks complete (77 build + 7 close-out per Wave 6.3 T067-T078 + T080-T084 sanity-check waves). Governance: PM APPROVED + Architect APPROVED_WITH_CONCERNS + Team-Lead APPROVED_WITH_CONCERNS on tasks.md (2026-05-01); Architect 0 BLOCKING / 0 HIGH / 2 MEDIUM (M-1 ADR-027 forward-pointer + M-2 aggregator filter line) / 2 LOW (L-1 canonical baseline path + L-2-arch T005 enumeration); all 7 plan-time concerns RESOLVED inline during build; 1 post-build surfacing at Wave 6.2 T070 (test-convention mismatch — NOT citation gap; 2 of 14 tests in `test_coverage_attestation_audit.py::TestCitationCompleteness` fail under bare-id substring matching stricter than spec-defined `# citation:` comment-anchored evidence convention) flagged as follow-on Issue trigger with Option (3) recommended (walk `# citation:` comment evidence directly per spec). PR #242 squash-merged to main 2026-05-01 (squash commit `e8a5370`); release-please PR #243 (`chore(main): release 4.27.0`) fired ~30s post-merge per F-212 + F-7 + F-6 cumulative validation — F-212 incident NOT invoked; ADR-037 SHA backfilled at T068 commit `8432cb5`; v4.27.0 release tag confirmed. |
| 2026-05-10 | F-282: F-5 Pre-commit Secret-Scanning Defaults (BLP-02 Wave 4+) | [282](../02_PRD/282-pre-commit-secret-scanning-defaults-2026-05-09.md) | **Fifth and final feature in the BLP-02 enterprise-hardening initiative; BLP-02 CLOSES at 5/5 features delivered** — LinkedIn-thread punch-list (F-3 SECURITY.md disclosure / F-4 CLAUDE_PERMISSIONS.md baseline / F-5 secret-scanning) fully retired. Adopter-facing default-deny secret-scanning hook closes the credential-exfiltration risk that prior BLP-02 features (F-1 substitution surface, F-2 source-pattern hardening, F-3 disclosure channel, F-4 Claude permissions baseline) left as the load-bearing remaining gap. New artifacts: `.pre-commit-config.yaml` (gitleaks v8.30.1 pinned commit hash per AC-1 supply-chain discipline; `pre-commit autoupdate --freeze` converts tag to SHA in-place); `.gitleaks.toml` (~50-80 LOC; gitleaks v8.25.0+ `[[allowlists]]` array-of-tables schema; 6-entry allow-list for env-var placeholders / fixture paths / docs paths / `.aod/personalization.env.example`; 2 additive tachi-tuned custom rules `tachi-personalization-env` warn-only + `tachi-security-exceptions-jsonl` warn-only; defaults catch the five canonical credential patterns AWS/GitHub-PAT/OpenAI/Anthropic/generic-high-entropy); `.aod/scripts/bash/precommit-wrap.sh` (~30-60 LOC wrapper per Architect C-2 resolution because gitleaks does NOT support stderr-message templating at CLI level — wrapper augments gitleaks' default rule-ID + file:line output with `SKIP=gitleaks git commit ...` bypass guidance + `See docs/standards/PRECOMMIT_HOOKS.md` docs link to deliver the AC-15 four-item structured stderr contract); new `.aod/personalization.env.example` init template (~10-20 LOC; placeholder values; path-allow-listed in `.gitleaks.toml`; addresses Architect C-1 adopter-onboarding friction); `scripts/init.sh` delta (TTY prompt `[ -t 0 ]` baseline + `--no-precommit` and `--precommit` CLI flag override per Q4; `pre-commit --version` floor check ≥3.5.0 per R-10; "hard-exit on `.aod/personalization.env` present" idempotency pattern preserved per Team-Lead A-3 making re-init impossible by design); `docs/standards/PRECOMMIT_HOOKS.md` (~150-250 LOC operator handbook; 9 mandatory sections — Why-this-hook-ships / Installation-paths / What-gets-scanned / Bypass-mechanisms / Refused-commit-error-message-contract / CI-parity / Re-init-behavior / Known-limitations / Adopter-customization); ADR-042 Accepted (130-180 LOC; 9 alternatives-considered with rejection rationale including GitGuardian / SecretLint / git-secrets / detect-secrets / talisman / commit-msg-hook / server-side-hook / pre-receive-hook / no-op-baseline dismissals; pin-bump cadence policy per Architect A-2 = bump-on-each-gitleaks-minor-release with empirical re-verification; raw read-p waiver per Architect C-3 = single-character Y/n below F-1 free-text input-validation threshold); `.github/workflows/gitleaks.yml` (~25-40 LOC CI back-stop per Team-Lead A-2 — replaces "extend existing security-scan workflow" framing because NO existing security-scan workflow existed at PRD-author time; invokes gitleaks binary directly to sidestep gitleaks-action license issue for org repos; full-repo scope on PRs per Architect A-4 /aod.spec resolution); `tests/fixtures/gitleaks-rule-interaction/` (16-fixture corpus + run.sh runner — AC-SPEC-1 preventive false-positive verification enforced at /aod.spec entry per Architect C-4: 6 should-fire fixtures + 4 placeholder should-NOT-fire + 4 path-allow-listed should-NOT-fire + 2 path-excluded should-NOT-fire — replaces reactive v4.34.x hot-patch with preventive synthetic-fixture rule-interaction test); `tests/scripts/test_init_precommit_matrix.py` (6-case prompt-flag pytest matrix). 30/37 tasks complete from build phase + 7 /aod.deliver-time tasks (T030-T036) in progress; 4 P1 user stories shipped (US-282-1 first-time adopter / US-282-2 default-deny / US-282-3 existing-adopter no-surprise / US-282-4 false-positive avoidance) + 2 P2 (US-282-5 SecOps reviewer / US-282-6 CI back-stop) all passing. PR #283 squash-merged as `18378bd` on main 2026-05-10. **Initiative closure**: BLP-02 5-feature enterprise hardening (F-1 #248 Substitution Surface Hardening 2026-05-04 → F-2 #256 Source-Pattern Hardening 2026-05-05 → F-3 #272 SECURITY.md and Private Disclosure Channel 2026-05-08 → F-4 #277 Claude Permissions Baseline 2026-05-09 → F-5 #282 Pre-commit Secret-Scanning Defaults 2026-05-10) DELIVERS COMPLETE at 5/5; ~6-day envelope start-to-finish; LinkedIn-thread enterprise-developer-environment posture trigger (2026-05-02 Daniel Wood thread) fully addressed across F-3 / F-4 / F-5 across the disclosure / permissions / secret-scanning axes. |

# Stream 3 fixture: Records with out_of_scope_rationale populated (F-241 T048).
#
# Purpose: verify that the `out_of_scope_rationale` field carries verbatim
# T040 rationale strings (per data-model.md §5 tactical-grouping table) AND
# derived rationale strings (per the T041 §3.4 curator decision documented
# at .aod/results/T041-T043-attack-expansion.md).
#
# Verbatim T040 rationales — uniform tactic-level Out-of-Scope rationale
# applied to all member items inside a design-time-irrelevant ATT&CK tactic.
#
# Derived rationales — authored using T040's narrative pattern with TA-ID
# substitution, applied to post-spec STIX-update tactics (TA0112 Defense
# Impairment + TA0043 Reconnaissance) NOT enumerated in data-model.md §5.
#
# Expected behavior:
#   - All 4 records carry `out_of_scope: true` + non-empty rationale
#   - The in-scope filter excludes all 4 records
#   - `_load_framework_yaml_records('synthetic_oos_with_rationale', in_scope_only=True)`
#     → returns [] (all 4 records filtered)

# Record 1: VERBATIM T040 rationale — TA0005 Defense Evasion
- id: T1027
  full_id: ATT&CK-T1027
  name: Obfuscated Files or Information
  url: https://attack.mitre.org/techniques/T1027/
  cwe_refs: []
  out_of_scope: true
  out_of_scope_rationale: "Tactic TA0005 operates at runtime via active-malware behavior; outside tachi's design-time threat-modeling scope."

# Record 2: VERBATIM T040 rationale — TA0007 Discovery
- id: T1018
  full_id: ATT&CK-T1018
  name: Remote System Discovery
  url: https://attack.mitre.org/techniques/T1018/
  cwe_refs: []
  out_of_scope: true
  out_of_scope_rationale: "Tactic TA0007 operates at runtime via active reconnaissance; outside tachi's design-time threat-modeling scope."

# Record 3: DERIVED rationale — TA0112 Defense Impairment (post-spec STIX update)
- id: T1562
  full_id: ATT&CK-T1562
  name: Impair Defenses
  url: https://attack.mitre.org/techniques/T1562/
  cwe_refs: []
  out_of_scope: true
  out_of_scope_rationale: "Tactic TA0112 operates at runtime via active defense impairment; outside tachi's design-time threat-modeling scope."

# Record 4: DERIVED rationale — TA0043 Reconnaissance (post-spec STIX update)
- id: T1595
  full_id: ATT&CK-T1595
  name: Active Scanning
  url: https://attack.mitre.org/techniques/T1595/
  cwe_refs: []
  out_of_scope: true
  out_of_scope_rationale: "Tactic TA0043 operates at runtime via pre-compromise reconnaissance; outside tachi's design-time threat-modeling scope."

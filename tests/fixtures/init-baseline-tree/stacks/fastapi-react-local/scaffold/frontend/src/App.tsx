/**
 * App shell component.
 *
 * Top-level layout for the application. Add your router
 * setup here (e.g., React Router, TanStack Router).
 */

// Add your router provider here (e.g., <RouterProvider router={router} />)

export function App() {
  return (
    <main className="min-h-screen bg-white text-gray-900">
      <div className="mx-auto max-w-7xl px-4 py-8">
        <h1 className="text-3xl font-bold text-brand">Welcome</h1>
        <p className="mt-2 text-gray-600">
          Replace this with your application content.
        </p>
      </div>
    </main>
  );
}

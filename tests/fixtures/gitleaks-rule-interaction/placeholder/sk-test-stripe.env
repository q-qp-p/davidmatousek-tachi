# F-5 fixture #10 (T010 — should-NOT-fire)
# Allow-list mechanism: regex `sk-test-\.\.\.` in `.gitleaks.toml` allow-list 1
# Pattern: Stripe-style test-key prefix + ellipsis
# Why this exists: tutorial/docs style placeholder for Stripe test mode
STRIPE_KEY=sk-test-...
STRIPE_PUBLIC_KEY=pk-test-...

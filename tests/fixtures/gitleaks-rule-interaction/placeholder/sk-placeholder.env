# F-5 fixture #9 (T010 — should-NOT-fire)
# Allow-list mechanism: regex `sk-PLACEHOLDER\.\.\.` in `.gitleaks.toml` allow-list 1
# Pattern: OpenAI-style key prefix + ellipsis (didactic stand-in)
# Why this exists: tutorial/docs style placeholder
OPENAI_API_KEY=sk-PLACEHOLDER...

# F-5 fixture #8 (T010 — should-NOT-fire)
# Allow-list mechanism: regex `PLACEHOLDER` in `.gitleaks.toml` allow-list 1
# Pattern: literal PLACEHOLDER string as value
# Why this exists: didactic placeholder convention (matches `.aod/personalization.env.example`)
OPENAI_API_KEY=PLACEHOLDER
ANTHROPIC_API_KEY=PLACEHOLDER

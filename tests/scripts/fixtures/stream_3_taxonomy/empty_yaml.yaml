# Stream 3 fixture: Empty YAML — zero-denominator case (F-241 T048).
#
# Purpose: verify that an empty taxonomy YAML (zero top-level records)
# does not crash the loader and returns an empty list. This exercises the
# `data is None` branch at `_load_framework_yaml_records()` line ~1107.
#
# Expected behavior:
#   - `_load_framework_yaml_records('synthetic_empty', in_scope_only=False)` → []
#   - `_load_framework_yaml_records('synthetic_empty', in_scope_only=True)`  → []
#   - `load_framework_yaml_record_counts()['synthetic_empty']`               → 0
#   - `load_framework_yaml_in_scope_record_counts()['synthetic_empty']`      → 0
#   - Aggregator emits `coverage_percentage: "N/A"` per data-model.md §3
#     (in_scope_yaml_record_count == 0 → "N/A" branch at line 1205)

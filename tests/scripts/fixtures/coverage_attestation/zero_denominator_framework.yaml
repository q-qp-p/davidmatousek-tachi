# Coverage Attestation Fixture — zero_denominator_framework (SYNTHETIC)
#
# Feature 194 Wave 1.1 / T005: Zero-denominator edge test.
#
# Simulates an empty framework catalog (zero top-level records). When
# ``yaml.safe_load``-ed, this file produces an empty list ``[]``. Feeds tests
# exercising the coverage-percentage aggregator's guard clause for division-
# by-zero — when a framework has zero catalog items, the per-framework
# coverage percentage MUST render as ``N/A`` (not ``0%``, not raise).
#
# Used-by (planned):
#   - tests/scripts/test_coverage_attestation.py::test_coverage_percentage_na_on_zero_denominator
#
# ARCHITECT CONSTRAINT M-3 (DO NOT VIOLATE):
# This fixture MUST NOT replace any real ``schemas/taxonomy/{framework}.yaml``
# file at runtime. The aggregator runs against the real taxonomy files;
# this synthetic fixture is injected via a test-scoped monkeypatch only
# (e.g., ``monkeypatch.setattr(module, "_load_catalog_ids", stub)`` or a
# ``taxonomy_dir`` parameter override pointing at this fixture file's
# parent directory). Any test that uses this fixture MUST restore the real
# catalog path before exiting.
#
# No edits to ``schemas/taxonomy/`` have been made by Feature 194 — the
# zero-denominator scenario is exercised exclusively through this fixture.

[]
